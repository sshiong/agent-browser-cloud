# 中英文 README 与双语漂移校验

> 日期：2026-09-20
> 状态：已完成

## 实现

- `README.md` 继续作为简体中文主入口，新增 `README.en.md` 英文版本；两者顶部均提供明确语言切换。
- 英文版完整覆盖安全声明、环境要求、OrbStack 约束、模型 Provider 配置、快速与分步启动、真实浏览器
  Gate、Personal Secure 边界、仓库结构、文档、开发命令、API 与技术栈。
- 修正中文 README 中“默认 Compose 尚未启动独立 Worker”的过期描述，使其与当前四 Worker 真实运行
  链一致；补充聚合模型别名、可选响应模型锁定、超时和输出预算配置。
- 文档生成器同时维护两份 README 的 Git 跟踪模块表，并按中文、英文分别生成表头与分隔符；任一文件
  缺失、模块表漂移或本地链接失效都会使 `make docs-check` 失败。

## 验证

| Gate | 结果 |
| --- | --- |
| `make docs-generate` | 通过；中英文模块表均可重复生成 |
| `make docs-check` | 通过；文档工具 7 项测试、两份 README 模块表及本地链接均通过 |
| `git diff --check` | 通过 |

## 边界

架构与进度证据仍以现有中文 `docs/` 为权威来源；英文 README 提供完整项目入口，但本切片不承诺已将
全部深层设计文档翻译为英文。
