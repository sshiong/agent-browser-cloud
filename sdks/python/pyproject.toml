[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[project]
name = "agent-browser-cloud"
version = "0.1.0"
description = "Agent Browser Cloud Python SDK"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["."]
include = ["browsercloud*"]
