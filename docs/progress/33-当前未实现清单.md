# 当前未实现清单

> 审计日期：2026-08-24
> 大纲来源：`docs/outline/` 下代码骨架、V16 架构设计和开发实施计划
> 口径：仓库内可重复验收、目标环境生产验收和组织签字分开判定。

## 结论

Phase 4 MVP（含低风险 Challenge 视觉自动化、SAFE/AUTONOMOUS 一次性敏感输入、按需
人工 OTP 续行、结构化 Agent Browser、原生 Dialog、文件上传/下载、状态围栏截图与受治理
Evaluate）、Phase 6 Browser Density Stage B、Operator N/N-1 本地滚动演练和
Phase 7 企业运营核心切片已经完成。仓库不再是“Phase 7 未开始”的状态，但仍没有通过
V16 全量生产发布 Gate，不能据此处理真实客户数据。

| 类型 | 当前剩余 |
| --- | --- |
| 代码产品化缺口 | Agent Browser 剩余高级 Primitive；Warm Tier SQLite/LevelDB 应用感知 Adapter、Multipart Resume、跨 Region Restore 与对象 Legal Hold 深度联动；Phase 3 目标 CRM/支付/IAM/邮箱/短信 Provider 的真实凭据获取、字段/事务映射与特有认证接入（Agent 一次性账号/密码/OTP 输入 API 已完成），目标云 Secret 解引用、商业 Proxy Adapter 与高级 SLA/业务成功率路由、Phase 4 高级 Validator/客户 Replay/跨 Region Workflow、目标云 GameDay Controller |
| 已有本地实现或 Stage A | Cgroup/PSI、并发 Browser Density、Validation Farm/隔离 Worker 队列/自动矩阵、Replay、Cost/SLA/Retention/Compliance/DR Registry、Terraform Module、四语言 SDK、N/N-1 Kind 演练 |
| 依赖目标环境 | 目标云 CNI/CSI/KMS/IAM/LSM、真实企业 IdP、HSM/Offline Root、真实多 Region 复制/切换、Pager 到达、Linux 节点压力长稳 |
| 组织 Gate | Owner/RACI、Threat Review、Residual Risk、Staging、发布审批与值班签字 |

## 按阶段列出的剩余项

### Phase 0：工程和契约

1. 核心模块 Primary/Secondary Owner、RACI、Threat Review 和发布审批签字；
2. 在正式 Release Pipeline 中以本轮 N/N-1、回滚、契约和迁移 Gate 阻断发布；
3. 对真实 Staging 制品完成下载后验签、升级、回滚和证据归档。

### Phase 1：Browser Runtime

1. 目标 Linux 节点的委派 Cgroup v2、OOM/PSI、Xvfb/x11vnc 并发长稳；
2. Renderer/Browser/Node 组合故障、磁盘/网络预算和多 Node 容量证书；
3. 独立 GPU Helper、硬件编解码和 GPU/Media 隔离证书。

本机真实 Chrome 已完成 500 次顺序生命周期和 500 次、并发 4 的 Browser Density
证书；这些证据不替代目标 Linux/目标云验收。

### Phase 2：Session Coordinator

1. 双 Coordinator 长稳下的数据库延迟、连接池拥塞和时钟偏差；
2. PostgreSQL 权威 Hot Tenant 逻辑路由、安全点迁移和旧 Route Epoch 拒绝已在
   [进度 63](63-Hot-Tenant权威路由与Route-Epoch安全迁移.md)完成；Node Command
   Outbox 物理分片派发与 Node 双重栅栏已在
   [进度 64](64-Node-Command物理分片派发与双重栅栏.md)完成；Session Coordinator
   HTTP/Timer/Workflow 物理 Owner 路由已在
   [进度 83](83-Session-Coordinator物理Shard命令路由闭环.md)完成；Reviewer 集成进一步
   验证并关闭了物理 Shard 重平衡期间向仍存活逻辑 Owner 投递的切换窗口，入口、Inbox
   Claim、Processor 与 SessionCoordinator 使用一致栅栏；仍缺双 Coordinator 热点长稳
   压力证书；
3. Session Ownership 与 Session/Group/Tag 列表批量投影已由
   [进度 92](92-Session与Group-Tag列表批量投影闭环.md)关闭；Agent 摘要投影、稳定
   Cursor 和按需详情已由[进度 96](96-Agent任务大列表轻量投影与游标分页闭环.md)
   关闭；仍缺目标规模容量证书；
4. 目标 Prometheus/Alertmanager/Pager 的跨 Pod 聚合、告警到达和关闭演练。

### Web Console

1. Workspace Groups/Tags 正式 PostgreSQL 模型/API/UI、创建 Session 的 Group/Tags
   归属和 Group 默认 AUTO 策略继承已完成；全局搜索已在
   [进度 86](86-全局搜索正式闭环.md)接入 PostgreSQL 有界索引、正式 API、RBAC 和
   Web/Tauri 共享命令面板；工作区通知中心已在
   [进度 89](89-工作区通知中心正式闭环.md)接入高信号 Audit 事务投影、正式 API、
   Tenant/Actor 已读游标和 Web/Tauri 共享面板；用户主题已在
   [进度 90](90-用户主题偏好与浅色控制台正式闭环.md)接入 Tenant/Actor PostgreSQL
   偏好、SYSTEM/DARK/LIGHT、正式 API 和共享主题层；认证用户菜单已在
   [进度 91](91-认证用户菜单与权限入口闭环.md)接入真实身份、角色权限入口和 OIDC
   退出；[进度 93](93-Workspace批量生命周期与组合筛选闭环.md)已完成 Group/Tag
   批量 Start/Pause/Migrate/Hibernate 的持久 Operation、真实进度/取消，以及
   Environment Group+Tag ANY/ALL 服务端组合筛选；工作区即时推送已由进度 108
   关闭，通知的跨 Region 顺序/复制、外部渠道和目标规模长稳属于后续生产深度；
2. Environment Import 已在[进度 78](78-Environment-Import正式闭环.md)接入
   PostgreSQL 权威预检/执行账本、全事务创建、正式 API 和 Web/Tauri 复用组件；
   Profile 内容/Checkpoint Import 已在[进度 79](79-Profile-Checkpoint-Import正式闭环.md)
   接入 PostgreSQL Job、mTLS Node 流、Storage Helper、对象存储、审计与 Web/Tauri
   共享组件；创建时可复用 Proxy Binding 已在
   [进度 82](82-可复用Proxy-Binding创建时闭环.md)完成；Provider Catalog 和运行中
   Safe Point Rebind 已在[进度 85](85-多Provider与SafePoint代理重绑闭环.md)完成。
   运行中主动出口探测、质量与新鲜度投影已由
   [进度 99](99-Proxy主动健康探测与质量投影闭环.md)关闭；未运行 Session 的冷 Binding
   探测由[进度 100](100-Proxy冷Binding探测与分布式租约闭环.md)以 PostgreSQL 租约、
   能力化 Node RPC 和隔离 Network Helper 关闭。目标云 Secret 解引用、商业 Provider
   Adapter 和环境更多操作仍未完成。质量/信誉/成本/地域/容量的确定性自动路由已由
   [进度 102](102-Proxy多Provider质量成本地域路由闭环.md)关闭；高级 SLA/业务成功率
   学习、Challenge/黑名单与供应商多样性仍未完成。
   Saved View 已在[进度 77](77-Environment-Saved-Views正式闭环.md)接入 PostgreSQL、
   正式 API、RBAC、CAS、幂等、审计和 Web/Tauri 复用组件；Group/Tag 组合条件的
   V067 Schema、租户验证、OpenAPI 和保存/恢复 UI 已由
   [进度 94](94-Saved-View-Group-Tag筛选持久化闭环.md)关闭；Group/Tag 批量归属、
   批量移除等 Workspace 元数据 Mutation 已由
   [进度 95](95-Workspace批量元数据Mutation闭环.md)关闭；
3. Runtime Build、Agent Policy 和 Extension 已成为创建时固化的一等契约；Agent
   Policy 在 Planner、Executor 和 Handoff 路径由服务端强制，Extension 由 V038 从旧
   Demand 回填并在 Session List/Detail 正式投影。Session 已返回 Group、Tags、
   Runtime、HumanTakeover、Agent Policy 和 Extension 基础投影；Session Ownership
   与 Group/Tags 列表 N+1 已由[进度 92](92-Session与Group-Tag列表批量投影闭环.md)
   关闭；Agent 队列宽 JSON 读取已由
   [进度 96](96-Agent任务大列表轻量投影与游标分页闭环.md)关闭；
   Execution Environment 和 AUTO Resource Policy 已是一等契约，Resource Template
   只保留为后端内部调度实现；V042 已补齐五分钟真实成本趋势、`maximumCostPerHour`
   决策、费率缺失 fail-closed 和上限前一次性 Node 非核心预算降载；V043 已补齐真实
   CDP 后台 Tab 冻结、新建 Tab 阻断、非特权 Extension 后台暂停及 Node ACK；V045
   已补齐成功命令 Trace 动态采样，失败和强制证据保持全量；V046/进度 69 已补齐
   Observer 帧率真实 Gateway 执行与 ACK；V047/进度 70 已补齐独立 CDP Pixel
   Recording、有界分段、Storage Helper 对象提交与上限停止；V048/进度 71 已将
   Session、Placement、企业费率、成本解释、OpenAPI 和四 SDK 的公开 Legacy L1−L5
   迁移为 Template；V049/进度 72 已补齐 Agent Navigate/Action 的真实 CDP
   Screenshot、成功采样执行器、Storage Helper 提交、FAILED Evidence、租户 API 与
   Web 卡片；V062/进度 87 已补齐 Observer 手动截图、Purpose-bound 五分钟 Grant、
   同操作者一次性 Redeem 和短期签名 GET；V063/进度 88 已补齐 Agent/Observer 共用的
   Password/OTP/Payment/Account/Secret/PII 语义分类、不透明遮罩、页面抗篡改复核和
   Redaction State/Count。当前仍缺保留期/Legal Hold 对象生命周期深度联动、
   Site Policy 与 Recording 帧级遮罩已分别由进度 137、138 关闭；当前仍缺无语义视觉
   文本/OCR 分类、对象 WORM/Retention/Legal Hold 深度联动和目标环境长稳。低风险
   Challenge 已由[进度 146](146-Challenge视觉自动化与人工兜底闭环.md)补齐脱敏截图
   OCR/视觉定位、默认三次点击/连续点击/滑动与失败通知；
   [进度 147](147-Agent安全与自动模式及一次性敏感输入闭环.md)进一步补齐默认 SAFE、
   可显式开启 AUTONOMOUS、账号/密码/OTP 一次性密文 API 和默认三次输入重试；
   [进度 148](148-AUTONOMOUS按需人工协助与OTP续行闭环.md)把自动穷尽改为只通知一次并
   保留原 Task，支持人工发送 OTP 由 Agent 有界代填后续行，或由人工自愿进入协作填写，
   不强迫接管。仍缺目标 IAM/邮箱/短信 Provider 主动取值和通用敏感像素分类器；
4. Resource/Migration、Browser State、Session、Audit、Operation 和 Agent Task 已在
   [进度 84](84-Session统一事件流与轮询替换.md)统一到 PostgreSQL 事务游标、可续传
   SSE，并删除 Session/State 固定轮询；工作区通知的 15 秒刷新也已由
   [进度 108](108-工作区通知可续传SSE闭环.md)替换为 Tenant 级 Audit Sequence
   可续传 SSE；   [进度 142](142-SecureDebug通知准入修复与轮询替换边界.md)已把 Break-glass、
   Key Rotation 与 Secure Debug 三个治理列表改为租户通知游标无损驱动并移除固定
   轮询；[进度 143](143-租户审计链可续传SSE与审计轮询移除.md)进一步以防篡改链
   `sequence_no` 新增租户级审计 SSE，移除安全中心最后一处轮询；Browser Node 心跳
   新鲜度转换与 Nodes 页面五秒轮询已由
   [进度 144](144-Browser-Node心跳新鲜度事件与轮询移除.md)关闭：稳定心跳不产生日志，
   仅 `FRESH / STALE` 转换进入可续传 SSE；Enterprise Overview 的 Validation、Cost、
   Media、SLO/Freeze、SLA、Retention、License、Region、GameDay/Trend/Remediation 与
   Compliance 全域专用单调流、时间窗口到期失效和 15 秒轮询移除已由
   [进度 145](145-Enterprise-Overview全量事件源与轮询移除.md)关闭。仍缺跨 Region
   Event Bus、列表批量流和大规模慢客户端/Ingress 长稳；
5. TypeScript Client 的 OpenAPI 全量自动生成、独立锁、漂移 Manifest、Node ESM、
   运行时与发布 Tarball 门禁已由[进度 101](101-OpenAPI自动生成TypeScript-SDK闭环.md)
   关闭；Python/Go/Java 的 166 Operation/224 Schema 生成、统一版本、确定性发布包与
   GitHub OIDC Provenance Release 流水线已由
   [进度 103](103-Python-Go-Java-OpenAPI生成与SDK供应链发布门禁.md)关闭；仍缺 npm、
   PyPI、Maven Central 等目标组织 Registry 的命名空间/Trusted Publishing、撤销和
   客户升级策略；
6. 真实企业 IdP 的 Metadata、Claim、MFA/ACR、Logout 和租户映射联调；
7. Browser Node 的 CPU/内存/PSI/Input Ledger、Renderer/Tab、CDP 主线程执行压力、
   Agent Action 延迟、持久 State Diff 深度和 Remote Desktop Frame Age 5 秒采集，
   以及同节点 Cgroup Actuator、Safe Point、自动休眠、严格终止和持久跨 Node 迁移
   核心链及 State Collector Budget、Remote Desktop Bitrate 在线执行器已完成；
   CDP 已真实补齐文件上传、文件下载和导航级表单提交 Safe Point 信号；Linux Cgroup
   v2 Browser 子组的真实 I/O 速率、API、决策和 UI 已完成。应用侧文件传输、SPA/Form、
   支付/账号安全、关键事务和 Business Recovery Unknown 已具备 PostgreSQL 短 Lease
   Producer API、Owner/TTL、审计事件、SSE 和迁移并发屏障；通用 CDP 路径启发式又在
   [进度 136](136-CDP业务事务SafePoint自动聚合闭环.md)补齐非幂等 SPA Mutation、常见
   支付/账号安全与关键事务的短时保护；[进度 137](137-版本化Site-Policy浏览器事务规则闭环.md)
   又将站点自定义事务 Path Prefix 纳入精确 Revision、双人审批、Session 绑定、Node
   哈希校验和 N−1 fail-closed Gate。仍缺各业务应用实际接入的 Adapter/自动埋点和
   可证明的完成语义。版本化 Application Recovery Contract、受限 DSL、持久 Verdict、
   Session 绑定、迁移 Ready Gate、按预算执行的 Reload/Refresh/受限导航、State ACK、
   二次 Ready Gate 和 Web 结果卡已完成；V039 已补齐受信 `RESTART_EXTENSION`；
   契约作者 UI 已在进度 62 完成；V050/进度 73 已完成精确版本双人审批、Session
   版本固定和审计；V051/进度 74 已完成不可变历史正文、精确旧版本继续运行和既有
   Session 显式 Rebind Operation；进度 75 已完成不可变版本列表、服务端字段 Diff
   和“历史版本生成新 DRAFT、重新审批”的受控回滚；V052/进度 76 已完成精确 Revision
   绑定的 Provider Evidence 要求、受信 Adapter 写入、短 TTL、幂等/审计和迁移
   fail-closed Gate。仍缺目标 CRM/支付等 Provider 的真实凭据与 Adapter 实际接入。
   Extension 可信加载、
   Session 级独立 Cgroup CPU/RSS 和 Weight Operation/ACK 已完成，但仍缺目标 Linux
   真实企业扩展长稳与逐 Extension 归因。x11vnc Media CPU Producer、独立 Cgroup、
   V032 当前 Slot、Operation/ACK 和 Web 展示已完成；仍缺硬件 Codec/GPU Helper、
   编码器级动态码率/封装/播放、Recording 治理和目标 Linux 长稳；仓库级双 Node +
   MinIO + CDP 数据面迁移 E2E 已在[进度 80](80-双BrowserNode对象存储迁移集成闭环.md)
   完成；N Node 会声明 `startRuntimeGenerationFloor=v1`，迁移候选在 PostgreSQL
   查询与服务层双重拒绝不支持 Generation Floor 的 N−1 Node，并由不可达旧 Node
   集成夹具证明不会误调度。V056/进度 81 已进一步完成目标 Restore 失败后的 fenced
   Cleanup、失败节点持久排除、最多三次重试和三 Node Storage Helper 故障注入。
   仍需目标 Linux/正式 Chromium 的源/目标进程宕机、网络分区、多 Coordinator 竞争和
   长稳矩阵；
   V092/进度 129 已关闭 `NODE_ACK_TIMEOUT` 后迟到 ACK 导致的 Node/Placement 权威漂移：
   严格匹配最新 Ledger、旧/新快照、Node、Policy 和 Operation 后以独立补偿 Operation
   对账，冲突则进入 Critical；ACK 永久丢失时的周期 Resource Readback/Drift Scanner 已由
   [进度 130](130-AUTO资源周期Readback与漂移扫描闭环.md)关闭，仍缺上述目标 Linux
   组合长稳；
   Browser/Profile I/O 仍需目标 Linux 长稳与路径级归因（如业务要求）；
8. Tauri 2 容器、平台适配器、OS 安全存储、系统浏览器 OIDC/Deep Link、Updater
   门禁和 macOS 本机无安装包构建已完成；仍缺 Apple/Microsoft 代码签名与
   Notarization、真实更新源/企业 IdP、Windows 真实验收，以及桌面视觉回归、
   全键盘、屏幕阅读器和休眠/网络切换矩阵；
9. Workspace Settings 的工作区名称、默认 Runtime/Region/HumanTakeover 已在 V036
   接入 PostgreSQL、正式 API、RBAC、幂等与审计；字段标签和基础窄屏布局已修复。
   Saved View 已在进度 77 关闭；触控目标 44px 命中区、深/浅主题 Token 对比自动
   回归 Gate 和 Session Detail 路由包拆分已由
   [进度 141](141-AUTO资源清单复核与WebConsole可访问性技术质量收口.md)关闭；
   仍缺 Proxy Binding/Import 等设置域，以及全键盘、屏幕阅读器、200% 缩放等
   完整辅助技术矩阵，详见
   [当前剩余实现与前端技术质量审计](53-当前剩余实现与前端技术质量审计.md)。

环境页分层、独立空状态、服务端搜索、六步 AUTO 创建、Resource Policy/Placement/
Event 详情、基础与首批细粒度 5 秒真实遥测、同节点在线扩缩容、Nodes、Extensions、
Workspace Groups、Enterprise Operations、OIDC/RBAC 与 Viewer/移动端 E2E 已接真实
API；Workspace Tags 也已在 V035 接入正式模型/API/UI，不再属于未实现项。
AUTO 成本与上限前降载的实现和剩余边界见
[进度 65](65-AUTO成本趋势与上限前降载闭环.md)。
标签页执行器见[进度 66](66-AUTO后台标签冻结与新建标签阻断闭环.md)。

### Phase 3：Profile、Proxy、State 与 HumanTakeover

1. 默认 Validator 与 Application-aware 声明式 Validator/规则 DSL 已完成，并已接入
   迁移 Ready Gate；`maximumAutoRecovery` 现已驱动持久化尝试预算、受限
   Reload/Refresh/导航、受信 Extension 重启、Node State ACK 与二次 Ready Gate。
   契约作者 UI、V050 精确版本双人审批/审计、V051 不可变历史正文和既有 Session
   显式 Rebind、版本 Diff/受控回滚，以及 V052 Provider Evidence 平台协议与迁移 Gate
   已完成；V074 已补齐真实 Document Ready、持续 CDP Network 安静窗口与精确
   Alert/Status/Dialog 瞬态阻断 Gate，Login/可访问目标 Visual/Business Entity 基础
   Validator 不再是代码缺口；通用最小权限 Application Adapter、真实 HTTP Provider 读取、哈希化 Evidence
   和业务 Lease SDK 已由[进度 106](106-Application-Adapter真实Provider与业务Lease-SDK闭环.md)
   关闭；仍缺客户正式 Provider/API 凭据、字段/事务映射和业务系统联调；
2. S3 Archive 校验恢复核心链以及 Profile/Checkpoint 正式导入已完成；
   Profile Checkpoint 导出已由[进度 134](134-Profile-Checkpoint一次性导出闭环.md)
   补齐 Purpose-bound 授权、Actor 隔离、一次性兑换、短期签名下载与敏感内容提示；
   稳定文件 Warm Tier Delta Journal 已由
   [进度 135](135-Profile-Warm-Tier增量日志闭环.md)补齐 Write Epoch/Sequence、事务屏障、
   内容寻址 Chunk、commit-last、Node Journal、PostgreSQL 投影及真实 API/UI；SQLite/LevelDB
   当前安全延后，仍缺应用感知 Adapter、Multipart Resume、跨 Region Restore、Profile
   对象保留期/Legal Hold 深度联动、目标云 KMS 和真实 IAM；
3. 静态 Provider Catalog、能力准入、出口验证和 Safe Point Rebind 已在
   [进度 85](85-多Provider与SafePoint代理重绑闭环.md)完成；运行中主动出口探测和
   质量/新鲜度已由[进度 99](99-Proxy主动健康探测与质量投影闭环.md)完成，冷 Binding
   探测已由[进度 100](100-Proxy冷Binding探测与分布式租约闭环.md)完成；仍缺目标云
   短期 Secret 解引用/轮换/撤销、带认证商业 Provider Adapter、DNS/认证/限流/Provider
   故障和目标 CNI 防逃逸长稳；质量/信誉/成本/地域/容量确定性路由已由
   [进度 102](102-Proxy多Provider质量成本地域路由闭环.md)关闭，仍缺 SLA/业务成功率
   学习、Challenge/黑名单、粘性与受约束探索；
4. State Resync 的持久事件高水位、Session/Tenant 加权请求预算和自动 Full 循环断路器
   已由[进度 118](118-State-Resync背压预算与循环断路器闭环.md)关闭，原生 Region Resync 已由
   [进度 119](119-原生Region-State-Resync原子替换闭环.md)关闭；Full Snapshot 的
   Begin/Chunk/Checksum/Commit、Node Journal 原子有序重投、流式事件高水位和
   PostgreSQL 原子发布已由[进度 120](120-Full-State-Snapshot流式原子提交闭环.md)
   关闭；按实际字节、Browser cgroup CPU、Region 和目标节点容量统一计费的多维预算已由
   [进度 122](122-State-Resync多维预算与实际结算闭环.md)关闭；仍缺目标 Linux 多 Node
   压力长稳、生产 PostgreSQL 在线迁移窗口实测和跨 Region 全局预算一致性证书；
5. 目标云单向网络分区、丢包和多客户端竞争矩阵。

### Phase 4：Agent 生产增强

MVP Gate、Production-like 授权 Replay、独立最小权限 Agent Worker 队列、固定 IPC 和
Kata 故障域，以及 Reviewer/固定 Responses Provider/模型版本、数据、Token、成本治理已
闭环。执行 Worker 详情见
[进度 113](113-独立Agent-Worker队列与最小权限故障域闭环.md)，Reviewer 详情见
[进度 114](114-Reviewer-Agent与真实模型治理闭环.md)。仍缺：

1. Document/Network、Toast/Dialog、可访问目标 Visual、Login 和 Provider Business
   Entity 的基础声明式 Validator 已由[进度 107](107-Business-Recovery浏览器就绪证据闭环.md)
   关闭；仍缺无语义像素/OCR Validator、高级组合编排和客户站点规则；
2. 客户批准站点与正式模型版本的大规模 Replay Matrix、升级 Canary/回滚阈值和长期
   质量/成本证书；目标生产 Provider 凭据、云出口和组织模型风险审批仍是部署 Gate；
3. 普通 VNC/Agent 并存、真实 RFB 输入识别、真人两秒优先和 Agent 无损自动续行已由
   [进度 115](115-VNC与Agent协作控制及真人输入优先闭环.md)完成；有界多协作者、
   x11vnc/noVNC Shared Mode、服务端强制只读观察和显式接管连接撤销已由
   [进度 117](117-VNC有界多协作者与只读观察闭环.md)关闭；Challenge Detection、
   一次授权一次点击 HumanAssist、失败不重试和多步骤转显式接管已由
   [进度 123](123-Challenge-Detection与一次性HumanAssist闭环.md)关闭；真实参与者在线列表、
   管理员精准撤销、连接审计与 Web/Tauri 共用 UI 已由
   [进度 124](124-远程桌面参与者治理与精准撤销闭环.md)关闭；终态历史的稳定游标分页、保留清理与 Web/Tauri UI 已由
   [进度 125](125-远程桌面参与者历史分页与保留治理闭环.md)关闭；单上游 RFB Fan-out、
   合并帧请求、晚加入基线和慢消费者隔离已由
   [进度 126](126-单上游RFB-Fan-out与慢消费者隔离闭环.md)关闭；每 Actor 独立带宽/FPS
   配额、Workspace PostgreSQL 覆盖、管理员 UI 与变更审计已由
   [进度 131](131-VNC每Actor独立带宽与帧率配额闭环.md)关闭；真实成功转发字节、配额等待、
   单调幂等账本和版本化每 Actor 出口成本归因已由
   [进度 132](132-VNC实时计量与每Actor成本归因闭环.md)关闭；仍缺目标 Linux 8 Client
   长稳和跨 Region Workflow；短暂
   真人输入竞争已经由 `executionWait.reason/since` 公开投影表达，Task 按协作语义保持
   `RUNNING`，不再错误建模为接管或断开 Agent；
   V103 视觉自动化、V104 Agent 控制模式和 V105 按需人工续行已由进度 146—148 将旧的
   “一律等人工”升级为：可自动视觉动作，AUTONOMOUS 下可用一次性密文填入账号/密码/
   OTP；只有自动路径确认无法继续时才通知一次并保留原 Task，人工可发送 OTP 由 Agent
   代填或自愿协作填写。支付与破坏性账号决策仍保留独立确认；
   [进度 149](149-Agent-Browser结构化感知与低延迟自动执行基础闭环.md)又完成了
   Snapshot/Inspect/Find、稳定 Element ID、可见/遮挡判定、精确 State Cursor，以及首批
   CLICK/TYPE/FILL/AgentClipboard/SCROLL/WAIT Batch/Fast Path、Human-like Motion、创建时
   Identity Lock 和双剪贴板隔离；完整本地 Gate 与提交 `a14e5f1` 的 GitHub `ci/desktop`
   均已通过。[进度 150](150-Agent-Browser批量动作稳定重绑定与自动模式静默续行.md)
   已进一步让 Batch 每个 Primitive 以稳定 Element ID 和上一步产生的最新 Target Revision
   重绑定，避免普通状态推进被误报成人工介入；完整 Test/Lint/Build、Desktop、契约/SDK、
   N/N−1 与 PostgreSQL/mTLS/Chromium Integration 已通过，实现提交 `54b28ea`、最终提交
   `ec7b61f` 的 GitHub `ci` run `32368996758` 和 `desktop` run `32368997078` 均通过。
   [进度 151](151-Agent-Browser扩展指针与表单控件动作闭环.md)又在同一 Gateway/Batch
   内补齐真实 CDP 双击、右键、悬停、清空、勾选和取消勾选，并以完整 Integration 验证
   checked 最终状态与逐动作重绑定。[进度 152](152-Agent-Browser权威多标签页与跨页执行闭环.md)
   已进一步以真实 Chromium Page Target 和 PostgreSQL Browser State 投影补齐
   OPEN/SWITCH/CLOSE Tab、活动页 Input Broker 重绑定、跨允许域收尾 Capability 与
   Tab Resource Policy 保护。[进度 153](153-Agent-Browser原生Dialog权威状态与粗粒度处理闭环.md)
   又以持续 CDP 事件、PostgreSQL freshness 投影和精确 Dialog ID 补齐
   alert/confirm/prompt/beforeunload 的 ACCEPT/DISMISS/Prompt。
   [进度 154](154-Agent-Browser文件上传下载权威闭环.md)进一步完成精确 Placement mTLS
   staging、PostgreSQL 元数据账本、CDP 隐藏 file input 和真实下载事件/freshness 投影，文件
   字节、Node 路径和 URL 不进入公共状态。
   [进度 155](155-Agent-Browser权威局部截图与一次性访问闭环.md)进一步完成五种
   Viewport/Full Page/Element/Region/Challenge Region 截图、精确 State/Target/Active Tab
   围栏、整页敏感遮罩、create-only Evidence 和原 Actor/Purpose 绑定的一次性访问。
   [进度 156](156-Agent-Browser受治理JavaScript-Evaluate闭环.md)进一步完成 V111 权威
   Evaluation、AES-GCM 密封源码派发、READ_ONLY/PAGE_ACTION、双层浏览器逃逸阻断、精确
   State/Tab 围栏和状态过期自动重试。[进度 157](157-Agent-Browser高级Action-Primitive闭环.md)
   已完成 Select/Press/Drag/Drop/Swipe 和目标约束的通用 Mouse/Keyboard/Touch，并以真实
   Chromium 19 Primitive Batch 验证。当前只剩显式、受控、可审计 Clipboard Bridge；底层
   AgentClipboard/UserClipboard 隔离不等于跨边界契约已经完成；
4. Observer Purpose-bound 截图访问已由进度 87 关闭，Agent Browser 状态围栏截图与原 Actor
   `AGENT_PERCEPTION` 一次性访问已由进度 155 关闭，基础 State 敏感分类与 Evidence 截图
   遮罩已由进度 88 关闭，Site Policy 和 Recording 帧级遮罩已由进度 137、138 关闭；仍缺
   无语义视觉文本/OCR 分类、对象 WORM/Retention/Legal Hold 与 Purpose-bound 播放。

### Phase 5：可靠性、安全与审计

Legal Hold、删除 Receipt、License Inventory 和签名 Audit Export 已完成。仍缺：

1. GPU Helper、AppArmor/SELinux/Landlock 强制策略和真实集群跨 UID 验收；
2. 独立 Secure Debug Worker，以及目标对象存储 Object Lock/WORM、用途绑定一次性播放和
   到期删除 Worker；像素录制、帧级 DOM 语义遮罩、create-only Manifest、PostgreSQL
   权威清单与 Retention/Legal Hold 投影已完成，见进度 138、140；
3. Offline Root/HSM、OCI Admission 强制验签、撤销和生产回滚；
4. 在线信任根双写、CRL/SPIFFE 撤销与过期证书演练；
5. 目标云 Object Storage/KMS/IAM、Multipart Resume、跨 Region Restore；
6. 目标云 Ingress/CNI 单向网络分区及 Provider 故障矩阵。

### Phase 6：密度与 Kubernetes

并发 Browser Density、Extension 自适应采样、PSI 有界驱逐、Media 独立容量、
多 Node 精确路由单测、跨 Node Generation Floor 能力准入以及 N/N-1 Kind 滚动/回滚
已完成。仍缺：

1. PostgreSQL/gRPC/GC 长稳的端到端 Coordinator 容量证书；
2. 双 Coordinator Hot Tenant、物理 Owner 路由与迁移长稳压测；
3. 目标云多节点 CNI 防直连、CSI Snapshot、Kata RuntimeClass 和 Browser Node Pool；
4. Operator resourceVersion List/Watch、410 重列举和 AUTO CRD 已由
   [进度 104](104-Kubernetes-Operator-List-Watch与AUTO-CRD闭环.md)关闭；低基数指标、
   七条告警和 Runbook 已由
   [进度 105](105-Kubernetes-Operator-Prometheus指标与告警闭环.md)关闭；仍缺目标云
   API Server/etcd 故障长稳及 Prometheus/Alertmanager/Pager 到达演练；
5. 目标 Linux 多 Browser Node 的压力驱逐、Media/GPU 和长期密度证书。

### Phase 7：企业运营

已完成 Validation Farm 数据/证据闭环、授权 Replay、Compatibility Gate、版本化费率与
成本解释、Cost-aware Placement、SLO/Error Budget、SLA Exclusion、Retention/Legal
Hold/删除 Receipt、Residency Admission、Compliance、License Inventory、签名 Audit
Export、Recovery GameDay 证据、Region/DR Registry、Media、Terraform Module、四语言
SDK、Error Budget 自动 Runtime Release Freeze、隔离 Validation Worker 队列、自动
Browser/OS/能力矩阵和企业运营 UI。Worker 闭环详见
[进度 110](110-Runtime-Validation-Worker队列与自动矩阵闭环.md)。Recovery GameDay 的
PostgreSQL 队列、隔离 Runner、Lease/Token/Epoch 栅栏、故障后强制恢复和签名发布已由
[进度 111](111-Recovery-GameDay-Runner自动演练与恢复栅栏闭环.md)关闭；事件分页、签名
报告、场景趋势和自动整改工单已由
[进度 112](112-Recovery-GameDay治理报表趋势与整改工单闭环.md)关闭；Enterprise Overview
全域持久单调 SSE、时间窗口到期失效、Web/Tauri 订阅与 15 秒轮询移除已由
[进度 145](145-Enterprise-Overview全量事件源与轮询移除.md)关闭。仍缺：

1. 在目标基础设施部署 Firefox/WebKit、Windows/macOS/多架构的精确版本 Worker Pool，
   接入客户批准 Fixture，并完成大规模业务页面长期回归；
2. 将进度 109 已完成的 Runtime Release Freeze Gate 接入外部生产发布系统，并补齐
   GameDay 连续失败/恢复未确认的自动联动与 Pager；
3. 为固定 GameDay 场景部署目标云最小权限故障 Controller，并完成真实跨 Region
   Authority、数据库/对象复制、流量切换和 RTO/RPO 演练；
4. Terraform Provider 已由
   [进度 116](116-Terraform-Provider正式API协议与签名发布闭环.md)完成正式 API Client、
   Group/Tag CRUD/Import/精确 Session 归属、Workspace Settings 数据源、Protocol 6 Schema、
   CI 和 GPG/OIDC 发布流水线；仍缺组织拥有的独立 Registry 仓库/Namespace、公开 GPG Key
   注册、Secrets、Trusted Publishing 开通和真实客户升级演练；
5. 四语言自动生成、统一版本、确定性发布包和 GitHub OIDC Provenance 流水线已由
   进度 101、103 关闭；仍缺目标语言 Registry 镜像、跨版本兼容/弃用策略和客户升级演练；
6. 目标云 KMS/HSM 签名 Audit Export；本地验收使用可替换的 HMAC Key。

## 下一步优先级

1. 按进度 157 的已确认边界继续实现 AgentClipboard/UserClipboard 显式受控 Bridge，不重做
   结构化感知、Action Primitive、Tab、Dialog、File、Screenshot 或 Evaluate；
2. 浏览器事务 Site Policy 已由进度 137 关闭；使用进度 106 的通用 Adapter/Lease SDK，
   为目标业务配置可信完成语义，并连接客户批准的真实 CRM/支付/IAM Provider 凭据与字段映射；不可变 Revision、版本 Diff、
   受控回滚、显式 Rebind、契约作者 UI、双人审批/审计和平台级证明 Gate 已完成；
3. 关闭目标 Linux/目标云安全、Media/GPU、多 Browser Node 正式 Chromium 完整故障
   矩阵和长期容量 Gate；仓库级数据面迁移由进度 80 关闭，目标启动失败/清理/换节点
   重试由进度 81 关闭；
4. Operator List/Watch 和仓库指标告警已由进度 104/105 关闭；继续完成目标云 API
   Server/etcd 故障长稳、Prometheus/Alertmanager/Pager 到达、跨 Region Event Bus 和
   列表批量事件投影；State/Audit/Operation/Agent Task 统一事件已在进度 84 关闭，
   Session Coordinator 入口物理路由已在进度 83 关闭；
5. Provider Catalog 与运行中 Safe Point Proxy Rebind 已在 V060/进度 85 关闭，运行中
   主动探测与质量/新鲜度已在 V071/进度 99 关闭，冷 Binding 探测已在 V072/进度 100
   关闭，质量/信誉/成本/地域/容量确定性自动路由已在 V073/进度 102 关闭；继续实现
   目标云 Secret 解引用、商业 Provider Adapter 与高级 SLA/业务结果路由，并关闭
   A11y、窄屏和包体积技术质量缺口；Group/Tags 批量生命周期和服务端组合筛选已由
   进度 93 关闭，批量元数据归属已由进度 95 关闭；创建时 Proxy Binding 已在 V057/进度 82 关闭，Profile Import 已在
   V055/进度 79 关闭，
   Workspace Settings 核心
   持久化已在 V036 关闭，Saved View 已在 V053/进度 77 关闭，Environment Import
   已在 V054/进度 78 关闭；
6. 把已完成的自动 Validation Matrix、Runtime Release Freeze Gate 和 GameDay 接入真实
   外部发布系统，并部署目标 Browser/OS Worker Fleet；
7. 完成真实多 Region、Terraform Provider Registry 与四语言目标 Registry Trusted Publishing、
   跨版本 SDK 兼容/弃用策略和组织签字；Python/Go/Java 自动生成与 GitHub OIDC 发布
   门禁已由进度 103 关闭，Provider 仓库内实现与签名/OIDC 发布门禁已由进度 116 关闭。

## 本轮可重复证据

```text
make test-integration
make test-real-url-agent
make test-upgrade-compatibility
KIND_BIN=/tmp/agentbrowser-kind-v0.32.0 make test-kubernetes-e2e
make test-sdk
terraform -chdir=deploy/terraform/modules/agent-browser-cloud validate
make test-terraform-provider
./tests/supply-chain/terraform_provider_release_test.sh
```

本地 N/N-1 演练实测 `createCalls=4`、`terminateCalls=1`，覆盖 N-1 首次调谐、Leader
Pod Kill、升级到 N、回滚到 N-1和删除 Finalizer。详细结果见 Phase 6 与 Phase 7
专项进度文档。
