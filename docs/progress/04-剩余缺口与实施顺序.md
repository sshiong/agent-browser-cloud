# 剩余缺口、Gate 与实施顺序

## 生产阻塞项

| ID | 缺口 | 验收标准 |
|---|---|---|
| SEC-001 | REST API 没有真实 Bearer/OIDC，`X-Tenant-Id` 只是开发防误读 | Tenant 必须从已认证 Principal 派生，并完成 OIDC、RBAC 与权限测试 |
| SEC-002 | CP↔Node gRPC 明文且没有节点身份校验 | mTLS、节点证书轮换和授权策略通过验收 |
| REL-003 | Domain Outbox 没有消息总线 Publisher/Inbox Consumer | At-least-once、消费去重、重放和 DLQ 演练通过 |
| RUN-001 | 真实 Chromium/CDP、状态采集、Crash Event、自动恢复、Node Runtime Lease 对账和资源统计已完成；资源上限与 500 次循环尚未完成 | 资源上限和 500 次循环测试通过 |
| RUN-002 | 已关闭：真实 noVNC、WebSocket 心跳、像素/输入回环、断线 State Resync 和未配对 Shift KeyDown 清理均通过 E2E | 保留回归测试 |
| PROF-001 | 已关闭：Profile Core/Ephemeral、单 Writer、Manifest/Commit Marker、Integrity Restore 和跨 Session 恢复 PoC 已通过 | 保留损坏恢复与跨 Session 回归；业务恢复见 PROF-002 |
| PROF-002 | 仅完成 Technical Ready，尚无 Login/Business Ready 与 Application-aware Adapter | 登录态恢复、业务验证器、SQLite/LevelDB/Cookie Adapter 和破坏性矩阵通过 |
| PROXY-002 | 应用层禁止直连已闭环，但尚无基础设施级出口防逃逸、真实 Provider Secret 与分布式健康治理 | NetworkPolicy/eBPF/防火墙、Vault 短期凭据、真实出口/DNS/故障演练和告警通过 |
| STATE-002 | State Diff、Truncated 和 Full Resync 已闭环，但 Region 仍为显式 Full fallback，且无流式 Snapshot/预算熔断 | 原生 Region、Chunk/Checksum/Commit、Backpressure、Resync Budget 与 Circuit Breaker 通过 |
| AGENT-002 | Agent Intent/Plan 和只读 Executor/Tool Result 已完成，但 Navigate、Target Input、确认和有限 Replan 未实现 | 自建表单流程、写 Tool、有限 Replan、平台确认与执行后验证全部通过 |
| GOV-001 | 审计保留、Legal Hold 与删除证明未闭环 | V16 治理和生产运营 Gate 通过 |

## 本轮关闭

| ID | 关闭证据 |
|---|---|
| REL-001 | SQLite Journal 测试验证重启后命令去重、最高 Coordinator Term 和 Event Sequence 持久存在 |
| REL-002 | Node Agent 测试验证 Journal 关闭/重开后待确认 Event 自动经 gRPC 重投并标记送达 |
| RUN-002 | 真实 RFB Canvas/输入、Ping/Pong、重连宽限、显式释放和断线 Key Up Loss 服务端回收均由 E2E 验证 |
| PROF-001 | 检查点 Epoch 1→2、跨 Session 持久计数 3→4、Commit Marker、损坏哈希拒绝与跨租户 403 均由测试验证 |
| PROXY-001 | Static Provider、Allocation/Binding、出口精确验证、应用层禁止直连、本地熔断、Release、真实 UI 和端到端回归已通过 |
| STATE-001 | 稳定 Target Ref、Base Version Diff、DiffTruncated、Gap Invalid、自动/手动 Full Resync、State Quality 和真实 UI 已通过 |
| AGENT-001 | 受限 Planner、Intent Guard、Plan Validator、Domain/State Gate、Prompt Injection 隔离、Capability Token、真实 API/UI 与 E2E 已通过 |

## Web Console 下一步

已完成：Session Contract 与受控查询投影现返回 `displayName`、`profileId`、
`region` 和 `resourceClass`，列表、详情及集成测试已同步。

已完成：Node `RuntimeStarted/RuntimeStopped` Event、Control Plane Inbox、版本校验、
Session 状态提交与 Start/Terminate Operation Commit 在线闭环。

已完成：`BrowserStateUpdated`、Control Plane JSONB 状态存储/租户 API 与 Session
详情 Browser State 面板；当前运行期间以 2 秒轮询同步。

1. 实现统一 SSE/WebSocket 层，并替换 Session 与 Browser State 详情轮询。
2. 接入 OIDC、Principal Tenant 与 RBAC `<Can>` 组件。
3. 生成 OpenAPI TypeScript Client，并迁移当前手写 Session Client。
4. Profile、Proxy、Agent 计划审查已接入真实 API；继续按 Runtime → Browser Node 的顺序替换 Fixture。
5. 接入 Logs、Security、Audit，并确保后端先脱敏。
6. 增加 1280×800、1440×900、1920×1080 的视觉回归与键盘可访问性测试。

## Coordinator/Node 下一步

1. 为已完成的 Crash Recovery/Node 对账补资源硬限制、容量指标，并完成 500 次循环。
2. Outbox Claim/Publisher 和故障注入。
3. Profile Business Recovery Validator、应用感知 Adapter、加密与 Warm Tier。
4. 为已完成的 Proxy MVP 补基础设施级出口防逃逸、真实 Provider、Secret 轮换、
   分布式健康状态与故障演练。
5. 为 State Gateway 补原生 Region Snapshot、Chunked Stream、预算熔断和
   State Checkpoint Epoch。
6. 为 Agent 补排他 Executor、只读 Tool Service、Token 防重放、Navigate/Target Input、
   Action Validation、有限 Replan 和 Human Confirmation。

## 发布前最低条件

- `make ci`、`make build`、`make test-integration` 和 Web E2E 全部通过。
- SEC-001、SEC-002、REL-003、RUN-001、PROXY-002、STATE-002、AGENT-002、GOV-001 全部关闭；
  REL-001—002、RUN-002、PROXY-001 和 STATE-001 已关闭并保留回归测试。
- 生产构建不启用 Fixture，不包含 Secret，不信任客户端 Tenant Header。
- 完成容量、故障恢复、审计、备份恢复与回滚演练。
