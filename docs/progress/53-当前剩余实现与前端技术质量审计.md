# 当前剩余实现与前端技术质量审计

> 审计日期：2026-08-08
> 审计基线：进度 105 Kubernetes Operator Prometheus 指标与告警闭环
> 来源：`docs/outline/` 三份大纲、OpenAPI、数据库迁移、Control Plane、Browser Node、
> Web Console、部署清单和测试证据

## 结论

当前仓库已经具备可运行的 Browser/Coordinator/Agent 主链、AUTO 资源策略、真实
PostgreSQL 状态、Operation/ACK、资源 SSE、Business Recovery Ready Gate、企业运营
数据面和 Tauri 2 安全容器基线，不是只有页面或接口骨架。

2026-07-29 增量：V042 已完成五分钟真实成本趋势、`maximumCostPerHour` 决策、费率
缺失 fail-closed 和上限前一次性 Node 非核心预算降载；V043 已补齐真实 CDP 后台 Tab
冻结、新建 Tab 阻断和 Node ACK；进度 67 已关闭非特权 Extension 后台暂停，进度 68
已关闭成功命令 Trace 动态采样，进度 69 已关闭真实 Observer 帧率 Gateway 执行。
独立录制数据面和上限停止已由进度 70 完成；V048/进度 71 已关闭公开 L0–L5
契约和企业费率泄露；V049/进度 72 已完成真实 CDP 截图、Storage Helper 提交、失败
证据和成功采样执行器；V062/进度 87 已完成 Observer 手动 Capture、Purpose-bound
Grant、一次性领取和短期签名访问；V063/进度 88 已完成基础敏感语义分类、不透明遮罩、
页面抗篡改复核和 Redaction State/Count。仍缺 Site Policy、无语义视觉文本/Recording
帧级遮罩、保留期/Legal Hold 对象生命周期深度联动与目标环境长期证书。

2026-07-31 增量：V064/进度 89 已将顶部通知占位替换为 PostgreSQL 高信号 Audit
事务投影、Tenant/Actor 单调已读游标、正式 API/RBAC 和 Web/Tauri 共享通知面板；
仓库内通知中心不再是代码缺口。跨 Region Event Bus、工作区级即时推送、外部通知
渠道和目标集群长稳仍属于生产深度。

2026-07-31 增量：V065/进度 90 已完成 Tenant/Actor 级 SYSTEM/DARK/LIGHT 偏好、
正式 API/Audit、Web/Tauri 共享 Theme Provider、浅色 OKLCH Token、资源图表 Token
化和 1440/390 浏览器验收；主题按钮不再是代码缺口。

2026-07-31 增量：进度 91 已把顶部静态身份块升级为读取当前 OIDC/Local 上下文的
Web/Tauri 共用菜单，完成真实 Tenant/Actor/Role 展示、权限化 Workspace/安全入口、
OIDC 退出、本地开发身份边界和 1440/390/键盘验收；完整用户菜单不再是代码缺口。
真实企业 IdP 联调和全站辅助技术矩阵仍属于生产 Gate。

2026-08-01 增量：进度 92 已将 Session 最新 Context、Coordinator Ownership、Active
Operation、Tag Summary、Proxy Binding，以及 Workspace Group/Tag 成员读取改为固定
批次投影；进度 93 进一步完成 Group/Tag 批量 Start/Pause/Migrate/Hibernate 的
PostgreSQL/Routed Command/子 Operation 账本、真实进度和未执行项取消，并完成 Group +
Tag ANY/ALL 服务端组合筛选。Agent 大列表读取后续已由进度 96 关闭，仍缺目标规模
容量证书。

2026-08-01 增量：V067/进度 94 已把 Group、最多 16 个 Tag 和 ANY/ALL 纳入 Saved View
PostgreSQL Schema、租户引用校验、OpenAPI、Audit 和 Web/Tauri 保存/恢复流程；V068/
进度 95 进一步完成 Group/Tag 批量加入和移出的独立持久账本、租约 Worker、真实
Operation 进度、取消、RBAC/租户隔离与共享 UI。两项均不再属于代码缺口。

2026-08-01 增量：进度 96 已把 Agent 队列从最多 100 条完整 JSON 实体读取改为最多
50 条轻量摘要、稳定 Keyset Cursor、索引覆盖的状态聚合和单 Task 详情按需加载；原问题
不是经典逐行 SQL N+1，而是宽行与响应体放大。代码级大列表读取不再是缺口，仍缺
10k/50k Task 目标规模容量证书和列表级跨 Region 事件流。

2026-08-01 增量：进度 97 已关闭 AUTO 的代码级危险事件旁路：Browser Node 从真实
Cgroup `memory.events`、Profile 文件系统容量与持续 CDP 健康失败上报 OOM、磁盘满和
Browser Unresponsive，OOM Crash 进入同一保护时间线；Control Plane 立即暂停 Agent，
OOM 在线时走正式 Adjustment Operation/Node ACK，磁盘满先降载、持续后受控终止，
隔离失效走持久 Stop Operation。仍缺目标 Linux OOM/磁盘/LSM 故障注入和长期证书。

2026-08-01 增量：V070/进度 98 已把顶层总览从最多 100 条 Session 的浏览器端统计迁移
为单条 PostgreSQL 权威聚合，覆盖 Session、Operation、Browser Node、Proxy、Agent、
成本和安全；Payload-minimized Workspace SSE 支持 Last-Event-ID、重放、Reset 和
离线重连。平台 Node 容量与全局事件只对 Platform Admin 可见，并同步修复
`PAUSED_BY_RESOURCE_POLICY` 未进入数据库状态约束的问题。总览统计失真和即时更新
不再属于仓库代码缺口；跨 Region、目标云长稳与事件保留容量证书仍未关闭。

2026-08-01 增量：V071/进度 99 已完成运行中 Proxy Binding 的 Browser Node 隔离主动
出口探测、凭据无关 PostgreSQL 样本、3 次失败/2 次成功 Hysteresis、成功/延迟 EWMA、
透明质量分与新鲜度，并将 Proxy 页面固定轮询替换为合并式 Workspace SSE。主动探测和
运行中质量状态不再是仓库代码缺口；冷 Binding 后续由进度 100 关闭，基础
价格/信誉/地域/容量联合路由后续由进度 102 关闭；仍缺商业 Provider 与目标云 Secret。

2026-08-08 增量：V072/进度 100 已用 PostgreSQL `SKIP LOCKED` 短租约与 Binding
Revision 栅栏调度无活动 Allocation 的冷 Binding；具备
`proxyColdProbe=network-helper-v1` 能力的新 Browser Node 通过 mTLS 和隔离 Network
Helper 执行真实临时绑定/出口验证/释放，N−1 Node 不会收到未知 RPC。冷 Binding 独立
探测不再是仓库代码缺口；基础价格/信誉/地域/容量联合路由后续由进度 102 关闭；
仍缺商业 Provider 与目标云 Secret 解引用。

2026-08-08 增量：进度 101 已将 TypeScript SDK 从少量手写接口升级为由正式 OpenAPI
生成的 158 个唯一 Operation、32 个服务和 230 个 Model；独立 lockfile、契约/文件
SHA-256 Manifest、NodeNext ESM、实例隔离、运行时请求和仅含 `dist` 的 Tarball 门禁
已进入 `make ci`。TypeScript 自动生成不再是代码缺口；目标 Registry OIDC 签名发布和
Python/Go/Java 自动生成后续已由进度 103 关闭；目标语言 Registry 与升级策略仍属于
发布深度。

2026-08-08 增量：V073/进度 102 已用真实健康新鲜度、质量、信誉、成本、地域和容量
完成多 Provider 确定性 AUTO 路由；候选悲观锁、Session Assignment 容量预留、
fail-closed 与完整决策证据已进入 PostgreSQL/API/OpenAPI/TypeScript SDK/Web/Tauri。
基础联合路由不再是代码缺口；仍缺商业 Provider/短期 Secret、DNS/认证/限流故障矩阵，
以及 SLA/业务成功率、Challenge/黑名单、粘性和受约束探索等高级优化。

2026-08-08 增量：进度 103 已将 Python、Go、Java 从少量手写兼容接口升级为正式
OpenAPI 生成的 158 个唯一 Operation 和 215 个公开 Schema；三语言统一 Query/Header/
必填 Body 契约校验与不可覆盖身份边界。四语言统一版本、确定性发布制品、SHA256SUMS、
Contract/Git Commit Manifest 和 GitHub OIDC Provenance Release 流水线已进入 CI。
三语言自动生成和 GitHub 可验证发布不再是代码缺口；仍缺 npm/PyPI/Maven Central 等
目标组织 Registry、跨版本兼容/弃用策略和客户升级演练。

2026-08-08 增量：进度 104 已将 Kubernetes Operator 每 2 秒全量 LIST 改为一致分页
快照、resourceVersion Watch、bookmark、410 重列举、周期 resync 和有界退避；稳定
Ready 状态无反馈 PATCH。公开 CRD 同步删除 L1—L4，并分离 Execution Environment 与
AUTO Resource Policy。14 项单测和真实 Kind Leader Kill、N-1→N→N-1、finalizer
验收已通过；指标告警后续由进度 105 关闭，剩余边界是目标云 API Server/etcd 故障
长稳和目标 Prometheus/Alertmanager/Pager 到达。

2026-08-08 增量：进度 105 已为 Kubernetes Operator 增加 Leader、Lease、LIST、
Watch/410、reconcile P99、循环错误和退避的低基数 Prometheus 指标；固定 bucket
Histogram 不保存无界样本。Metrics Service、七条告警和事故 Runbook 已通过 17 项单测、
Prometheus 3.13.1 promtool 与真实 Kind Lease Holder 抓取。仓库指标告警不再是代码缺口；
仍缺目标 Prometheus/Alertmanager/Pager 到达和 API Server/etcd 故障长稳。

2026-07-30 增量：V058/进度 83 已关闭 Session Coordinator HTTP/API、Timer、
Workflow、Agent Recovery、AUTO 决策和 Migration Reconcile 的物理 Owner 路由，并
通过双 Control Plane 非 Owner 入口集成；该项不再属于仓库代码缺口，剩余的是目标集群
长稳、指标告警、网络分区和跨 Region Gate。

但是项目仍未达到 V16 全量生产发布状态。剩余工作必须区分：

| 类型 | 结论 |
| --- | --- |
| 可直接继续开发 | 客户 Provider 凭据/字段/事务映射、Proxy 商业接入与高级 SLA/业务结果路由、Phase 4 高级 Agent、企业运营自动编排 |
| 目标环境 Gate | Linux Cgroup/多 Node 长稳、GPU/LSM、目标云 CNI/CSI/KMS/IAM、真实 IdP、桌面签名发行、真实多 Region |
| 组织 Gate | Owner/RACI、Threat Review、Residual Risk、Staging、发布审批、值班和 Pager 到达签字 |

因此当前版本可以继续用于本地/集成开发和受控验收，但不能据此宣称已经可以处理真实
客户生产数据。

## 已用代码确认的主要缺口

### P0：生产发布阻断项

1. **目标 Linux/目标云长期证书未关闭**
   - 本机已有真实 Chrome 500 次顺序与并发 4 容量证书；
   - AUTO 代码层已有真实 Cgroup OOM 计数、文件系统容量、持续 CDP Unresponsive、
     Agent Pause、Operation/ACK 扩容和危险终止派发；
   - 仍缺目标 Linux 多 Browser Node 的 Cgroup v2、OOM/PSI、Xvfb/x11vnc、
     Extension、Media、磁盘与网络长期压力证书；
   - 仓库级双 Browser Node + Object Storage + CDP 数据面迁移与恢复已由进度 80
     关闭；仍缺正式 Chromium/目标 Linux 的故障注入、网络分区与长期恢复验证。
2. **生产安全与信任根未关闭**
   - 缺 GPU Helper、AppArmor/SELinux/Landlock 强制策略与跨 UID 集群验收；
   - 缺 Offline Root/HSM、OCI Admission 强制验签、CRL/SPIFFE 撤销和目标云 KMS/IAM；
   - 缺真实企业 IdP Claim/MFA/ACR/Logout/租户映射联调。
3. **发布和运营组织 Gate 未关闭**
   - 缺 Primary/Secondary Owner、RACI、Threat Review、Residual Risk 和发布签字；
   - 缺真实 Staging 制品下载后验签、升级/回滚证据与 Pager 到达演练；
   - Apple/Microsoft 签名、Notarization、Windows 真实机器和更新源尚未验收。

### P1：下一批应实现的产品能力

1. **业务站点 Adapter 与恢复证明**
   - 通用安全 Lease、Application Recovery Contract、持久 Verdict 和迁移 Ready Gate
     已完成；
   - `maximumAutoRecovery` 已在 V034 接入持久尝试预算、受限 Reload/Refresh/导航、
     Node State ACK 与二次 Ready Gate；V039 已补齐受信 `RESTART_EXTENSION`；
   - 正式契约作者 UI、精确版本双人审批/不可变审计、历史 Diff/受控回滚和 Session
     Rebind 已在进度 62、73—75 完成；
   - V052/进度 76 已完成 Provider Evidence 平台协议、独立 Adapter 角色、短时证明账本
     和迁移 fail-closed Gate；进度 106 已完成通用最小权限 Adapter、真实 HTTP Provider
     读取、哈希化 Evidence 和业务 Lease SDK；仍缺客户支付、账号安全、SPA/Form、关键
     事务埋点，以及连接正式 CRM/支付/IAM Provider 凭据后的业务系统联调。
2. **Hot Tenant/Shard 生产迁移**
   - PostgreSQL 权威 Route Epoch、逐 Session Binding、旧 Owner 拒绝、幂等 API 和
     安全点增量迁移已由[进度 63](63-Hot-Tenant权威路由与Route-Epoch安全迁移.md)
     关闭；
   - Node Command Outbox 的 Worker Lease、Rendezvous Hash、物理分片 Claim 和 Node
     Route+Term 双重栅栏已由[进度 64](64-Node-Command物理分片派发与双重栅栏.md)
     关闭；
   - HTTP/Timer/Workflow 等 Session Coordinator 命令的物理 Owner 路由已由
     [进度 83](83-Session-Coordinator物理Shard命令路由闭环.md)关闭；仍缺双
     Coordinator 热点长稳、回滚与压力证书。
3. **统一事件层的生产深度**
   - Resource/Migration、Browser State、Session、Audit、Operation 和 Agent Task 已由
     进度 84 统一到 PostgreSQL 事务游标、断点重放和 Web/Tauri SSE，Session/State
     固定轮询已删除；
   - 仍缺跨 Region Event Bus、列表批量事件流、大规模慢客户端和目标云 Ingress 长稳；
   - 工作区通知中心已由进度 89 关闭，但当前使用 15 秒真实 API 刷新，仍缺工作区级
     SSE/WebSocket 即时推送和外部通知渠道。
4. **Web Console 产品能力**
   - Workspace Groups 的 PostgreSQL 权威模型、OpenAPI、RBAC、幂等审计、Session
     归属、默认 AUTO 策略继承和正式 UI 已在 V033 完成，不再读取生产 Fixture；
   - Workspace Tags 的 V035 权威模型、API、Session 归属/投影和正式 UI 已完成；
     Session/Group/Tag 批量查询投影已由进度 92 关闭；Group/Tags 批量生命周期
     Operation 和服务端组合过滤已由进度 93 关闭；
   - 全局搜索、通知中心、主题和认证用户菜单已由进度 86、89、90、91 关闭；
   - Environment Import 已在[进度 78](78-Environment-Import正式闭环.md)接入
     PostgreSQL 权威预检/执行账本、全事务 Session 创建、正式 API 与 Web/Tauri
     共享工作区；Profile 内容/Checkpoint Import 已在
     [进度 79](79-Profile-Checkpoint-Import正式闭环.md)接入 PostgreSQL Job、mTLS
     Node 流、Storage Helper、对象存储、审计和 Web/Tauri 共享 Drawer；创建时 Proxy
     Binding 已在进度 82 接入 PostgreSQL/API/UI；Provider Catalog 和运行中 Safe
     Point Rebind 已由进度 85 关闭；运行中主动探测、质量/新鲜度 API/UI 已由进度 99
     关闭，冷 Binding 探测已由进度 100 关闭，基础多 Provider 联合路由已由进度 102
     关闭；仍缺商业 Provider/目标云 Secret、高级 SLA/业务结果路由和环境“更多操作”；
   - Saved View 已在[进度 77](77-Environment-Saved-Views正式闭环.md)接入正式
     PostgreSQL/API/RBAC/CAS/审计与 Web/Tauri 复用组件；新增 Group/Tag 组合条件已由
     V067/[进度 94](94-Saved-View-Group-Tag筛选持久化闭环.md)纳入 Schema、租户验证、
     OpenAPI 和保存/恢复 UI；V068/[进度 95](95-Workspace批量元数据Mutation闭环.md)
     已完成 Group/Tag 批量归属和批量移除等元数据 Mutation；
   - Workspace Settings 的工作区名称、默认 Runtime/Region/HumanTakeover 已在 V036
     接入 PostgreSQL、正式 API、RBAC、幂等和审计；Profile Import 已在进度 79
     关闭；创建时 Proxy Binding 已在进度 82 关闭，Provider Catalog 和运行中 Rebind
     已在进度 85 关闭；主动探测与运行中质量投影已由进度 99 关闭，冷 Binding 探测已由
     进度 100 关闭，基础多 Provider 联合路由已由进度 102 关闭；仍缺商业 Provider、
     目标云 Secret、高级 SLA/业务结果路由和环境更多操作；
   - Runtime Build、Agent Policy 和 Extension 已成为正式创建绑定；Agent Policy 已在
     Planner、Executor/Handoff 强制并投影到 Session/Agent Task；Extension 由 V038
     固化并投影到 Session List/Detail。Session Ownership 与 Group/Tags 大列表已由
     进度 92 批量化；Agent 队列轻量摘要/游标和按需详情已由进度 96 关闭；
   - 顶层总览已由进度 98 接入单条 PostgreSQL 权威聚合、权限化平台容量与可续传
     Workspace SSE；最近 Session 只读取 8 条用于导航，不再参与总数计算；
   - TypeScript Client 已由进度 101 完成 OpenAPI 全量自动生成、漂移与发布包门禁；
     Python/Go/Java 自动生成、统一版本、发布包和 GitHub OIDC Provenance 流水线已由
     进度 103 关闭。仍缺目标语言 Registry Trusted Publishing 与升级策略。
5. **Phase 4 Agent 生产增强**
   - V074/[进度 107](107-Business-Recovery浏览器就绪证据闭环.md)已完成真实
     Document/Network、Toast/Dialog、可访问目标 Visual、Login 和 Provider Business
     Entity 基础 Validator；仍缺无语义像素/OCR、客户站点规则和高级组合编排；
   - 缺独立无宿主权限 Agent Worker、Reviewer Agent、真实模型治理和大规模 Replay；
   - 缺 Challenge Detection、一次性 HumanAssist、协作取消、跨 Region Workflow；
   - Purpose-bound 截图访问已由进度 87 关闭，基础 State 敏感分类与 Evidence 截图
     遮罩已由进度 88 关闭；仍缺 Site Policy、无语义视觉分类和 Recording 帧级遮罩。
6. **Phase 7 自动编排**
   - Validation Farm、GameDay 和 Region/DR 已有真实数据库/API/证据模型；
   - 当前 Validation 和 GameDay 仍由 API `start/complete` 登记结果，缺隔离 Worker 队列、
     自动浏览器矩阵、真实故障执行器和大规模回归；
   - 缺 Burn Rate 自动冻结发布、GameDay 失败联动发布系统；
   - 缺真实 Region Authority、数据库/对象复制、流量切换和 RTO/RPO 演练；
   - 当前交付 Terraform Module，不是 Terraform Provider；
   - 四语言 SDK 自动生成、漂移、统一版本、确定性发布包与 GitHub OIDC Provenance
     流水线已由进度 101、103 关闭；仍缺目标语言 Registry、版本兼容/弃用策略和客户
     升级演练。

### P2：生产深度与完整性

1. Warm Tier Delta Journal、Multipart Resume、原生 Region Resync、流式 Snapshot、
   Backpressure 和熔断；
2. Proxy 基础 Reputation/Provider Score 和多供应商确定性路由已由进度 102 关闭；
   仍缺短期 Secret、商业 Adapter、真实 DNS/认证/限流/Provider 故障矩阵，以及
   SLA/业务成功率学习、Challenge/黑名单、粘性和受约束探索；
3. 独立 Secure Debug Worker、像素级强制录像和 WORM Recording Manifest；
4. Extension 当前是 Session 级 `--extension-process` 聚合，缺逐 Extension/Content
   Script 归因和企业扩展长稳；
5. Browser/Profile I/O 是 Browser Cgroup 聚合信号；只有明确计费需求时再补路径级归因；
6. Session Ownership 与 Session/Group/Tag 列表 N+1 已由进度 92 关闭，Agent 队列
   宽 JSON 读取已由进度 96 关闭；仍缺 Agent 目标规模容量证书，以及双 Coordinator 下
   数据库连接池/GC/时钟偏差长稳；
7. Operator resourceVersion List/Watch 与 410 重列举已由
   [进度 104](104-Kubernetes-Operator-List-Watch与AUTO-CRD闭环.md)关闭；指标、七条
   告警和 Runbook 已由进度 105 关闭；仍缺目标云 API Server/etcd 故障长稳和
   Prometheus/Alertmanager/Pager 到达闭环。

## 前端技术质量审计

### Audit Health Score

| # | 维度 | 分数 | 关键发现 |
| --- | --- | --- | --- |
| 1 | Accessibility | 2/4 | 深色 `text-muted` AA 对比已关闭；触控命中区和辅助技术矩阵仍未关闭 |
| 2 | Performance | 3/4 | 路由已懒加载，但 Session Detail 产物为 469,683 bytes / gzip 127.22 kB，仍需进一步拆分 |
| 3 | Responsive Design | 3/4 | Viewer 390×844 已测，Settings 窄屏已改为横向分区导航；Enterprise/详情页和 200% 缩放仍待矩阵验收 |
| 4 | Theming | 4/4 | SYSTEM/DARK/LIGHT、账户持久化和图表 Token 已完成；noVNC 黑场为远端像素观看基准 |
| 5 | Anti-Patterns | 3/4 | 工业观测站方向明确；部分页面仍偏卡片化，但没有大面积玻璃拟态或伪造指标 |
| **总分** |  | **15/20** | **Good：主题闭环完成，但发布前仍需关闭 A11y 与完整响应式矩阵** |

### Anti-Patterns Verdict

**通过，但不是满分。** 当前界面有明确的 Neo-Industrial Observatory 约束、真实状态
边界和紧凑表格，不是通用营销仪表盘。低饱和青色深色主题符合项目既定设计上下文；
需要控制的倾向是 Enterprise/Settings 中的重复卡片容器，以及部分 9—11px
弱对比说明文字。

### 详细发现

#### P1：Accessibility

1. **深色弱文本对比已关闭，仍缺自动回归**
   - 位置：`apps/web-console/src/index.css` 的 `--color-text-muted: #80919e`；
   - 实测：在 Surface 1/2/Canvas 上对比分别约为 5.40:1、5.01:1 和 5.87:1，
     已满足 WCAG 1.4.3 普通文本 4.5:1；
   - 剩余：尚未将深/浅色各 Surface 的 Token 对比加入自动 Gate。
2. **Settings 标签关联已关闭**
   - `SettingGroup` 现在通过 `htmlFor` 关联工作区名称、Runtime、Region 和
     HumanTakeover 控件；
   - 仍需对全站表单和组合控件执行自动 axe/ARIA 与真实屏幕阅读器回归。
3. **触控目标普遍小于 44×44**
   - 位置：顶部栏、列表操作、侧栏等多处 `h-8 w-8`、`h-9`；
   - 影响：窄屏和触控设备误触风险较高；
   - 建议：桌面可保留高密度视觉尺寸，但用不可见 padding/伪元素把命中区扩大到 44px。
4. **缺完整辅助技术验收**
   - 当前有全局 focus-visible 和 reduced-motion，Viewer 窄屏 E2E 已通过；
   - 仍缺全键盘操作、焦点陷阱、屏幕阅读器、浏览器缩放 200% 和自动 axe/ARIA Gate。

#### P2：Performance / Responsive / Theming

1. **Session Detail 路由包较大**
   - 生产构建 `SessionDetailPage` 当前为 469,683 bytes（约 458.68 KiB），
     gzip 后 127.22 kB；
   - 建议按 Remote Desktop、Resource Chart、Migration/Recovery 抽屉继续懒加载。
2. **Settings 基础窄屏布局已关闭**
   - 小屏使用横向可滚动分区导航，`lg` 以上才恢复 200px 二级导航；
   - 仍需增加 1280×800、390×844 管理员路由、200% 缩放和长文本截图回归。
3. **主题 Token 已闭环**
   - Resource Chart 的 Grid/Axis/Tooltip/Series 已使用语义 Token；
   - noVNC 背景保留受控黑场，避免主题改变远端像素观看基准；
   - SYSTEM/DARK/LIGHT、账户持久化、系统主题同步与失败回滚已由进度 90 完成。

### 正向结果

- Web lint、21 个测试文件/66 项 Vitest 和生产构建全部通过；
- 路由级懒加载已生效，核心页面不是单一巨大入口包；
- 全局 `focus-visible` 与 `prefers-reduced-motion` 已实现；
- 真实 API 页面具有 Loading/Error/Empty/Offline/Operation 状态，没有用前端定时器伪造
  资源曲线或扩容；
- 生产业务路由已不再引用 Mock/Fixture 数据；
- 设计 Token、权限路由和 Web/Tauri 平台适配层已经形成可复用基线。

## 推荐实施顺序

1. **P1 `/harden`**：通用 Adapter/Lease SDK 已由进度 106 关闭；继续补客户 Provider
   凭据、字段/事务映射与 Secret Manager 解引用；运行中主动探测与冷 Binding 探测已由
   进度 99、100 关闭，Session Coordinator
   入口物理路由和统一 Session 事件流已由进度 83、84 关闭；
2. **P1 `/normalize`**：创建时 Proxy Binding、Provider Catalog 和运行中 Safe Point
   Rebind 已由进度 82、85 关闭，列表批量查询、Group/Tags 批量生命周期/组合筛选与
   批量元数据归属已由进度 92、93、95 关闭，冷 Binding 探测已由进度 100 关闭；继续
   基础多 Provider 联合路由已由进度 102 关闭；继续实现商业 Provider/短期 Secret、
   高级 SLA/业务结果路由、环境更多操作，并统一其余 Operation/错误模型；
3. **P1 `/adapt`**：补 Settings/Enterprise/Session Detail 的窄屏与 200% 缩放布局；
4. **P1 `/clarify`**：通知、全局搜索、主题和认证用户菜单已关闭占位；继续移除或
   明确其余不可用入口；
5. **P2 `/optimize`**：拆分 Session Detail 重型依赖和图表/远程桌面路由块；
6. **P2 `/polish`**：统一主题 Token、触控命中区、标签关联和细节回归。

完成上述修复后应重新执行 `/audit`，比较技术质量得分和剩余发布 Gate。

## 本轮验证

```text
cd apps/web-console && pnpm lint
cd apps/web-console && pnpm test
cd apps/web-console && pnpm build
```

结果：

- ESLint：通过，0 warning；
- Vitest：21 个测试文件、66 项测试通过；
- TypeScript + Vite production build：通过；
- Workspace Overview 的 V070 空库迁移、Tenant 隔离聚合、权限化 Node 投影和
  Last-Event-ID 重放通过完整 PostgreSQL Integration；1440×1000 与 390×844
  Playwright 验收通过，最终控制台 0 error、0 warning；
- 代码检索确认生产业务路由不再引用 Mock/Fixture 数据；
- Workspace Groups V033、正式 API/UI、默认策略继承和 Integration 证据见
  [进度 55](55-Workspace-Groups正式数据模型与UI.md)；
- Media x11vnc Cgroup Producer、V032 Slot、Operation/ACK、API/UI 和 Integration
  证据见[进度 54](54-Media编码遥测与Slot执行闭环.md)；
- V040、Control Plane 单测和 Integration 确认 Hot Tenant 路由已从 JVM 内存迁移到
  PostgreSQL 权威状态，Route Epoch 已进入 ownership 栅栏；V041 进一步完成 Node
  Command 物理分片派发和 Node Route+Term 原子栅栏；V058/进度 83 已完成 Session
  Coordinator 入口物理 Owner 路由，目标集群长稳仍未完成；
- V034、Control Plane、Browser Node、OpenAPI/Web 和 Integration 证据确认
  `maximumAutoRecovery` 已进入有界低风险动作执行链，详见
  [进度 56](56-Business-Recovery有界自动动作闭环.md)；`RESTART_EXTENSION` 已在
  [进度 61](61-受信Extension自动恢复动作闭环.md)关闭，契约作者 UI 已在
  [进度 62](62-Application-Recovery-Contract作者工作区.md)关闭；Provider 证明协议、
  审批/审计和通用 Adapter/Lease SDK 已由进度 73—76、106 关闭，仍缺客户正式凭据与
  具体站点字段/事务接入。
