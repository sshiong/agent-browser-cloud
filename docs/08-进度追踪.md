# Agent Browser Cloud — 进度追踪总览

> 版本：v6.91
> 日期：2026-08-12
> 口径：只把已通过构建、测试或真实运行验收的能力计为“已完成”；仅有类型、页面或接口占位时计为“骨架”。

## 当前结论

| 工作流                                          | 状态                                                    | 结论                                                                                                                                                                                                                                        |
| ----------------------------------------------- | ------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Phase 0：工程与契约                             | 技术项基本完成，组织 Gate 待签字                        | Java/Rust/Web 可构建，Contract、CI、迁移和集成烟雾测试已通过                                                                                                                                                                                |
| Phase 1：Browser Runtime                        | 主链路、资源硬限制和本机容量 Gate 已闭环                | Chromium/CDP、State/Input、Crash Recovery、真实 noVNC、Cgroup v2，以及真实 Chrome 500 次顺序和并发 4 的无泄漏证书已闭环；目标 Linux/多 Node/Media-GPU 长稳仍待完成                                                                           |
| Phase 2：Session Coordinator                    | 单 Session 故障接管、Hot Tenant 迁移和物理 Shard 命令路由已闭环 | 幂等、CAS、Operation、Outbox/Inbox、Node Journal、Workflow、Lease/CAS、Term/Route Epoch Fencing、PostgreSQL 权威路由、安全点迁移、租约 Worker/Rendezvous Hash、Node 双重栅栏和 API/Timer/Workflow Owner 路由已落地；物理 Shard 重平衡期间会优先投递仍存活的逻辑 Owner，三层执行栅栏已统一；双实例长稳、指标告警与目标网络分区仍待完成 |
| Web Console / Desktop                           | Web 主链、Workspace 权威总览/实时流、Workspace 管理/批量生命周期/批量归属、组合筛选与 Saved View 持久化、导入、全局搜索、通知中心可续传 SSE、用户主题、认证用户菜单、创建时 Proxy Binding、Safe Point Proxy Rebind、Proxy 主动探测及多 Provider 自动路由，以及 Tauri 2 安全容器基线已闭环 | Web 环境页、AUTO Policy、资源执行/SSE、Workspace Overview PostgreSQL 聚合与可续传 SSE、OIDC/RBAC、Groups/Tags 批量操作与筛选、Settings、Global Search、通知 Audit Sequence SSE、主题、用户菜单、导入、Provider Catalog、Binding、受控 Rebind、运行中/冷 Binding 探测，以及质量/信誉/成本/地域/容量联合路由 PostgreSQL/API/UI 已完成；目标云 Secret 解引用、商业 Provider Adapter 和高级 SLA/业务成功率学习仍待开发；Tauri 2 具备 OS Vault、系统浏览器 OIDC、Deep Link、Updater Gate 和 macOS 本机构建，真实 IdP 联调、签名证书、真实更新源、Windows/桌面矩阵仍待完成 |
| Phase 3：Profile、Proxy、State 与 HumanTakeover | 通用恢复、Full/Region State Resync、浏览器就绪证据、业务 Lease、Application Adapter、Provider Evidence Gate、契约治理与有界自动动作已闭环，目标业务接入和生产环境 Gate 进行中 | Profile/State/HumanTakeover/Safe Point、版本化 Recovery Contract、审批/不可变历史/Diff/受控回滚/Rebind、真实 Document/Network 安静窗口与 Alert/Status/Dialog 阻断 Gate、原生 CSS/Target Ref Region 采集与原子替换、Full Snapshot Begin/Chunk/Checksum/Commit 流与 PostgreSQL 原子发布、最小权限 Application Adapter/Lease SDK、真实 Provider HTTP 读取与哈希化 Evidence、迁移 Ready Gate，以及 Reload/受限导航/Extension 重启与 State ACK 已落地；仓库级三 Node/Object Storage/CDP 数据面迁移、目标清理屏障与有界重试证书已完成；客户 CRM/支付/IAM 凭据、无语义视觉/OCR、站点字段/事务接入和目标 Linux 多节点长稳尚未完成 |
| Phase 4：Agent 基础与安全边界                   | MVP、Worker/Reviewer、Challenge 与 HumanAssist 仓库闭环已完成 | 单 Planner/Executor、Intent/Plan Gate、Prompt Injection、Capability、首批九个 Tool、持久 Step、有限 Replan、平台确认、Agent→Human Handoff、结果验证、独立最小权限执行 Worker、独立 Reviewer Worker、固定 Responses Provider、模型治理，以及输入权隔离的 Challenge Detection/一次授权一次点击 HumanAssist 已完成；高级 Validator、大规模客户 Replay、参与者治理、跨 Region Workflow 与目标云模型准入仍待完成 |
| Phase 5：可靠性、安全与审计                     | 核心安全与本地演练闭环，生产环境 Gate 进行中            | OIDC/RBAC、mTLS、Workflow、哈希审计、治理、Helper 隔离、签名发布、PostgreSQL/S3 故障、换主/网络分区、Legal Hold/删除 Receipt/签名 Audit Export 已验收；GPU/LSM、目标云 KMS/IAM、Offline Root/HSM 待完成                                          |
| Phase 6：密度与 Kubernetes                      | Stage B、本地 N/N-1、Operator List/Watch/指标告警、Hot Tenant 与 Coordinator 物理 Shard 路由已闭环，目标云 Gate 进行中 | 50k Coordinator Stage A、真实 Chrome 500 次顺序/并发 4、Placement、Media、Extension 自适应、PSI 驱逐、Cgroup v2、双副本 Operator、resourceVersion List/Watch、AUTO CRD、Prometheus 指标/七条告警、N-1→N→N-1 Kind、Route Epoch 迁移、Node Command 动态派发和 API/Timer/Workflow Owner 路由已完成；目标集群 API Server/etcd 与 Coordinator 长稳、Prometheus/Alertmanager/Pager 到达、CNI/CSI 和多 Node 长稳待完成 |
| Phase 7：企业运营                               | 仓库内核心功能已闭环，生产集成 Gate 进行中              | Validation 自动矩阵、Recovery GameDay 隔离 Runner/强制恢复栅栏/事件分页/签名报告/趋势/整改工单、Cost/SLO/Retention/Compliance/Residency/DR Registry、Error Budget 自动 Runtime Release Freeze、License/签名 Audit、Terraform Module、Terraform Provider、四语言 OpenAPI SDK 和真实 UI 已完成；统一版本、八组件镜像/十个渲染镜像引用的确定性发布包与 GitHub OIDC Provenance 流水线已闭环，目标多浏览器/多 OS Worker Fleet、客户 Fixture、目标云故障 Controller、真实多 Region、HSM、外部生产发布系统接入、Provider/目标语言 Registry Trusted Publishing 和组织审批待完成 |

当前代码已经通过 Phase 4—7 的仓库内核心验收，但没有通过 V16 全量生产发布 Gate，
仍不能据此处理真实客户数据。生产剩余项见
[当前未实现清单](progress/33-当前未实现清单.md)。

## 本轮已完成

- 新增 [Challenge Detection 与一次性 HumanAssist 闭环](progress/123-Challenge-Detection与一次性HumanAssist闭环.md)：
  V087/V088 建立 PostgreSQL 权威 Challenge Event、单次 Human Click Intent、Agent 等待绑定、
  Operation/Audit 和统一 Session SSE；检测器没有 Node/Input 依赖，只保存信号码、状态 Hash
  与目标 Hash。普通单击挑战必须先预览并由当前用户确认，随后创建独占
  `HUMAN_ASSIST` Operation；Browser Node 在当前 State、Target Revision、可见性、角色、
  精确 Bounds、视觉锚点、真人输入优先和 HumanTakeover 栅栏全部通过后只点击一次，失败
  不自动重试。OTP、图片、拼图、设备、多轮、支付及敏感流程强制转显式 HumanTakeover；
  点击后若仍检测到挑战，Agent 继续暂停并绑定新事件，不会误恢复。Web/Tauri 共用 Session
  详情卡、正式 API/OpenAPI/四语言 SDK 已完成；全量 Test/Lint/Build 和真实 PostgreSQL
  88 个迁移、Control Plane/Browser Node Integration 已通过。无语义 OCR/像素检测、客户
  站点分类 Replay、参与者治理及跨 Region Workflow 仍未完成。

- 修复真实托管 E2E 暴露的 Full Snapshot 过期清理 JDBC 类型问题：清理 SQL 现在显式使用
  `java.sql.Timestamp`，避免 PostgreSQL 无法推断 `Instant` 参数类型而令定时任务失败。
  修复后 `make test-e2e` 已覆盖 Web、PostgreSQL、Browser Node、noVNC、Agent 计划与执行；
  `make test-integration` 再次通过 88 个迁移、105 张公开表和完整审计链。

- 新增 [State Resync 多维预算与实际结算闭环](progress/122-State-Resync多维预算与实际结算闭环.md)：
  V084—V086 以 expand、在线并发索引、独立约束验证三阶段扩展权威 Ledger，不回填仍在
  五分钟窗口内自然过期的旧请求。Full/Region 在命令下发前分别预留有界字节和 Browser
  cgroup CPU，并在 Tenant → Region → Node → Session 固定锁序下同时检查四维滚动预算、
  Placement、Node 心跳/压力和 Region 准入；锁后 Placement 变化会失败关闭。Browser Node
  从真实 cgroup 累计值计算采集 CPU 增量，Full Snapshot Begin 与 Region Diff 回传实际
  字节和 Request ID，Control Plane 幂等结算并写审计；N−1 未提供 CPU 时明确保留保守
  预留，Region Request ID 只允许从认证的 `evt_cmd_*` 恢复。多 Node/跨 Region 全局预算
  长稳和生产 PostgreSQL 在线迁移窗口仍是目标环境 Gate。

- 新增 [VNC 协作缺省与 Agent 不断线回归](progress/121-VNC协作缺省与Agent不断线回归.md)：
  Browser Node 将滚动升级期间缺少 `accessMode` 的旧票据从隐式
  `EXCLUSIVE_TAKEOVER` 改为安全的 `COLLABORATIVE` 缺省；只有明确签名的专用交接票据
  才能进入排他仲裁。普通 noVNC 连接不创建 HumanTakeover、不改变 Agent Operation，
  仅真人实际输入触发两秒优先窗口，Agent Task 保持 `RUNNING` 并在空闲后自动续行。
  Gateway 13 项测试、Control Plane 定向测试和真实托管 E2E 已通过。

- 新增 [Full State Snapshot 流式原子提交闭环](progress/120-Full-State-Snapshot流式原子提交闭环.md)：
  显式 Full Resync 由单一 `BrowserStateUpdated` 改为有界
  `Begin → 16 KiB Chunk → Commit`，最多 32 块/512 KiB；每块和整流双重 SHA-256。
  Node 在单一 SQLite 事务中有序记录整批 Event，发布失败即停止直发并由持久 Journal
  续投，所有 Frame 纳入现有 State Event 高水位。V083 建立 PostgreSQL Manifest/临时
  Chunk 表，Assembler 在 Tenant、Term、Context/Operation Epoch、Manifest、顺序、字节
  与 Hash 全部通过后，才在同一事务中发布权威 Browser State 并清除临时块；支持重复
  Event、Commit 早到、新流取消旧流和十分钟过期清理。Rust/Java 单测、N/N−1 迁移门禁
  和完整 PostgreSQL/Browser Node Integration 均已通过。多维字节/CPU/Region/节点容量
  统一计费已由进度 122 关闭。
- 新增 [原生 Region State Resync 原子替换闭环](progress/119-原生Region-State-Resync原子替换闭环.md)：
  Browser Node 使用安全序列化的 CSS Selector 或已验证 Target Ref 只执行指定
  DOM 子树的 `Runtime.evaluate`，在同一 Target Revision 内删除旧区域目标、合并新区域
  目标并保留区域外 Target Ref。区域结果通过带 `baseStateVersion`、
  `REGION_RESYNC` 和 `requestedRootRef` 的有界 State Diff 入库；Control Plane 在
  PostgreSQL 行锁、Context Epoch、Base Version 和 Target Revision 四重栅栏下原子提交，
  失配时保持 `INVALID` 并自动 Full Resync。Region 来源、Root 和前后版本进入防篡改
  Audit；Rust 真实 CDP 协议模拟、Java Mapper/Repository、完整单测和 PostgreSQL
  Integration 覆盖区域外引用稳定、区域内删除/新增、后续周期采集不抢跳
  Target Revision。周期无变化探测不再虚增 State Version，但 Agent 动作后的显式
  Confirmation 仍提交新版本作为动作完成屏障。Chunked/Checksum/Commit 流式 Full
  Snapshot 已由进度 120 关闭；字节/CPU/Region/节点容量多维预算已由进度 122 关闭。
- 新增 [State Resync 背压、预算与循环断路器闭环](progress/118-State-Resync背压预算与循环断路器闭环.md)：
  Browser Node 新增默认 64 的持久 State Event 高水位，达到限制时只生成一次
  `BACKPRESSURE_LIMIT` 截断并在发布前安装 Resync Barrier，Full Resync 前不再放大 Diff
  Journal。V082 建立 PostgreSQL 权威 Admission Ledger，Full/Region 分别计 10/2 Token，
  默认五分钟 Session/Tenant 上限 60/600，并通过 Tenant → Session 事务 Advisory Lock
  保证多实例不会超发；自动 Full 默认 60 秒三次后断路，保留 `INVALID` 且提交触发事件，
  人工超额返回 `429`、`Retry-After`、稳定错误码和预算 Scope。幂等重放不重复计费，拒绝
  不残留 Claim，Region Root 只持久化 SHA-256；Rust/Java、契约、N/N−1 和完整 PostgreSQL
  Integration 通过。原生 Region Resync 已由进度 119 关闭，Chunked/流式 Snapshot
  已由进度 120 关闭；字节/CPU/Region/节点容量多维预算已由进度 122 关闭。
- 新增 [VNC 有界多协作者与只读观察闭环](progress/117-VNC有界多协作者与只读观察闭环.md)：
  Gateway 从 Session 单连接改为按单次票据 Nonce 管理的有界 Lease，最多 8 个协作者；
  x11vnc/noVNC 统一 Shared Mode，任一普通观察连接不再踢掉已有连接。最后一个连接断开才执行
  Grace/Generation 栅栏后的清键与 State Resync。显式 HumanTakeover 会通过 Revocation
  Channel 撤销全部 Collaborative Lease 后独占，并拒绝接管期间的新协作者。新增签名
  `viewOnly` Claim、正式查询参数和 Web/Tauri“协作控制/只读观察”切换；Browser Node 会在
  x11vnc 前强制拒绝只读 Key/Pointer/Clipboard，安全性不依赖前端。Rust 11 项、Web 68 项、
  Java/Rust/Python 全量测试、Web 68 项、Lint、生产构建、契约、四语言 SDK 与 N/N−1 已通过；
  真实托管 E2E 同时维持 Shared Control 与 View-only Client，观察页关闭后控制页保持
  `RFB LIVE`，并继续通过真人输入/Agent 自动续行及断线清键。目标 Linux 8 Client 长稳、
  完整集成的跨 Node Migration、Coordinator Failover、HumanTakeover、Agent Reviewer、
  资源 Actuator 与审计链通过；单编码器 Fan-out、参与者治理和跨 Region Relay 仍待完成。
- 新增 [Terraform Provider 正式 API、协议与签名发布闭环](progress/116-Terraform-Provider正式API协议与签名发布闭环.md)：
  新建独立 Go 1.25/Plugin Framework Protocol 6 Provider，正式模式强制 HTTPS 与 Bearer，
  本地 Header 身份只允许显式 loopback；HTTP Client 拒绝重定向、限制 8 MiB 响应并只返回
  脱敏状态码/Error Code/Request ID。`browsercloud_group` 与 `browsercloud_tag` 使用正式
  Control Plane API 完整 CRUD、Import 和精确 Session 归属收敛，
  `browsercloud_workspace_settings` 只读 PostgreSQL 权威配置且不伪造 Destroy 语义。
  Provider 已进入根 Makefile 构建、race test、vet、gofmt 和主 CI；独立 tag Workflow
  使用多平台确定性 ZIP、SHA-256、GPG detached signature、固定 SHA Action 与 GitHub
  OIDC Provenance。Registry namespace、公开 GPG Key 注册、Secrets 与 Trusted Publishing
  仍是组织发布 Gate，不计为完成。远端 Trivy 首轮阻断了旧 Plugin Framework 传递依赖，
  已升级到 Framework 1.19/plugin-go 0.31、Go 1.25，以及修复版 x/net 0.55、x/text 0.39、
  gRPC 1.82.1，保留 HIGH/CRITICAL fail-closed 门禁。
- 新增 [VNC 与 Agent 协作控制及真人输入优先闭环](progress/115-VNC与Agent协作控制及真人输入优先闭环.md)：
  普通“打开远程桌面”不再创建或抢占独占 `HUMAN_TAKEOVER` Operation，Control Plane
  改签发绑定 Session Context 的短期协作票据。Browser Node 从真实 RFB Client 消息识别
  Key/Pointer/CutText；仅观察 Agent 行为不阻塞 Agent，真人实际输入后的两秒窗口内，
  Agent Navigate/Click/Type/Scroll 返回可重试 `HUMAN_INPUT_PRIORITY`。Dispatcher 不增加
  Attempt、不进入 Dead Letter，输入空闲后同一命令自动续行；断线仍执行 CDP/X11
  All-keys-up 与 State 重采集，但不结束 Agent Operation。Web/Tauri 共用页面已改为协作
  语义，定向及全量 Rust/Java 测试、Web 67 项测试、Lint、契约、生产构建和真实托管
  noVNC E2E 通过。显式 Agent→Human Handoff/HumanTakeover 仍保留为真正暂停 Agent 的
  独立治理流程。V081 进一步增加独立 `executionWait.reason/since` 权威投影，Task 仍保持
  `RUNNING`，首次真人竞争写入、Node 接受原命令后清除，并由既有 Session `AGENT_TASK`
  可续传 SSE 触发实时重取；Automation UI、OpenAPI 与 TypeScript/Python/Go/Java SDK 已
  同步。真实双页面 E2E 已验证 VNC 连续输入与 Agent 写命令并发、同一 Operation 自动续行、
  VNC 全程 `RFB LIVE`；当前 190 个 Operation、254 个公开 Schema。目标 Linux 输入竞争
  长稳仍需执行。
- 修复 GitHub Kind Operator E2E 的双副本日志竞态：升级完成后的 cache 同步断言和失败诊断
  改为扫描 Deployment Selector 对应的全部 Operator Pod，不再由
  `kubectl logs deployment/...` 随机选择非 Leader 而误报失败；本地语法/仓库校验与远端
  重跑证据纳入本轮提交。
- 新增 [Reviewer Agent 与真实模型治理闭环](progress/114-Reviewer-Agent与真实模型治理闭环.md)：
  V080 新增 PostgreSQL 权威 Reviewer 队列、不可变事件和 Agent Task 审核投影；执行前强制
  `AWAITING_REVIEW`，以 Plan Hash、256-bit Claim Token Hash、Epoch、Lease、Attempt、
  Deployment/Model Revision 和最低 Confidence 失败关闭。独立 Python Reviewer Worker
  只接收 capability-free/密封载荷无关的有界计划摘要，通过固定、无重定向、禁代理的
  OpenAI Responses `/v1/responses` 协议调用显式 HTTPS Host Allowlist；Provider Secret
  不进入 Control Plane 或 PostgreSQL。Token/延迟/Provider Request ID/输入输出 Hash 和
  按服务端固定费率计算的成本进入审计；低置信度转为 `MODEL_UNCERTAIN`。Kubernetes 使用
  独立 Reviewer 身份、Kata Sandbox 和受控模型出口，并以默认关闭/全 CP 升级后开启的
  Feature Gate 保持 N/N−1 安全。Web/Tauri 共用 UI 展示真实审核状态与成本。正式 API/
  四语言 SDK 当前为 190 个 Operation、253 个 Schema，TypeScript 为 310 个服务方法、
  32 个服务和 272 个 Model；完整 PostgreSQL/双 Control Plane/Browser Node 集成已验证
  `agent_reviewer=true` 和 `reviewer_model_provider=true`。目标生产 Provider 凭据与
  云出口、客户大规模 Replay、高级 Validator 和跨 Region 长稳仍缺；当时尚缺的
  Challenge/HumanAssist 已由进度 123 关闭。
- 修复双 Control Plane 的物理 Shard/逻辑 Owner 切换窗口：API 入口不再因本实例刚获得
  物理 Shard 就绕过仍存活的 Session Owner；PostgreSQL Coordinator Command Inbox 优先
  选择活跃逻辑 Owner，Claim Processor 与 `SessionCoordinator` 使用同一规则。独立
  Reviewer Session 的真实 Worker 驱动、终止、主 Session Crash Recovery 与 Node Restart
  已在同一完整集成中通过，最终证据为 `durable_workflows=17`、`audit_chain_valid=true`。
- 新增 [独立 Agent Worker 队列与最小权限故障域闭环](progress/113-独立Agent-Worker队列与最小权限故障域闭环.md)：
  V079 将生产 Agent Execute 改为 PostgreSQL 权威队列，以 SKIP LOCKED、256-bit 一次性
  Claim Token Hash、Epoch、Lease 和有界 Attempt 栅栏 Worker；独立 Python Worker 只接收
  不透明 Job/Task ID 并调用固定 Claim/Start/Heartbeat/Drive/Fail IPC，不接收 Prompt、
  Plan、网页数据、Capability Token、客户凭据或任意命令。真正 Tool 执行仍由 Control
  Plane 的 Capability/Operation/Coordinator Route/Outbox 安全内核完成。生产清单使用
  Kata `agent-sandbox`、无 ServiceAccount Token/Host Mount/数据库凭据、CP-only Egress；
  Web/Tauri 共用 UI 增加 QUEUED/资源暂停状态。Agent Worker 已进入第八个签名组件镜像、
  SBOM/Provenance/Release Bundle。正式 API/四语言 SDK 当前为 185 个 Operation、243 个
  Schema，TypeScript 为 300 个服务方法、32 个服务和 261 个 Model；Reviewer 与模型治理
  后续已由进度 114 关闭。仍缺高级 Validator、客户大规模 Replay、目标云 Kata/CNI/LSM
  强制证书和跨 Region 长稳。
- 新增 [Recovery GameDay 治理报表、趋势与整改工单闭环](progress/112-Recovery-GameDay治理报表趋势与整改工单闭环.md)：
  V078 增加不可变签名报告和一场演练一张整改工单；事件使用绑定 GameDay 的稳定 Keyset
  Cursor 分页，报告包含权威 Run/Job/完整时间线/工单并保存 SHA-256、HMAC-SHA256 与
  Key ID。失败、中止或目标未达成会幂等建单，ACK/RESOLVED 强制 Owner/Resolution 并
  进入签名 Audit；90 天场景趋势返回通过率、P95 RTO/RPO、Recovery Unknown 和未关闭
  工单；Platform Admin 可在共用 UI 确认归属并填写关闭证据。正式 API、Web/Tauri
  共用 UI 和四语言 SDK 当时同步为 180 个 Operation、238 个 Schema；进度 113 已扩展为
  185 个 Operation、243 个 Schema、TypeScript 300 个服务方法/32 个服务/261 个 Model。完整 PostgreSQL 集成已
  验证分页无重复、篡改 Cursor 拒绝、HMAC 验签、自动建单/关闭和趋势聚合。仍缺目标云
  Controller、真实跨 Region、外部发布/Pager/工单平台联动及生产 KMS/HSM 报告签名。
- 新增 [Recovery GameDay Runner 自动演练与恢复栅栏闭环](progress/111-Recovery-GameDay-Runner自动演练与恢复栅栏闭环.md)：
  V077 将管理员手工 GameDay 升级为 PostgreSQL 权威队列、不可变事件和 Worker 投影；固定
  Catalog/HTTPS Controller、Blast Radius、环境/生产审批、256-bit Claim Token Hash、
  Epoch/Lease、单调阶段和有界尝试共同约束执行。Runner 在异常、超时和 SIGTERM 下仍通过
  `finally` 恢复；故障注入后 Lease 丢失会进入优先 `RECOVERY_REQUIRED`，旧 Token 失效，
  只有 Recovery-only Claim 确认健康后才能安全结束。独立 `GAMEDAY_WORKER`、Sandbox
  Kubernetes、默认拒绝网络、空生产 Catalog、真实 UI 和第七个签名组件镜像已接入；
  发布渲染现有八个 Digest-locked Workload 引用。正式
  当时契约为 174 个 Operation、232 个 Schema，TypeScript 为 278 个服务方法、32 个服务；
  事件分页、签名报告、趋势和整改工单后续已由进度 112 关闭。目标云故障 Controller、
  正式 Blast Radius 证书、真实跨 Region 切换和外部发布冻结/Pager 联动仍是生产 Gate。
- 新增 [Runtime Validation Worker 队列与自动矩阵闭环](progress/110-Runtime-Validation-Worker队列与自动矩阵闭环.md)：
  V076 新增 PostgreSQL 权威 Job/不可变事件/Worker 投影，以 `SKIP LOCKED`、256-bit Claim
  Token Hash、Epoch 和 Lease 栅栏实现精确 Browser/OS/能力 Claim、有界重试和迟到结果
  拒绝；独立 `VALIDATION_WORKER` 只执行部署固定 Runner，Runner 环境不继承 Token/Secret，
  输出边读取边限流，超时或租约丢失立即终止进程组。真实 Chromium Runner、Sandbox
  Kubernetes、自动矩阵 API、OpenAPI/四语言 SDK、真实 UI 和 Validation Worker 签名
  镜像已接入；后续 GameDay 隔离 Worker 已由进度 111 作为第七个签名组件镜像关闭。
  真实 URL/真实 Chrome 验收已证明精确版本 Claim、固定 Runner、授权 Dataset、Evidence
  COMMIT 及跨域/非 Allowlist fail-closed；测试会等待首份权威 State 和短暂 Resync 恢复，
  不把合法 204 或安全状态窗口误判为功能失败。
  当前契约经进度 113 扩展为 185 个 Operation、243 个公开 Schema，TypeScript 为 300 个服务方法、32 个
  服务和 261 个 Model 文件。目标多浏览器/多 OS Fleet、客户批准 Fixture、大规模长期
  回归和外部发布联动仍是生产 Gate。
- 新增 [Error Budget Burn Rate 自动发布冻结闭环](progress/109-Error-Budget-Burn-Rate自动发布冻结闭环.md)：
  V075 将冻结/恢复阈值、评估窗口和稳定恢复窗口写入 PostgreSQL；30 秒 Tenant 评估以
  Policy 行锁兼容多 Control Plane，并用上下阈值和稳定窗口避免抖动。Runtime Promotion
  在申请和审批两个时点同步检查 Gate，冻结时 fail-closed 并写 Audit；Emergency
  Disable 保留双人审批且可旁路冻结。企业运营 UI 展示真实阶段、Burn Rate、原因和版本。
  OpenAPI/四语言 SDK 经进度 113 扩展为 185 个 Operation、243 个公开 Schema，TypeScript 为 300
  个服务方法、32 个服务和 261 个 Model；后端/Web/Contract/N/N-1、完整 PostgreSQL
  Integration 与 Object Storage GameDay 已通过。GameDay Runner 已由进度 111 关闭；
  外部生产发布系统和连续失败自动冻结联动仍属于后续 Gate。
- 新增 [工作区通知可续传 SSE 闭环](progress/108-工作区通知可续传SSE闭环.md)：
  通知中心删除 15 秒固定轮询，改用 Tenant-scoped、Payload-free 的 Audit Sequence
  SSE；支持 `Last-Event-ID`、Replay、Future Cursor Reset、Heartbeat、全局/Tenant
  连接上限和 1—30 秒重连退避。Web/Tauri 共用 Query 层只以事件失效正式通知 API，
  明确展示实时、连接中、重连和离线状态，不在事件流传输正文或 Actor 已读数据。
  OpenAPI/四语言 SDK 经进度 113 扩展为 185 个 Operation、243 个公开 Schema，TypeScript 为
  300 个服务方法；`make ci`、完整 PostgreSQL/双 Control Plane/Browser Node 集成和
  对象存储 GameDay 已通过。集成中同步修复 Network Quiet 原始时钟导致严格 Provider
  Evidence 状态版本持续失效的竞态：1 秒语义桶在 30 秒策略上限封顶，安全绑定未放宽。
- 新增 [Business Recovery 浏览器就绪证据闭环](progress/107-Business-Recovery浏览器就绪证据闭环.md)：
  Browser Node 从真实 `document.readyState` 和持续 CDP Network/Download 事件流生成
  失败关闭的文档/网络证据，并将 Alert、Status、Dialog、AlertDialog 纳入有界可访问目标。
  V074 把 Document Complete、0—30 秒 Network Quiet 和精确瞬态阻断目标固化到当前契约
  与不可变 Revision；迁移 Ready Gate、公开 State API、OpenAPI/四语言 SDK、Web/Tauri
  共用作者 UI、Diff/回滚和 N/N−1 Tag 兼容已同步。无语义视觉/OCR、客户站点规则和
  目标 Region 长稳仍未完成。
- 新增 [Application Adapter 真实 Provider 与业务 Lease SDK 闭环](progress/106-Application-Adapter真实Provider与业务Lease-SDK闭环.md)：
  独立最小权限组件从 `0600` Token 文件调用 Allowlist Provider HTTPS API，以 JSON
  Pointer 提取业务值并只提交本地 SHA-256；提供 owner-bound Lease SDK/CLI。修复
  `APPLICATION_ADAPTER` 无法写 Safety Lease、测试误用 Tenant Operator 的权限断点，
  并以通用 Resource Policy Mutation 的真实 `403` 证明该角色没有获得 Session 运维权限。
  11 项单测、OpenAPI、签名 Release Bundle v3 和完整 PostgreSQL/Browser/Chromium
  Integration 通过；客户正式凭据、具体字段/事务映射和目标 Region 长稳仍待接入。
- 新增 [Kubernetes Operator Prometheus 指标与告警闭环](progress/105-Kubernetes-Operator-Prometheus指标与告警闭环.md)：
  每个 Pod 暴露 Leader、Lease、LIST 快照、Watch/410、reconcile P99、循环错误与退避的
  低基数指标；固定 bucket 直方图不保留无界样本。Metrics Service、七条告警和
  List/Watch 事故 Runbook 已交付。17 项单测、Prometheus 3.13.1 promtool 和真实 Kind
  当前 Lease Holder 指标抓取通过；目标 Prometheus/Alertmanager/Pager 到达仍是生产 Gate。
- 新增 [Kubernetes Operator List/Watch 与 AUTO CRD 闭环](progress/104-Kubernetes-Operator-List-Watch与AUTO-CRD闭环.md)：
  将 Leader 每 2 秒全量 LIST 改为 500 条一致快照分页、resourceVersion Watch、bookmark、
  410 重列举、五分钟 resync 与 1—30 秒有界退避；稳定 Ready 状态不再重复 PATCH，避免
  自反馈事件环。公开 CRD 删除 L1—L4，Execution Environment 与 AUTO Resource Policy
  分离。14 项单测和 Kind v1.36.1 的 Leader Kill、N-1→N→N-1、finalizer E2E 已通过；
  目标云 API Server/etcd 故障长稳仍待完成；仓库指标与告警后续已由进度 105 关闭，
  目标 Prometheus/Alertmanager/Pager 到达仍待验收。
- 新增 [Python、Go、Java OpenAPI 生成与 SDK 供应链发布门禁](progress/103-Python-Go-Java-OpenAPI生成与SDK供应链发布门禁.md)：
  三语言从正式 OpenAPI 确定性生成 166 个唯一 Operation 与 224 个公开 Schema，旧手写
  Client 保持兼容；Query/Header/必填 Body 契约校验、身份 Header 不可覆盖、结构化错误和
  真实 Transport/HTTP 测试已覆盖。
- 四语言统一使用 `sdks/VERSION=0.1.0`；发布构建生成 TypeScript tgz、Python wheel、
  Go module proxy 三件套、Java JAR/source JAR/POM、SHA256SUMS 和绑定 Contract/Git
  Commit 的 Manifest。精确版本标签流水线会固定工具链、重新生成与测试，以 GitHub
  OIDC 生成 Provenance Attestation 后发布 GitHub Release。npm/PyPI/Maven Central
  命名空间、Trusted Publishing、撤销和客户升级策略仍属于组织 Gate。
- 新增 [Proxy 多 Provider 质量、成本与地域路由闭环](progress/102-Proxy多Provider质量成本地域路由闭环.md)：
  V073 持久化 Provider 成本、信誉、容量与 Session 路由证据；AUTO 候选以健康新鲜度、
  地域、Provider 精确匹配和容量为硬 Gate，再按质量 45%、信誉 20%、成本 15%、地域
  10%、容量余量 10% 稳定评分。悲观锁和 Assignment 预留阻止并发超卖；无健康路线时
  fail-closed，不允许直连回退。
- Session API/OpenAPI/自动生成 TypeScript SDK 与 Web/Tauri 共用 UI 已展示选择模式、
  分数、原因、容量和候选数量；详情保留完整候选证据，列表保持轻量。真实浏览器验收
  修复了多 Provider Catalog 下 Binding 创建按钮被错误禁用的问题。Java/Rust/Web、
  Contract、V073 N/N−1、完整 PostgreSQL 集成和 1024px Playwright 均通过。仍缺目标云
  短期 Secret、商业 Provider Adapter、DNS/认证/限流故障矩阵和高级 SLA 学习路由。
- 新增 [OpenAPI 自动生成 TypeScript SDK 闭环](progress/101-OpenAPI自动生成TypeScript-SDK闭环.md)：
  固定版本生成器和独立 lockfile 从正式 OpenAPI 生成 166 个唯一 Operation、32 个服务与
  241 个 Schema Model；实例化 Client 不共享 Base URL、Token 或 Transport。原手写 Client
  保持兼容，但创建类型和 Operation 类型已改为生成契约。
- 生成清单以 OpenAPI SHA-256、生成器版本、完整文件集合和逐文件 SHA-256 防止漂移；
  `make ci` 现在会重新生成并验证。NodeNext ESM 真实加载、类型构建、运行时请求和仅含
  `dist` 的发布 Tarball 均通过。Python/Go/Java 自动生成及统一 GitHub OIDC 发布门禁
  后续已由进度 103 关闭；目标语言 Registry 仍是组织 Gate。
- 新增 [Proxy 冷 Binding 探测与分布式租约闭环](progress/100-Proxy冷Binding探测与分布式租约闭环.md)：
  V072 为启用但无 `ALLOCATED/BOUND` Session 的 Binding 建立 `SKIP LOCKED` PostgreSQL
  租约、成功/失败/节点不可用差异化周期和配置 Revision 栅栏；多 Control Plane 不会重复
  探测，旧配置的慢结果不会污染新版本。
- Control Plane 只向声明 `proxyColdProbe=network-helper-v1` 的新 Browser Node 发起 mTLS
  RPC；Node Agent 以有界并发通过隔离 Network Helper 解析本机 Provider Route，执行真实
  出口探测后立即释放临时绑定。Secret 正文不进入 Control Plane、Node Agent、样本或
  日志。V072、Proto、N−1 Gate、Java/Rust 全量和完整 PostgreSQL/mTLS/Network Helper
  Integration 已通过；基础质量/成本/信誉/区域/容量联合路由后续已由进度 102 关闭，
  当前 Proxy 剩余为目标云 Secret Manager、商业 Provider 和高级 SLA/业务结果路由。
- 新增 [Proxy 主动健康探测与质量投影闭环](progress/99-Proxy主动健康探测与质量投影闭环.md)：
  Browser Node 默认每 30 秒经隔离 Network Helper 验证运行中 Binding 的真实出口，结果以
  Proto 追加字段和有界错误码立即送达 Control Plane；V071 持久化 7 天凭据无关样本、
  3 失败/2 成功 Hysteresis、成功/延迟 EWMA 和透明质量分，后台健康写不再与管理员配置
  CAS 冲突。
- Proxy API/UI 显示质量、延迟、成功率、真实探测数、失败连续数和新鲜度；固定 5 秒
  轮询已替换为 Workspace SSE，并将事件合并为首次/状态切换/5 分钟刷新。PostgreSQL 17
  空库 V001—V071、完整 Integration、Java/Rust/Web/Contract/N−1、1440/390 真实 API
  页面均通过。冷 Binding 探测后续已由进度 100 关闭，基础联合路由后续已由进度 102
  关闭；当前仍缺目标云 Secret、商业 Provider 和高级 SLA/业务结果路由。
- 新增 [Workspace Overview 权威投影与实时事件闭环](progress/98-Workspace-Overview权威投影与实时事件闭环.md)：
  顶层总览从浏览器端最多 100 条 Session 统计迁移到单条 PostgreSQL 权威聚合，正式覆盖
  Session、Operation、Browser Node、Proxy、Agent、成本和安全信号；最近 Session 仅保留
  8 条导航摘要，不参与统计。
- V070 建立 Payload-minimized 可续传 Workspace SSE，支持 Last-Event-ID、重放、Reset、
  Heartbeat 和双层连接上限；原始 5 秒遥测不触发刷新。平台 Node 容量和全局事件只对
  `PLATFORM_ADMIN` 可见。同步以 expand–validate–contract 修复
  `PAUSED_BY_RESOURCE_POLICY` 缺失于数据库约束的问题。`make ci`、完整 Integration、
  Web Build/Lint、OpenAPI/N−1 和 Playwright 1440/390 验收通过。
- 按 `docs/outline/` 三份大纲与当前代码重新审计未实现项：AUTO 资源的
  产品/API/执行代码闭环已完成，剩余主要是目标 Linux/目标云生产 Gate、
  业务站点 Adapter/Provider、高级 Agent、Phase 7 自动编排与组织签字。
- 校正[当前剩余实现与前端技术质量审计](progress/53-当前剩余实现与前端技术质量审计.md)：
  深色 `text-muted` 在三个主 Surface 上已达 5.01—5.87:1，不再是已知 AA 违规；
  Session Detail 当前产物为 469,683 bytes。审计确认的总览统计失真与缺少服务端
  Workspace Overview 投影/即时更新已由进度 98 关闭。

- 新增 [AUTO 危险事件即时保护闭环](progress/97-AUTO危险事件即时保护闭环.md)：
  Browser Node 从 Cgroup v2 `memory.events`、Profile 文件系统容量和持续 CDP 健康失败
  采集真实 `OOM / DISK_FULL / BROWSER_UNRESPONSIVE`，Crash Event 保留 OOM 分类；不再
  永远上报空 `dangerEvent`。
- Control Plane 对危险事件立即暂停 Agent；OOM 在线时通过正式资源调整 Operation/Node
  ACK 有界扩容，磁盘满先执行非核心降载、持续告警后受控终止，隔离失效直接进入持久
  Stop Operation；普通负载峰值仍必须通过窗口与冷却。Rust/Java 全量、`make ci`、
  `make build` 和完整 Integration 已通过。仍缺目标 Linux 真实 OOM/磁盘/LSM 长稳证书。

- 新增 [Agent 任务大列表轻量投影与游标分页闭环](progress/96-Agent任务大列表轻量投影与游标分页闭环.md)：
  新 `/agent-task-summaries` 使用最多 50 条的 `(created_at, task_id)` Keyset Cursor，固定
  两次 PostgreSQL 查询返回最小摘要和索引覆盖的权威状态聚合；Plan、Allowed Domain、
  Execution Result 和安全事件正文只在选择单个 Task 后读取。
- V069 以非事务 `CREATE INDEX CONCURRENTLY` 增加稳定 Cursor/状态覆盖索引，旧 Offset
  完整列表保留 N−1 兼容。Automation 改为摘要 Infinite Query、详情按需刷新和真实加载/
  失败状态；Java/Web、OpenAPI、N/N−1 Gate、完整 PostgreSQL Integration、真实 Chromium
  E2E 与 Tauri 2 unsigned native build 已通过。
  当前仍缺 10k/50k Agent Task 目标规模容量证书、列表级跨 Region 事件流和高级 Agent。
- 新增 [Workspace 批量元数据 Mutation 闭环](progress/95-Workspace批量元数据Mutation闭环.md)：
  V068 建立独立于生命周期批处理的 Tenant/Actor/Selector/Target/幂等 Header 和逐
  Session Item 租约账本，支持 `ASSIGN_GROUP / REMOVE_GROUP / ASSIGN_TAGS /
  REMOVE_TAGS`；每项最多重试 3 次，取消只影响未执行项，跨租户引用由复合外键和服务
  校验双重拒绝。独立表保证 N−1 生命周期 Worker 不会读取新动作。
- Group/Tag 卡片新增 Web/Tauri 共用批量归属区，支持最多 100 个环境、原因、明确确认、
  真实 Operation 轮询、成功/失败/取消计数、稳定失败码和 Request ID；Viewer 不显示写
  入口。四类动作的 PostgreSQL Integration、幂等/RBAC/租户隔离、OpenAPI、V068
  N/N−1 Gate、Java/Web 和真实 Chromium E2E 已通过。Agent 大列表读取后续由进度 96
  关闭；跨 Region 列表流和目标规模长稳仍待完成。
- 新增 [Saved View Group/Tag 筛选持久化闭环](progress/94-Saved-View-Group-Tag筛选持久化闭环.md)：
  V067 以向后兼容列持久化 Group、最多 16 个 Tag 和 ANY/ALL；数据库校验非法、重复与
  越界 Tag，后端复用租户级 Group/Tag 验证，OpenAPI 请求字段保持可选以兼容 N−1。
- Web/Tauri 共享 Saved View 组件已支持保存、覆盖、摘要和恢复 Group/Tag 组合条件；
  管理员真实 Chromium 完成保存→清除→恢复闭环，Viewer 验证只读应用和写入口隐藏。
  完整 Integration、Java/Web、Contract、V067 N−1 Gate 均通过。
- 新增 [Workspace 批量生命周期与组合筛选闭环](progress/93-Workspace批量生命周期与组合筛选闭环.md)：
  V066 建立 Tenant/Actor/Selector/幂等 Header 与逐 Session Item 账本，每个 Item 外键绑定
  物理路由的 Coordinator Command；Start、Pause Agent、Migrate、Hibernate 聚合真实
  Command、Exclusive Operation 或 Migration Ledger 状态，取消只影响未 Claim 项。
- Environment 列表新增 Group + 最多 16 个 Tag 的 ANY/ALL PostgreSQL 服务端筛选后分页；
  Group/Tag 卡片新增真实批量进度、风险确认、失败码、Request ID 和取消未执行项。
  OpenAPI、Java/Rust/Web、V066 N/N−1、完整 Integration、Tauri Build 与管理员/Viewer
  真实浏览器 E2E 全部通过。Agent 大列表读取后续由进度 96 关闭；跨 Region 列表流和
  目标规模长稳仍待完成。
- 新增 [Session 与 Group/Tag 列表批量投影闭环](progress/92-Session与Group-Tag列表批量投影闭环.md)：
  Session 分页读取已批量加载最新 Context、Coordinator Ownership、Active Operation、
  Tag Summary 和 Proxy Binding；Workspace Groups/Tags 也改为 Tenant 范围固定批次
  后在应用层建立成员投影，不再随 Session、Group 或 Tag 数量追加逐项查询。
- 新增四组回归测试，明确验证列表路径不会回退到逐项 Repository/Service 调用；所有
  数据仍来自正式 PostgreSQL/JPA。Group/Tags 批量生命周期写操作与服务端组合筛选
  后续已由进度 93 关闭；Agent 宽 JSON 大列表读取后续由进度 96 关闭。
- 新增 [认证用户菜单与权限入口闭环](progress/91-认证用户菜单与权限入口闭环.md)：
  顶部静态身份块已替换为 Web/Tauri 共用用户菜单，直接读取当前 OIDC/Local 认证上下文，
  展示真实 Tenant、Actor、认证模式和角色；Workspace 设置与安全中心按现有角色 Gate
  显示，不创建个人资料、密码或会话管理死链接。
- OIDC 模式连接真实 `signoutRedirect`，阻止重复退出并显示失败原因；Local 模式明确
  标识仅用于开发且不伪造退出。1440/390 视口及 Enter/Escape/方向键验收通过。当前仍缺
  真实企业 IdP Claim/MFA/ACR/Logout/租户映射联调和全站辅助技术矩阵。
- 新增 [用户主题偏好与浅色控制台正式闭环](progress/90-用户主题偏好与浅色控制台正式闭环.md)：
  V065 以 Tenant/Actor 复合主键持久化 `SYSTEM / DARK / LIGHT`，无数据时读取系统默认、
  不回填身份目录；重复写相同模式不增版本或重复 Audit。
- Web/Tauri 共用 Theme Provider 和键盘可访问菜单；浅色模式使用独立 OKLCH 冷灰蓝
  Token，资源图表改用语义色。失败写入会视觉回滚并显示 Request ID。Java/Web、
  OpenAPI、V065 N/N−1、完整 Integration、Tauri 构建和 1440/390 浏览器验收通过；
  用户菜单后续已由进度 91 关闭；当前仍缺真实 IdP 和全站辅助技术矩阵。
- 新增 [工作区通知中心正式闭环](progress/89-工作区通知中心正式闭环.md)：V064 将高信号
  Audit Event 在同一 PostgreSQL 事务投影为 90 天通知，并以 Tenant/Actor 单调游标
  持久化已读状态；不回填历史账本，不复制 Audit Detail 或敏感正文。
- 顶部禁用铃铛已替换为 Web/Tauri 共享通知面板，支持未读数、分类/严重度、分页、全部
  已读、资源跳转、离线过期提示和 Request ID。正式 API、RBAC、OpenAPI、V064 N/N−1、
  Java/Web 测试、完整 PostgreSQL/双 Control Plane/三 Browser Node Integration 和
  1440/390 浏览器验收均通过；通知中心自身的即时推送后续已由进度 108 以可续传 SSE
  关闭，当前仍缺跨 Region Event Bus、区域间顺序/复制与外部通知渠道；Overview 实时
  失效流后续由进度 98 关闭，主题与用户菜单后续已由进度 90、91 关闭。
- 新增 [截图敏感区域遮罩与分类闭环](progress/88-截图敏感区域遮罩与分类闭环.md)：
  Agent/Observer 两条 Evidence 管线在 Browser Node 写入 Ephemeral 前统一执行不透明
  遮罩；覆盖 Password/OTP/Payment/Account/Secret/PII、显式 Data Classification、
  开放 Shadow DOM 和跨域 Iframe，并以 10,000 Element 上限失败关闭。
- 遮罩注入后先校验数量，再暂停页面脚本并通过 CDP DOM 域独立复核 Root/Region，
  防止不受信页面在截图瞬间移除遮罩。V063 持久化
  `MASKED / NOT_REQUIRED / FAILED_CLOSED / LEGACY_UNVERIFIED` 和区域数；N−1 截图只标为
  `LEGACY_UNVERIFIED`，Web/Tauri Evidence Card 明确展示保护状态。
- 新增 [Observer 手动截图与受控证据访问闭环](progress/87-Observer手动截图与受控证据访问闭环.md)：
  管理员手动 Capture 通过 PostgreSQL Request、Outbox、固定 Node 命令和真实 CDP JPEG
  执行，Storage Helper commit-last 上传；HumanTakeover 时 Control Plane/Node
  双重阻断，不允许前端直接调用 CDP 或伪造截图。
- V062 建立 Purpose-bound 五分钟 Grant 和同操作者一次性 Redeem；Control Plane 经
  mTLS 请求当前 Placement Node，Storage Helper 校验 `COMMITTED`、大小与 SHA 后只对
  精确对象签发 60 秒 GET。Object Key、Credential 和签名 URL 均不进入数据库/Audit。
  Web/Tauri 共享 Evidence UI/API/权限，完整 PostgreSQL/Node/MinIO Integration 验证
  真实 JPEG 下载与哈希、RBAC、跨操作者/重复领取拒绝和无 URL 泄漏。
- 新增 [全局搜索正式闭环](progress/86-全局搜索正式闭环.md)：顶部禁用入口已替换为
  PostgreSQL/API 驱动的 Web/Tauri 共享命令面板，支持 `⌘K / Ctrl+K`、类型过滤、
  键盘访问、Request ID 和资源跳转。V061 以 `pg_trgm` 建立六类资源有界索引；
  Session 仅索引 ID、Profile、Region、Resource Class、State 和受治理 Display Name，
  不再扫描完整 Metadata。Session/Profile/Group/Tag 严格按 Tenant，Node 结果仅管理员
  可见；Java/Web/OpenAPI、真实 PostgreSQL 空库迁移和 Web/Viewer RBAC E2E 均通过。
- 新增 [多 Provider 与 Safe Point 代理重绑闭环](progress/85-多Provider与SafePoint代理重绑闭环.md)：
  Provider Catalog 以 `providerId + credentialRef` 精确驱动 Control Plane、Node 和
  Network Helper；Node 通过 `proxyProviderDescriptor=v1` 能力标签阻断旧版本误调度，
  多 Provider 不允许隐式选择或直连回退。
- V060 将 `PROXY_REBIND` 纳入持久迁移工作流；管理员在真实 Safe Point 后执行
  Hibernate/Checkpoint、源代理释放、Assignment 切换、同 Node 优先安全重启、
  State Resync、Business Recovery 和 Agent Resume。Web/Tauri 共用详情组件、API、
  RBAC 与统一 SSE，明确 Browser 重启及连接重建风险，不伪造无感切换。Remote
  Desktop 与 Automation 页面也保持 Session 事件订阅，接管状态和 Browser Target
  Revision 不再因页面切换而滞后，旧 Revision 动作仍由服务端 fail-closed。
- 集成验收同时修复释放后旧 `proxyBindingId` 阻断迁移恢复、gRPC 调用方取消导致 Node
  副作用任务中断、同一 Node Event 首次投递与重投并发重复三项可靠性问题。Java/Rust/
  Web/Tauri、真实 Web Console/Viewer RBAC E2E、OpenAPI、V060 N/N−1、Kubernetes
  和 PostgreSQL/三 Node/MinIO/mTLS 完整烟测均通过。运行中主动探测与质量投影后续
  已由进度 99 关闭；冷 Binding 探测和基础联合路由后续已由进度 100、102 关闭；仍缺
  目标云 Secret Manager、商业 Provider Adapter、高级 SLA/业务结果路由和目标环境长稳。
- 新增 [Session 统一事件流与轮询替换](progress/84-Session统一事件流与轮询替换.md)：
  V059 将既有逐 Session 事务游标扩展为无业务正文的统一事件信封，覆盖 Session、
  Browser Current State、Audit、Operation、Agent Task、Resource 和 Safety Lease；
  N−1 原写路径通过数据库 Trigger 自动镜像，原 `/resource-stream` 保留兼容。
- 新 `/event-stream` 支持 Last-Event-ID、Reset、重放、容量上限和租户隔离；
  Web/Tauri 共用客户端已切换到统一流，并删除 Session Detail 与 Browser State 的固定
  2 秒轮询。Java/Web/OpenAPI、V059 N/N−1 和完整 PostgreSQL/双 Control Plane/
  三 Browser Node Integration 已通过。跨 Region Event Bus、列表批量流、慢客户端和
  目标云 Ingress 长稳仍待完成。
- 新增 [Session Coordinator 物理 Shard 命令路由闭环](progress/83-Session-Coordinator物理Shard命令路由闭环.md)：
  V058 建立租户/Session/Route Epoch/Shard 绑定的 PostgreSQL Durable Command Inbox，
  Active Worker Membership 与 Rendezvous Hash 只允许唯一物理 Owner Claim；应用变更
  和 Command Result 在同一事务提交，入口 Pod 不转发 JWT、Cookie 或 Secret。
- Session Start/Terminate/Takeover、Agent Execute/Handoff、Operation/Workflow
  Deadline、Agent Recovery、AUTO 资源决策和迁移 Reconcile 已统一路由。双 Control
  Plane Integration 会计算当前 Owner，并故意把 Start/Terminate 请求发往非 Owner；
  两条命令均由 Owner 提交真实 Operation，输出 `coordinator_command_routed=true`。
  V058 空库/升级/N−1 Gate 和完整 Browser Node/Workflow DLQ/三 Node 迁移烟测通过。
  目标集群长稳、Prometheus/Pager、跨 Pod 网络分区和终态 Command 保留清理仍待完成。
- 新增 [可复用 Proxy Binding 创建时闭环](progress/82-可复用Proxy-Binding创建时闭环.md)：
  V057 分离租户级 `pbind_…` 管理配置、Session 创建时不可变 Assignment 和每 Session
  独立 `pxy_…` Runtime Allocation；创建、更新、禁用和未使用删除具备正式 API、
  ADMIN RBAC、Idempotency-Key、Version CAS、复合租户外键和不含 Secret 正文的审计。
- Session 创建正式提交 `proxyBindingProfileId`，校验 Tenant/Enabled/Region 后固化
  Provider、Version、Expected Exit 和不透明 Secret 引用；Node 实际出口匹配后才
  `BOUND` 并更新健康时间。Web/Tauri 共享管理区和六步向导已连接真实 API，不再使用
  禁用占位或 Metadata。Java/Web/OpenAPI、V057 N/N−1、完整 PostgreSQL/Browser Node/
  Network Helper Integration 和真实浏览器视觉/交互验收均通过。多 Provider 和运行中
  Rebind Operation 后续已在进度 85 关闭，运行中主动探测与质量投影后续已在进度 99
  关闭，冷 Binding 探测后续已在进度 100 关闭；目标云 Secret 解引用与商业 Provider
  质量/成本路由仍是缺口。
- 新增 [迁移目标清理与有界重试闭环](progress/81-迁移目标清理与有界重试闭环.md)：
  V056 持久化目标尝试预算、失败节点、Cleanup Operation 和失败原因；Restore 失败后
  必须先通过 `MIGRATION_CLEANUP → StopRuntime → RuntimeStopped` 屏障回到
  `HIBERNATED`，才可排除源/失败目标并重新 Placement。默认最多 3 次，清理失败或耗尽
  均 fail-closed，Agent 不恢复。
- Integration 将迁移场景扩展为三个独立 Node/Storage Helper：首个目标的真实
  `START_RUNTIME` 因 Helper 故障超时，第三节点 Helper 恢复后，系统完成旧目标清理、
  失败节点持久化、第二次 Restore、State Resync 与 Business Recovery READY。
  `targetAttempt=2`、Cleanup Operation、失败节点列表和最终 Placement 均有断言；
  Session Detail 同步展示真实重试状态，不使用前端模拟。
- 关闭混合 N/N−1 Browser Node 的迁移目标能力准入：新 Node 心跳固定声明
  `startRuntimeGenerationFloor=v1`；迁移使用独立 PostgreSQL 候选查询，在候选上限前
  过滤旧 Node，并由服务层再次校验。只有旧 Node 时 fail-closed，不会把
  `minimum_browser_generation` 下发给会忽略它的目标。
- 双节点 Integration 新增容量充足、排序靠前但不声明能力且 gRPC 不可达的 N−1 形态
  Node；完整迁移仍落到兼容 Node 并通过 Checkpoint、Restore、Resync、Business
  Recovery 和最终 Placement 断言。N/N−1 Gate 同时固定能力声明、查询和调用链。
- 新增 [双 Browser Node、对象存储与迁移集成闭环](progress/80-双BrowserNode对象存储迁移集成闭环.md)：
  Integration Smoke 启动两个独立 Browser Node、两套 Storage Helper/Runtime/Profile
  根和共享 MinIO，以真实 AUTO 压力窗口、Level 1 Node ACK、mTLS 安全观察、
  Checkpoint、排除源 Node Placement、Restore、State Resync 与 Business Validation
  验证迁移完成；源/目标两端同一 `COMMITTED` Marker、目标 Placement、Context Epoch
  和完整资源事件时间线均有断言。
- 迁移验收发现并修复跨 Node `browserGeneration` 从 1 重置的问题：
  `StartRuntimeCommand.minimum_browser_generation` 下发 Control Plane 已提交世代，目标
  Node 启动前恢复下界，保留旧世代事件 fail-closed。测试使用确定性 CDP test double，
  因而关闭仓库级双节点证据，但不替代目标 Linux/正式 Chromium/Kubernetes 长稳与
  网络分区故障矩阵。
- 新增 [Profile / Checkpoint Import 正式闭环](progress/79-Profile-Checkpoint-Import正式闭环.md)：
  V055 建立租户/操作者隔离的 Import Job 与稳定 Operation；Control Plane 通过 mTLS
  流式上传到具备能力标签的 Browser Node，Storage Helper 独立复核大小/SHA、拒绝危险
  Archive 路径并归一化 Manifest，commit-last 写入对象存储后才提交 Profile 与审计。
- Web/Tauri 共享导入 Drawer 已接入 Profile 页面和创建向导，使用浏览器分块 SHA-256、
  正式 Stable Runtime、真实 Job/Request ID 和幂等重放，不使用 Mock 或 localStorage。
  Rust/Java/Web、OpenAPI、V055 N/N−1，以及 PostgreSQL 17 + mTLS Node + 独立 Helper +
  MinIO 完整 Integration 已通过。
- 新增 [Environment Import 正式闭环](progress/78-Environment-Import正式闭环.md)：
  V054 建立操作者隔离的导入 Job/Item 账本；JSON 只作为瞬时输入，预检结果、清单哈希、
  状态和真实 Session/Operation ID 均以 PostgreSQL 为准。
- `preview` 只读校验 Runtime、Profile 租户、Group、Tags、Recovery Contract、AUTO
  资源策略与严格终止权限，不创建 Profile/Session；`commit` 在一个数据库事务中创建
  全部 Session，任一失败全部回滚，写入具备 Idempotency-Key、expectedVersion 与审计。
  Web/Tauri 共享导入工作区已替换原禁用入口，显示真实校验错误、Request ID 和执行
  结果。Java、Web 50 项、OpenAPI、N−1、PostgreSQL 17 空库迁移/全事务回滚
  Integration，以及管理员/Viewer 真实浏览器 E2E 已通过。
- 新增 [Environment Saved Views 正式闭环](progress/77-Environment-Saved-Views正式闭环.md)：
  V053 建立租户隔离的 PERSONAL/WORKSPACE 视图配置、大小写不敏感唯一约束和版本 CAS；
  Viewer 可应用，Operator 管理自己的个人视图，Workspace 写入只允许管理员。
- 创建、覆盖和删除使用真实 API、Idempotency-Key、expectedVersion 与审计链；环境页保存
  主视图、精确状态、搜索和列配置，不保存 Session 结果快照或分页缓存。Web/Tauri 复用
  同一 React 组件和 API Client，并补齐管理员与 Viewer 的真实浏览器 E2E。
- 新增 [Business Recovery Provider Evidence 可信证明闭环](progress/76-Business-Recovery-Provider-Evidence可信证明闭环.md)：
  V052 将账号、租户/工作区、权限和业务实体证明要求纳入精确不可变 Contract Revision；
  只有独立 `APPLICATION_ADAPTER` 身份能按当前 Context/State 提交短时有效、哈希化的
  Provider 观察结果，普通 Operator 不能伪造证据。
- 迁移在证据缺失、过期或 UNKNOWN 时保持 `BUSINESS_VALIDATION` 且 Agent 暂停；
  MISMATCH 映射到明确业务 Verdict，MATCH 才允许 Ready Gate 继续。写入具备
  Idempotency-Key、租户隔离、不可变账本、引用哈希与 Audit；Session Detail 只读展示
  Provider Attestation Ledger，契约作者 UI 支持正式证明要求。Java、Web 48 项、
  OpenAPI/N−1 和完整 PostgreSQL/Chromium Integration 已通过。
- 新增 [Application Recovery Contract 版本 Diff 与受控回滚](progress/75-Application-Recovery-Contract版本Diff与受控回滚.md)：
  正式 API 返回 PostgreSQL 不可变历史及精确审批状态，并在服务端生成逐字段 Diff；
  管理员可选择已批准历史版本，以 CAS、Idempotency-Key 和原因生成新的 DRAFT 头版本。
- 受控回滚不会 UPDATE 历史、继承旧审批或静默切换 Session；新版本必须重新经过双人
  审批，既有 Session 仍按原精确 Revision 运行。Web 新增历史索引、Diff、新旧值和
  两步恢复确认，保持 Web/Tauri 组件复用。Java、Web 47 项、OpenAPI、N−1、
  PostgreSQL/Browser Node Integration、管理员与 Viewer 真实浏览器 E2E 全部通过。
- 新增 [Application Recovery Contract 不可变历史与 Session Rebind](progress/74-Application-Recovery-Contract不可变历史与Session-Rebind.md)：
  V051 追加保存完整不可变 Contract Revision，Trigger 兼容 N−1 写入并禁止历史
  UPDATE/DELETE；Business Recovery 和自动恢复现在读取 Session 绑定的精确正文，
  发布 v2 后获批 v1 Session 仍可继续按 v1 运行，当前头停用仍是全局 Kill Switch。
- 新增管理员显式 Rebind API 和真实 `APPLICATION_BINDING / COMMITTED` Operation；
  Session/Binding 行锁、CAS、活跃 Operation Gate、Idempotency-Key、精确批准、复合
  外键和哈希审计在同一事务闭环。Session Detail 显示绑定/最新版本与升级入口。
  未绑定/未验证的可选读取使用 204，不再制造预期 404 控制台噪声。Java、Web 46 项
  测试、Build/Lint、OpenAPI、PostgreSQL 17 V001—V051、完整 PostgreSQL/Browser
  Node Integration，以及管理员与 Viewer 真实浏览器 E2E 均通过。
- 新增 [Application Recovery Contract 版本审批与审计闭环](progress/73-Application-Recovery-Contract版本审批与审计闭环.md)：
  V050 建立精确 Contract Version 的 REQUESTED/APPROVED/REJECTED 双人审批记录、
  证据哈希和租户审计事件；请求幂等，同一操作者不能自批，迁移不会伪造历史批准。
- V050 先将 Session Binding 固化为不可为空的 `contract_version`，阻断静默策略漂移；
  V051 已进一步保存完整不可变正文，使获批 v1 Session 在发布 v2 后继续按 v1 运行，
  需要升级时必须执行显式 Rebind；N−1 Trigger 不伪造批准。
- Web 契约工作区新增 DRAFT/PENDING/APPROVED/REJECTED 状态、理由、请求、批准和驳回；
  创建向导只列出已批准契约。Java/Web/OpenAPI、V050 N/N−1、PostgreSQL 17 空库迁移
  和完整 PostgreSQL/Chromium Integration 均通过。
- 新增 [AUTO 截图证据与采样执行闭环](progress/72-AUTO截图证据与采样执行闭环.md)：
  V049 持久 Node 权威成功截图采样和租户隔离 Evidence 元数据；Browser Node 使用真实
  `Page.captureScreenshot` JPEG，成功动作稳定采样，失败动作/导航始终尝试留证。
- 截图只落入 Session ephemeral 有界路径，由 Storage Helper 校验路径、SHA、大小后
  commit-last 上传；对象失败会持久化 FAILED 事件，不伪造成功。达到资源上限时
  Operation/ACK 将成功截图 100%→10%；正式 Evidence API 不暴露 Object Key，Session
  Detail 显示真实证据事件。
- 新增 [公开资源等级迁移为 Resource Template](progress/71-公开资源等级迁移为Resource-Template.md)：
  V048 为企业费率增加 `resource_template`，通过同步 Trigger 保持 N−1 写入兼容；
  Session、Context、Placement、成本解释、企业费率、OpenAPI、Web 和四 SDK 不再公开
  `L0–L5`，创建 SDK 统一默认提交 `resourcePolicy.mode=AUTO`。
- 新增 [AUTO 独立像素录制与停止执行闭环](progress/70-AUTO独立像素录制与停止执行闭环.md)：
  V047 分开持久化创建时请求值与 Node 当前确认值；Browser Node 通过独立回环 CDP
  Screencast 真实采集 JPEG，使用 8 帧有界队列和 4 MiB/10 秒滚动段。
- 段文件只能落入 Session ephemeral 固定目录，并由持有对象存储凭据的 Storage Helper
  校验路径、符号链接、大小和 SHA-256 后 commit-last 上传。达到资源上限时会真实停止
  CDP 录制、排空队列、提交最终标记后再 ACK；Create API、Placement、Resource
  Event、四 SDK 和 Web 创建/详情均使用正式字段。
- 新增 [AUTO Observer 帧率在线执行闭环](progress/69-AUTO-Observer帧率在线执行闭环.md)：
  V046 持久 Node 权威确认的 Observer FPS；有桌面 Session 正常为 30 FPS，达到资源
  上限的非核心降载调整为 5 FPS，无桌面严格为 0。
- Browser Node 在真实受控 VNC Gateway 的 Server→Client 路径执行在线节流，并把
  待发送 Observer 数据与 Client→Server Human Input 分离；1 FPS 背压测试证明输入
  不等待 Observer 定时器。Start/Adjust/回滚/ACK、Placement、Resource API/Event 和
  Web 详情使用同一值，并纳入 V046 N/N−1 与完整 Integration 验收。
- 新增 [AUTO 成功 Trace 动态采样闭环](progress/68-AUTO成功Trace动态采样闭环.md)：
  V045 持久 Node 权威确认的成功 Trace 采样百分比；正常默认 100%，达到资源上限的
  非核心降载调整为 10%。
- Browser Node 使用 Message ID 稳定哈希对现有结构化成功命令 Trace 采样，失败、
  Crash、Audit、Operation 和 Billing 不进入采样器。Start/Adjust/ACK、Placement、
  Resource API/Event 与 Web 详情使用同一真实值，并纳入 V045 N/N−1 和完整
  Integration 验收。
- 新增 [AUTO 非特权扩展后台暂停闭环](progress/67-AUTO非特权扩展后台暂停闭环.md)：
  V044 持久 Node 权威确认的暂停扩展列表；达到资源上限时，Control Plane 只从
  Session 已绑定且 Profile `privileged=false` 的扩展中选择目标，未知和特权扩展保持
  运行。
- Browser Node 通过回环 CDP 对 background page/service worker/worker 执行可逆
  `Debugger.pause/resume`，并持续抑制策略生效后重启的后台 Target；首次执行成功后
  才 ACK。Placement、Resource API/Event 和 Web 详情使用同一真实列表，V044、
  Protobuf N/N−1、Java/Rust/Web 与完整 Integration 均纳入验收。
- 新增 [Node Command 物理分片派发与双重栅栏](progress/64-Node-Command物理分片派发与双重栅栏.md)：
  V041 在事务 Outbox 捕获权威 Route Epoch/Shard，以 PostgreSQL 短 Membership Lease、
  Rendezvous Hash 和 `FOR UPDATE SKIP LOCKED` 把 Shard 动态分配给 Deployment Pod；
  Pod 正常退出主动注销，被强杀后由 3 秒 Worker Lease/6 秒 Claim Lease 接管。
- `CommandEnvelope` 新增兼容 Route 字段；Browser Node Journal 在同一 SQLite 事务中
  原子校验并提交 Route Epoch/Shard 与 Coordinator Term，拒绝旧 Route、错误 Shard 和
  旧 Term，避免任一栅栏被失败命令提前污染。
- Node Event 入口现在允许 Service 落到任意健康 Pod，但只接受权威 Route/Term 且远端
  Owner Lease 仍有效的事件，不替其他 Pod 续租。Java/Rust、Contract、V041 N/N-1 和
  完整故障切换 Integration 均通过；验收同时修复旧 Runtime Monitor 跨 Browser
  Generation 残留并重复触发 Recovery 的竞态。HTTP/Timer/Workflow 等 Session
  Coordinator 命令的物理 Owner 路由后续已由进度 83 关闭；目标集群长稳仍待完成。
- 新增 [Hot Tenant 权威路由与 Route Epoch 安全迁移](progress/63-Hot-Tenant权威路由与Route-Epoch安全迁移.md)：
  V040 持久化 Tenant Route、逐 Session Binding 与 Migration Attempt，并把
  `route_epoch` 加入 Coordinator Ownership 栅栏。
- 生产 Coordinator 命令现在读取 PostgreSQL 权威 Route；新 Session 在迁移期间直接进入
  目标 Epoch，旧 Session 只有在同一 Session 行锁下重新得到 SAFE Verdict 后才迁移。
  Unsafe Session 保留原 Browser/Route，旧 Epoch owner 不能续租。
- 新增 Platform Admin 幂等迁移 API、expected Epoch CAS、后台分批 Reconcile 与租户
  审计。Java Check、OpenAPI/Contract、V040 N/N-1、PostgreSQL 升级夹具和完整
  Browser Node Integration 通过；Node Command 物理分片后续已在进度 64 关闭，
  Session Coordinator 入口物理路由后续已在进度 83 关闭；双 Coordinator 热点长稳
  仍是目标环境 Gate。
- 新增 [Application Recovery Contract 作者工作区](progress/62-Application-Recovery-Contract作者工作区.md)：
  “扩展与应用”页面新增真实契约索引和版本化编辑器，连接现有 PostgreSQL/Admin API；
  支持创建、启停、Origin/Route、四类可访问性证据、Extension 绑定、Depth Limited、
  有界恢复动作与尝试预算。
- Web 写入使用 `expectedVersion` CAS，409 不覆盖较新版本；前端按后端同一边界校验
  Origin、Route、Target、Extension ID 和动作预算。Lint、13 个测试文件/42 项测试、
  production build，以及 1440/390 Playwright 视觉与交互验收通过。
- 新增 [受信 Extension 自动恢复动作闭环](progress/61-受信Extension自动恢复动作闭环.md)：
  V039 将 `RESTART_EXTENSION` 接入版本化 Contract、Placement Gate、持久 Attempt、
  Node Command/ACK 和二次 Ready Gate；目标必须是 Contract 必需且当前 Session 已绑定
  的真实 Chromium Extension ID。
- Browser Node 只在匹配的 `chrome-extension://<id>/` 受信 Context 中执行固定
  `chrome.runtime.reload()`，找不到 Target 或 ID/Context 不匹配时 fail-closed，不会
  扩大为 Browser 重启。OpenAPI/Proto、Java/Rust、Web、V039 N/N-1 和完整
  PostgreSQL/Browser Node Integration 已通过。
- 新增 [Session Extension 正式绑定与投影](progress/60-Session-Extension正式绑定与投影.md)：
  V038 将规范化 Extension IDs 固化到 Session，从旧资源需求确定性回填，并让创建、
  Demand 和 Placement 使用同一集合；终止 Session 后创建绑定仍保留。
- Session List/Detail、环境列表和详情页已使用正式 Extension 投影。Java/Web/OpenAPI、
  V038 N/N-1 Gate 和完整 PostgreSQL/Browser Node Integration 已通过；逐 Extension
  归因和目标 Linux 企业扩展长稳仍是明确缺口；受信重启动作已在进度 61 关闭。
- 新增 [Agent Policy 一等契约与执行强制](progress/59-Agent-Policy一等契约与执行强制.md)：
  V037 将 `DISABLED`、`RESTRICTED`、`BALANCED`、`INTERACTIVE` 固化到 Session 和
  Agent Task，按旧 Metadata 确定性回填，并在 Planner、Executor 与 Handoff 重复校验。
- 创建向导不再提交 Agent Policy Metadata；Session List/Detail、环境列表、Automation
  和 Agent Task 使用正式 API 投影。Disabled/Restricted 冲突由服务端保存为 BLOCKED，
  不产生执行 Step；Java/Web/OpenAPI/V037 N/N-1 Gate 和完整 PostgreSQL/Browser Node
  Integration 已通过。Session/Group/Tag 读取投影 N+1 后续由进度 92 关闭；高级
  独立 Agent Worker 后续由进度 113 关闭，Reviewer/固定 Responses Provider/模型治理
  后续由进度 114 关闭；高级 Validator、客户大规模 Replay 仍是缺口，Agent 大列表读取
  后续由进度 96 关闭。
- 新增 [Workspace Settings 与创建默认值正式闭环](progress/58-Workspace-Settings与创建默认值正式闭环.md)：
  V036 建立租户级工作区名称、默认 Runtime/Region/HumanTakeover 权威设置；GET 明确区分
  系统默认与已持久化覆盖，PUT 具备 Admin RBAC、幂等重放、Runtime 发布 Gate 和租户
  审计。
- Runtime Build 与 HumanTakeover 已从任意 Metadata 升级为正式 Create Session
  契约。显式创建值优先，否则继承 Workspace/系统默认；值在创建时固化，启动使用
  Session 已绑定 Runtime，禁用接管由服务端返回 `HUMAN_TAKEOVER_DISABLED`。
- Web Settings、侧边栏、创建向导和 Session Detail 已连接正式 API，并补齐窄屏设置
  导航与字段标签。Java/Web/OpenAPI/V036 N/N-1 Gate 和完整
  PostgreSQL/Browser Node Integration 已通过；Agent Policy 一等契约后续已在进度
  59 关闭；Saved View 后续已在进度 77 关闭；Environment Import 后续已在进度 78
  关闭；Profile Import 后续已在进度 79 关闭；创建时 Proxy Binding 后续已在进度 82
  关闭；运行中 Proxy Rebind 与 Provider Catalog 后续已在进度 85 关闭，运行中主动
  探测和质量投影后续已在进度 99 关闭，冷 Binding 探测后续已在进度 100 关闭；目标云
  Secret 解引用仍是明确缺口；Session/Group/Tag 列表批量投影后续由进度 92
  关闭；全局搜索、通知中心和主题后续已由进度 86、89、90 关闭。
- 新增 [Workspace Tags 正式数据模型与 UI](progress/57-Workspace-Tags正式数据模型与UI.md)：
  V035 建立租户级标签与 Session 多对多归属，并用 Session/Tag 复合外键在数据库层阻断
  跨租户写入；同租户名称大小写不敏感唯一，重复归属并发安全，删除标签不会终止
  Browser Session。
- 创建 Session 新增正式 `tagIds`，List/Detail 返回受控标签投影；Tag CRUD、归属、
  RBAC、幂等和审计均接 PostgreSQL。V035 会将旧 `metadata.tags` 逗号字符串确定性
  回填，旧字段在 N/N-1 窗口继续保留。
- Web “分组与标签”、创建向导、环境列表和 Session Detail 已连接真实 Tag API，
  不再用自由文本或 Mock 伪造标签。Java/Web/OpenAPI/V035 Gate 与完整
  PostgreSQL/Browser Node Integration 已通过；Saved View 后续已在进度 77 关闭；
  列表批量投影后续已由进度 92 关闭；Group/Tags 批量生命周期 Operation 和服务端
  组合过滤后续已由进度 93 关闭。
- 新增 [Business Recovery 有界自动动作闭环](progress/56-Business-Recovery有界自动动作闭环.md)：
  V034 持久化契约动作、每次 Attempt、命令、Deadline、Base/Result State Version 和
  REQUESTED/EXECUTING/ACKNOWLEDGED/COMMITTED/FAILED 状态；迁移新增
  `BUSINESS_RECOVERY_ACTION`，重启后可继续对账，且尝试号进入校验幂等键。
- 当前只允许 `RELOAD`、`REFRESH_SESSION`、`NAVIGATE_HOME` 和
  `REOPEN_KNOWN_ROUTE`；目标只能从规范化 Application Contract 解析，禁止租户
  JavaScript、任意 CDP Method 和任意 URL。Login、权限、账号不一致和人工恢复 Verdict
  不会触发自动动作；受信 `RESTART_EXTENSION` 后续已在进度 61 关闭。
- Browser Node 真实执行 CDP `Page.reload`/受限导航，Full State Resync 后通过既有
  `BrowserStateUpdated` 返回 Action ID 和递增 State Version；Control Plane 收到权威
  ACK 后再次执行 Business Ready Gate，READY 才恢复 Agent。Session Detail 显示动作、
  状态、尝试数和版本推进。
- Java/Rust、OpenAPI、V034 N/N-1 Gate、Web lint/37 项测试/生产构建和完整
  PostgreSQL/Browser Node Integration 已通过。集成实际验证
  `Page.reload → State ACK → COMMITTED → 二次 READY → Migration COMPLETED`；
  Provider Evidence 平台协议后续已在进度 76 关闭；目标站点 Adapter、真实 Provider
  凭据接入和目标 Linux 双 Node/S3 长稳仍是明确缺口；仓库级双 Node 迁移 E2E 已由
  进度 80 关闭。契约作者 UI 后续已在进度 62 关闭，精确版本审批/审计后续已在
  进度 73 关闭。
- 新增 [Workspace Groups 正式数据模型与 UI](progress/55-Workspace-Groups正式数据模型与UI.md)：
  V033 建立租户隔离的 `workspace_groups` 与 Session 外键；创建、更新、删除、成员归属、
  幂等和审计全部进入 PostgreSQL/正式 API，删除分组只解除归属，不终止 Browser。
- 创建 Session 可提交 `groupId`；未显式提交资源策略时继承 Group 的 AUTO 默认策略，
  显式策略优先。`TERMINATE_STRICT` Group 默认值仅 Platform Admin 可配置。
- Web Groups 页面和创建向导已连接正式 API，移除生产 Fixture、Mock 数据和路由开关；
  lint、37 项 Web 单测、生产构建、Java Check、OpenAPI、V033 N/N-1 Gate 与完整
  PostgreSQL/Browser Node Integration 已通过。Tags 正式能力后续已在进度 57 关闭；
  列表查询 N+1 后续已由进度 92 关闭；批量生命周期操作与服务端组合筛选后续已由
  进度 93 关闭；全局搜索、
  通知中心和主题后续已由进度 86、89、90 关闭；Settings 核心持久化已在进度 58
  关闭。
- 新增 [Media 编码遥测与 Slot 执行闭环](progress/54-Media编码遥测与Slot执行闭环.md)：
  x11vnc 真实 RFB 编码进程进入独立 Media Cgroup，Runtime Supervisor 从 `cpu.stat`
  读取累计 CPU，Node 按相邻样本/单调时钟/当前 Slot 上报真实
  `mediaEncoderPercent`；无 Cgroup、首次样本和非 Media Session 保持为空。
- V032 将 Placement 的最大预留 `mediaSlots` 与当前执行 `mediaEncoderSlots` 分离。
  AUTO 决策只在预留范围内增减当前 Slot，并通过真实 Resource Operation 下发；
  Media Cgroup `cpu.weight` 写入成功后 Node 才 ACK，Control Plane 校验旧值与上限后
  持久化和写入资源时间线。协议新增 optional 字段并通过 N/N-1 Gate。
- Session Resource API/Web 已展示 Media 当前/最大 Slot和真实编码压力。Rust、Java、
  Web、升级兼容和完整 PostgreSQL/Browser Node Integration 均通过；无 Cgroup
  Session 保持实际 Slot值，未把请求值伪造为执行成功。
- 当前实现基于现有 x11vnc/RFB 编码路径，不等于硬件 H.264/H.265/AV1 Helper；
  编码器级动态码率、GPU 指标和目标 Linux 多 Session 长稳仍是明确 Gate；仓库级
  双 Node/Object Storage/CDP 数据面迁移已由进度 80 完成，仍缺目标环境正式 Chromium
  故障矩阵；独立 CDP Pixel Recording、分段队列和上限停止已在进度 70 完成，但不
  等于硬件 Codec/MP4/WebRTC Recording。
- 新增 [当前剩余实现与前端技术质量审计](progress/53-当前剩余实现与前端技术质量审计.md)：
  重新以三份大纲、OpenAPI、迁移、Control Plane、Browser Node、Web 和部署清单交叉核对
  未完成项，区分可直接开发、目标环境 Gate 和组织 Gate。
- Media Encoder Producer/Slot 已在进度 54 关闭仓库内实现缺口，有界低风险
  Business Recovery 动作已在进度 56 关闭，Workspace Tags 正式能力已在进度 57
  关闭，Workspace Settings 核心 API 已在进度 58 关闭，Provider Evidence 平台协议
  已在进度 76 关闭，State/Audit/Operation/Agent Task 统一事件已在进度 84 关闭；
  运行中 Proxy Rebind 与 Provider Catalog 已在进度 85 关闭，运行中主动探测与质量
  投影已在进度 99 关闭；当前最高优先级转为目标业务 Adapter/真实 Provider Secret
  接入、冷 Binding 探测/质量成本联合路由和目标 Linux/多 Node
  正式 Chromium 生产 Gate；仓库级双 Node 集成证书已由进度 80 关闭。
- Web 技术审计更新为 14/20：lint、42 项单测和生产构建均通过；Settings 字段标签与
  基础窄屏布局已修复，发布前仍需关闭 muted 文本对比度、触控命中区、完整辅助技术
  矩阵和 Session Detail 路由包体积问题。
- 新增 [Extension 真实加载、遥测与资源权重](progress/52-Extension真实加载遥测与资源权重.md)：
  Placement Extension ID 通过向后兼容协议下发，Node 只从可信根目录解析并校验
  Manifest，Chromium 按 Session 精确加载扩展；Linux 将真实 `--extension-process`
  进程归入独立 Cgroup，并从 `cpu.stat`/`memory.current` 产生真实聚合指标。
- V031 持久化 Extension `cpu.weight`；AUTO 决策通过 Operation 下发在线调整，Node ACK
  携带实际旧/新值。无 Cgroup 的真实 PostgreSQL/Browser Node 集成仍回报旧值，验证
  控制面不会伪造执行成功。Web 资源详情已显示 Extension Weight、CPU 压力和 RSS。
- 当前仍是 Session 级 Extension 进程聚合，目标 Linux 真实 Chromium/企业扩展长稳和
  逐扩展归因尚未关闭；Media x11vnc Producer/Slot 已在进度 54 完成。
- 新增 [应用感知 Business Recovery 与 Ready Gate](progress/51-应用感知Business-Recovery-Ready-Gate.md)：
  V030 以 PostgreSQL 持久化 Tenant/Application 版本化恢复契约、Session 不可变绑定和
  每次恢复 Verdict；DSL 只允许精确 Origin、Route Prefix、可访问性 Target 与
  Extension 证据，不执行租户 JavaScript 或正则表达式。
- 创建 Session 可绑定真实 `applicationId`；迁移 `BUSINESS_VALIDATION` 使用同一权威
  服务，只有 READY/READY_WITH_WARNING 才恢复 Agent。Web 创建向导和 Session 详情已
  接真实 API，显示契约版本、Context/State Version、Evidence 和 Request ID。
- `make ci`、Web 36 项测试、OpenAPI、V030 N/N-1 Gate 和完整
  `make test-integration` 已通过。真实 CDP 集成发现并修复 Target `name=null` 导致的
  500；当前匹配保持 null-safe 和 fail-closed。
- 当时未完成的 `maximumAutoRecovery` 执行链现已在进度 56 关闭；Provider Evidence
  平台协议与迁移 Gate 已在进度 76 关闭；仓库级双 Browser Node/Object Storage 迁移
  集成已在进度 80 关闭；仍未完成的是各目标业务站点 Adapter、真实 CRM/支付 Provider
  凭据接入以及目标 Linux 双 Browser Node 长稳；
  `RESTART_EXTENSION` 已在进度 61 关闭，契约作者 UI 已在进度 62 关闭，精确版本
  审批/审计已在进度 73 关闭。平台不会把声明式校验和
  低风险动作夸大为任意网站自动恢复。
- 新增 [应用业务安全点 Lease 闭环](progress/50-应用业务安全点Lease闭环.md)：
  可信应用 Adapter 可在 CDP 无法判定的 SPA/API Form、支付/账号安全、关键事务和
  Business Recovery Unknown 前 Acquire 5—300 秒短 Lease，持续 Renew，并在业务提交
  或回滚明确后 Release；它是显式阻塞协议，不是网页语义检测器。
  Lease、Owner、Context Epoch、TTL 和 ACQUIRED/RENEWED/RELEASED/EXPIRED 事件全部进入
  PostgreSQL，不使用内存状态、JSON 或前端定时器模拟。
- Safe Point Aggregator 只聚合当前 Context 内未过期的 ACTIVE Lease；Acquire/Renew/
  Release 与迁移/休眠最终派发使用同一 Session 行锁，关闭“判定 SAFE 后新业务事务
  开始”的并发窗口。生命周期 Operation 已开始时，新的应用 Lease fail-closed 拒绝。
- Resource SSE 事务游标新增 `SAFETY_LEASE_EVENT`；Web 会刷新权威 Safe Point，并按
  Blocker `expiresAt` 在自然过期后重新读取。真实 PostgreSQL/Chromium Integration
  已验证幂等重放、Owner 隔离、阻塞/解除、SSE 实时与断点重放。
- 这关闭的是通用 Producer API/Lease 协议，不等于已为任意网站自动识别支付或关键
  事务；声明式 Business Recovery DSL 已在进度 51 完成，有界低风险自动动作已在
  进度 56 完成，Provider Evidence 平台协议已在进度 76 完成，Extension 重启动作已在
  进度 61 完成；各 Tenant/Application Adapter 的真实业务接入仍是明确 Gate。
- 新增 [Browser/Profile I/O 真实遥测闭环](progress/49-Browser-ProfileIO真实遥测闭环.md)：
  Session Cgroup 将 Chromium 与 Xvfb/x11vnc 分入 Browser/Desktop 子组，Runtime
  Supervisor 从 Linux Cgroup v2 `io.stat` 读取真实累计读写字节，Node Agent 按相邻
  采样和单调时间计算速率；首次样本、计数器回退和不可用控制器均保持字段为空。
- Node 以 `profileIoTelemetry=browser-cgroup-io-v1/unavailable` 声明能力；既有可选
  Protobuf、PostgreSQL、30 秒 P95/EWMA 决策链已接通，持续 50 MiB/s 压力会产生
  `SUSTAINED_PROFILE_IO_PRESSURE`，单次尖峰不会直接扩容。Session 详情显示真实速率
  和压力线，不生成模拟曲线。
- 该指标覆盖 Chromium 进程树的 Profile、Cache 和其他 Browser I/O，是资源压力信号，
  不是 Profile 目录的路径级精确计费。目标 Linux 多 Session 长稳仍是 Gate；AUTO
  当时剩余真实遥测/执行器缺口为 Extension 与 Media；二者分别在进度 52、54 完成。
- 新增 [CDP 浏览器活动 Safe Point](progress/48-CDP浏览器活动SafePoint.md)：Browser Node
  通过持续 CDP Target/Network/Browser Download 事件，真实采集正在进行的文件上传、
  文件下载和导航级表单提交；断线后同一 Runtime Generation 保持 fail-closed，不把
  丢失的在途请求误判为安全。
- Node 以 `safePointBrowserActivity=cdp-network-v1` 声明能力；Control Plane 只对已
  声明能力的新 Node 强制要求三类信号，旧 Node 继续按 Input Ledger 判定，实现
  N/N-1 滚动兼容。V028 已按 Expand→Validate→Contract 扩展安全信号约束。
- Safe Point 当前会聚合 Input/Drag、Upload、Download、Form、HumanTakeover、
  Operation、Agent Task 和 Durable Workflow；Session 详情已显示新增阻塞原因。
  支付/账号安全、SPA 应用语义和关键业务事务现已有显式 Lease Producer API；仍需
  目标业务 Adapter 实际调用，未被误报为自动语义识别。
- 修复安全专用资源上报中空 `dangerEvent` 被误判为 Critical、以及安全样本稀释 AUTO
  CPU 决策窗口的问题；真实 PostgreSQL 17 + Browser Node 集成已验证 5 个 Node
  Safe Point 信号均为 LIVE/非活跃，真实 Chrome CDP 测试也已通过。
- 新增 [State Collector 与远程桌面在线执行器](progress/47-StateCollector与远程桌面在线执行器.md)：
  V027 持久化每个 Session 的 State Collector 预算和 Remote Desktop 码率；创建、启动、
  Placement、资源 API、Web 详情和资源时间线均使用同一份 PostgreSQL 权威值。
- State Collector 预算会真实改变采集 cadence、Diff 字节/变更上限和 CDP Target 上限；
  Remote Desktop Gateway 会按 Session 码率限制真实 VNC Server→WebSocket 数据发送，
  不把网关限速误述为媒体编码器控制。
- `AdjustRuntimeResources` 采用可选 Protobuf 字段兼容 N/N-1；Node 只有在 Cgroup、
  State Collector 和已注册桌面执行器全部成功后才发送实际 ACK，后续执行失败或事件
  序号失败会回滚已应用值。无 Cgroup 的本地节点会明确回报 CPU/内存未变，只提交实际
  成功的非 Cgroup 调整，不伪造资源执行结果。
- 修复新建 Policy 解析内部模板时错误写入 `lastAdjustedAt`、导致首次自动扩容被 5 分钟
  冷却期阻断的问题；真实 PostgreSQL 17 + Browser Node 集成已验证持续压力触发
  `stateCollectorBudgetPercent=50 → 75`，Node ACK 后才持久化和写入时间线。
- Extension Resource Weight 与 Media Encoder Slot 后续已分别在进度 52、54 完成；
  Remote Desktop 路径已有 Rust 数据面测试，但仍缺双真实桌面 Node 的 Control Plane
  端到端与目标 Linux 长稳证书。
- 新增 [AUTO 细粒度资源信号与决策迟滞](progress/46-AUTO细粒度资源信号与决策迟滞.md)：
  Browser Node 已通过真实 CDP、Agent 执行路径、持久 Node Journal 和 Remote Desktop
  数据面采集 Renderer、Tab、主线程 `TaskDuration` 差值、Agent Action 延迟、
  State Diff 待交付深度和远程桌面帧龄；不可用时保持字段为空，不生成模拟值。
- Control Plane 的 30 秒决策窗口现在会对 CPU、内存、PSI、Renderer、Tab、主线程、
  Agent、State Diff、Profile I/O、Extension、Remote Desktop 和 Media 指标执行
  持续时间、P95、EWMA 与缩容迟滞判断；单次尖峰不会直接扩容，次级压力会阻止缩容。
- State Diff 深度在 Node 重启后从未交付 Journal 重建，只有 Control Plane 接受事件并
  标记已交付后才递减；Remote Desktop 无活跃客户端时不伪造 Frame Age。
- 本轮本地已通过 Browser Node 全 Workspace Test/Clippy、Control Plane Test/Check
  和完整 `make test-integration`。该阶段当时尚缺的 Profile I/O 已在进度 49 补齐；
  Extension 已在进度 52 补齐进程级指标与 Weight；Media 已在进度 54 补齐
  x11vnc Cgroup Producer 与 Slot。
- 新增 [Tauri 2 桌面端安全容器与发布基线](progress/45-Tauri2桌面端安全容器与发布基线.md)：
  `apps/desktop` 复用 Web React/API/权限状态，完成最小 Capability、CSP、单实例、托盘、
  Deep Link、系统浏览器 OIDC、macOS Keychain/Windows Credential Manager、Updater
  签名门禁和桌面状态 UI；未引入 Shell/FS/任意 HTTP 权限。
- Desktop OIDC 的 PKCE/User/Refresh Token 使用原生系统凭据库，不写 localStorage；
  Platform Adapter 保持业务组件跨 Web/Tauri 复用。Web 单测现为 9 文件/34 项。
- macOS 本机 `cargo check --locked` 和 `tauri build --no-bundle` 已成功产出 Release
  二进制；新增 macOS/Windows 独立 Workflow。Apple/Microsoft 代码签名、Notarization、
  真实 Updater/IdP、真实 Windows 安装/系统集成和桌面视觉/无障碍矩阵仍明确为发布 Gate；
  GitHub macOS/Windows Desktop Workflow 已全部通过。
- 新增 [资源事件 SSE 与断点恢复](progress/44-资源事件SSE与断点恢复.md)：
  V026 以按 Tenant/Session 的事务游标统一排序 Resource Sample/Event，V029 将应用
  安全 Lease Event 接入同一游标；正式 SSE 支持 `Last-Event-ID`、重放、Reset、
  Keepalive、连接上限和租户隔离。
- Web Session 详情已移除 Resource/Safe Point/Migration 的 5 秒/30 秒轮询，改用认证
  Fetch Stream 驱动权威 API 失效刷新；断线显示数据可能过期，并以指数退避自动恢复。
- PostgreSQL 17 真实集成发现并修复了连接缓存 Sequence 导致的 `101 → 6` 游标倒退；
  当前每 Session 写事务串行分配，实时提交和断点重放均已通过 `make test-integration`。
- 新增 [Safe Point、自动休眠与跨节点迁移闭环](progress/43-SafePoint自动休眠与跨节点迁移闭环.md)：
  V024 持久化安全信号，Node 每 5 秒上报真实 Input Ledger；缺失/过期信号 fail-closed，
  并聚合 HumanTakeover、Agent Task 和 Durable Workflow 阻塞。
- `HIBERNATE` 与 `TERMINATE_STRICT` 已接入真实 Operation/Node 回调；V025 持久化
  Checkpoint、排除源 Node 的 Placement、S3 校验恢复、Full State Resync 和 Business
  Recovery Validation，只有 READY 才恢复被资源策略暂停的 Agent。
- 新增 Safe Point/Migration 正式 API 和 Session 详情状态卡；核心迁移链已从 V025
  继续扩展到 V030/V034；仓库级双 Browser Node + MinIO + CDP 数据面迁移 E2E 已由
  进度 80 关闭；目标 Restore 失败后的 Cleanup 屏障、失败节点排除和有界重试已由
  进度 81 关闭。正式 Chromium/目标 Linux 完整故障矩阵、目标业务 Adapter 和真实
  Provider 接入仍是明确 Gate；Provider Evidence 平台协议已在进度 76 关闭，
  `RESTART_EXTENSION` 已在进度 61 关闭。
- Storage Helper 恢复采用本地优先、对象存储兜底；`make test-integration` 已实际通过
  两次 Checkpoint、四次 Profile 恢复、Coordinator 故障接管、Node 重启与 Runtime
  Crash Recovery。
- 新增 [AUTO 资源遥测与在线执行闭环](progress/42-AUTO资源遥测与在线执行闭环.md)：
  Browser Node 每 5 秒上报真实 CPU、RSS 和 Memory PSI；Control Plane 通过 30 秒窗口、
  P95、EWMA、快扩慢缩与冷却期创建真实资源 Operation；Node 在 Cgroup 调整成功后返回
  ACK Event，控制面收到前不更新当前分配。
- 在线执行器支持 `cpu.max`、`memory.high/max` 和 `pids.max`，缩容前校验当前使用，
  写入失败恢复旧限制；非 Workflow Operation 增加超时扫描，避免 Node 故障时永久 ACTIVE。
- 修复 GitHub CI 的两个确定性问题：Kind N/N-1 Checkout 使用完整 Git 历史；
  Integration Smoke 使用隔离 PSI Fixture，不再读取 Runner 宿主机瞬时压力。
- 修复 Node 重启时 CDP 固定端口复用引发的伪 Crash/Recovery；Integration Smoke 继续
  严格验证一次 Recovery、Context Epoch 5 与 Browser Generation 3，本地全仓
  `make test-integration` 和 `make ci` 均已通过。
- 新增 [Web Console AUTO 资源策略与详情面板](progress/41-WebConsole-AUTO资源策略与详情面板.md)：
  创建向导删除 L1～L5，默认提交 `resourcePolicy.mode=AUTO`；运行环境与资源策略分离；
  V022 增加 Policy/Sample/Event 权威表；资源 GET/Event GET/Policy PATCH、幂等 Operation、
  Platform Admin 严格终止 Gate、30 秒窗口决策、达到上限暂停 Agent/保留 Browser、
  Session 资源组件和真实 API 联调已完成。
- Resource Class 与内部 Template 继续用于后端 Placement，但普通 Web 用户不再看到或提交；
  无 Node Session 级样本时资源详情明确显示 `AWAITING_TELEMETRY`，不生成模拟曲线。
- Node Critical PSI 默认治理从立即终止改为暂停 Agent、保留 Browser 并等待安全点；
  在线 Cgroup 调整器和 Browser Node 5 秒基础采集已完成；安全点迁移、自动休眠执行、
  SSE 与其余细分指标仍列为缺口。
- 本轮验证：Control Plane 118 项、Web 26 项、Web Build/Lint、V022/V023 PostgreSQL 实迁移、
  AUTO 创建/启动/Placement/Policy 幂等/403 权限，以及 1440 与 390×844 Playwright
  视觉检查均通过。
- 新增 [Web Console 环境管理与六步创建向导](progress/40-WebConsole-环境管理与六步创建向导.md)：
  重构 Global/Page/Toolbar/Content 四层结构、独立空状态、236/68px 导航、1440 抽屉与
  1280 全屏适配，并接入真实 Runtime、Profile、Proxy、Region、Extension 与 Create
  Session API；该阶段的临时 L2 选择现已由本轮 AUTO Resource Policy 完整替代。
- Session 列表新增租户隔离的 PostgreSQL 服务端搜索，URL 同步搜索/筛选/分页；
  Web E2E 实际按新建环境名称查询，修复原生 SQL 排序字段映射后通过。
- `make test-e2e` 完成两次六步创建、真实启动/终止、State、Proxy、noVNC、Agent、
  Security、Profile 与 Viewer/RBAC 全链路，输出 `WEB_CONSOLE_E2E_OK` 和
  `WEB_CONSOLE_VIEWER_RBAC_OK`。
- 修复 Viewer 在创建向导关闭时仍预取管理员 API 的 403/RBAC 回归；无操作权限时不再
  挂载向导或发出相关请求。
- 新增 [Phase 7 企业运营核心验收](progress/38-Phase7-企业运营核心验收.md)：
  Validation Farm、授权 Replay/Compatibility、Cost-aware Placement、SLO/Error
  Budget、Retention/Residency/Compliance、GameDay/DR Registry、Media、四语言 SDK、
  Terraform Module 和企业运营 UI 已通过本地验收。
- 新增 [Phase 4—7 本地最终验收与生产 Gate](progress/39-Phase4-7-本地最终验收与生产Gate.md)，
  将仓库内已关闭项、必跑命令、目标环境和组织 Gate 分开记录。
- Operator Kind E2E 升级为真实 N/N-1：先从提交 `540a72e` 构建并运行 N-1，
  Kill Leader 后升级 N、保持 Session Ready，再回滚 N-1；严格验证创建 4 次、
  终止 1 次。
- `V019`—`V021` 完成企业运营迁移；兼容 Gate 验证 Expand-only/default、
  Protobuf Field Number、JSON Optional、RollingUpdate、PDB 与 Probe，Evidence Hash
  `2c40f45b...72a55f63`。
- 真实 Chrome 兼容矩阵绑定授权 Replay Dataset、环境摘要、浏览器版本和 Validation
  Evidence；Navigate/Read/Type/Scroll 通过，跨域与非 Allowlist 继续 fail-closed。
- 新增 SLA Exclusion、Legal Hold 删除阻断、删除 Receipt、License Inventory 和签名
  Audit Export；集成测试对 HMAC 签名做独立复算，不只检查格式。
- 新增 [Phase 6 Browser Density、资源硬限制与真实 UI](progress/37-Phase6-Browser-Density与真实UI.md)：
  Resource/Extension/Placement、Node mTLS 容量与 PSI 心跳、多 Node 命令路由、Cgroup
  v2 硬限制以及 Nodes/Extensions/三步创建真实 UI 已通过 Integration 与 Web E2E。
- 同一进度项新增本机真实 Chrome 并发容量证书：500/500、并发 4、Start P99
  6.543s、Stop P99 152ms、FD 增长 0、残留进程 0；PSI Critical 增加单 Tick
  单 Session 有界驱逐，多 Node gRPC 精确路由测试通过。
- 新增至少 500 轮才可通过的真实 Chromium 生命周期证书工具；逐轮验证 CDP Health、
  整棵 Profile 进程树、PID/端口释放、Profile 可删除及 Runner RSS/FD 增长。
- 正式证书绑定源码提交 `e59e24a`，使用 Chrome 150 完成 500/500：Start P99 708ms、
  Stop P99 20ms、RSS 增长 6,356,992 bytes、FD 增长 0、残留进程 0。
- 证书 JSON 已纳入 [容量证据](evidence/capacity/runtime-capacity-e59e24a.json)，
  Canonical Hash 已独立复算为 `aa06b74b...f684838`。
- 新增 [Phase 1 真实 Chromium 500 次生命周期证书](progress/36-Phase1-真实Chromium-500次生命周期证书.md)，
  明确该结果不替代 Linux Cgroup 或并发 Browser Capacity。
- Coordinator Reconcile 新增延迟 Histogram、旧 Operation 中止、Cleanup Started/
  Failure 四项低基数指标；完整换主演练跨 C/D 两个进程验证指标。
- Control Plane 增加 Prometheus Actuator Registry；生产端点只允许
  `PLATFORM_ADMIN`，继续受 Admin MFA Validator 约束。
- Kubernetes 基线增加 Cleanup Failure、Stale Operation Spike、Reconcile P99 三条
  Alert Rule ConfigMap，并在 Incident Runbook 补换主响应流程。
- 新增 [Phase 5 Coordinator Reconcile 可观测性](progress/35-Phase5-Coordinator-Reconcile可观测性.md)，
  明确目标 Prometheus/Alertmanager/Pager 接入仍待生产验收。
- Remote Desktop Gateway 增加有界可配置 Disconnect Grace、Heartbeat 和 Client
  Liveness Timeout；生产配置强制 0.5—10s/1—60s/5—120s，并要求存活窗口至少覆盖
  两个心跳。
- Gateway 单测验证输入已发送后客户端黑洞会触发一次 Disconnect Barrier，Grace 内
  重连会通过 Connection Generation 抑制旧连接误释放。
- Web E2E 在 Vite 与 Gateway 间加入独立 TCP 故障代理；未配对 Shift KeyDown 后
  `SIGSTOP` 代理，验证无 WebSocket Close 时 Node 仍执行 `clear_all`、State Resync
  和 HumanTakeover 自动结束。
- 新增 [Phase 5 Remote Desktop 输入网络分区](progress/34-Phase5-RemoteDesktop网络分区.md)，
  保留目标云 Ingress/CNI 单向分区、跨 Region 和媒体容量缺口。
- 新增 [当前未实现清单](progress/33-当前未实现清单.md)，以提交 `666cdf4` 为基线，
  重新区分代码缺口、Stage A 骨架和只差生产环境/组织验收的 Gate。
- Coordinator 新增旧 term Active Operation Reconcile：Lifecycle Operation fail-closed
  转为新 term Termination Cleanup；HumanTakeover 按当前 Actor 重建 Begin/End Barrier；
  旧 Agent/Human 输入使用幂等 `ReleaseAllInput` 清理。
- Agent Executor 创建/短 Lease 恢复前进入 Coordinator Ownership/Reconcile；旧 Agent
  Operation 中止后任务稳定失败为 `COORDINATOR_FAILOVER_ABORTED`，不重放结果未知的写 Step。
- 同一 `make test-integration` 新增第二次真实换主：暂停 Node 使 Navigate 保持 pending，
  `SIGKILL` Coordinator B，Coordinator C 由 Recovery Scanner 自动取得 term=3；
  term=2 Agent Operation=`ABORTED`、Task=`FAILED`/`currentStep=0`，后续 Agent 正常完成。
- Agent Executor Lease 改为生产默认 30 秒、受控范围 1—300 秒；演练使用 2 秒以确定性
  触发恢复，不修改生产默认值。
- 同次 Node Pause/B Kill 新增独立 Session 的 STARTING 未知结果竞态：C claim term=2，
  旧 Start Operation=`ABORTED`，新 Stop Cleanup=`COMMITTED`，Session=`TERMINATED`；
  覆盖旧 Start 已执行或被新 term 拒绝两种安全收敛路径。
- 另一个独立 Session 先进入 RUNNING，再在 Node Pause 后提交 Stop；B Kill/C 接管后，
  旧 Stop term=1=`ABORTED`，新 Stop term=2=`COMMITTED`，验证 TERMINATING
  未知结果通过幂等 Stop 和 fencing 收敛。
- 指定 Recovery Profile 的第二次 Fake Chromium 启动延迟 30 秒；从 Node Journal
  精确 Kill Runtime PID 并确认 `RECOVERING` Active 后再 Kill B。C 将 term=1
  Recovery=`ABORTED`、term=2 Stop Cleanup=`COMMITTED`，Session 安全终止。
- 两个独立 Session 分别在 Node Pause 时固定 HumanTakeover `PREPARING` 与
  `COMPLETING`；C 以 term=2 重建 Begin/End，旧 Operation 均 `ABORTED`，
  replacement 均 `COMMITTED`，随后 Session 可正常终止。
- TYPE_TEXT 副作用场景暂停 C、恢复 Node，确认 Node Journal 已持久化 Command Result
  且 Event 未提交，再 `SIGKILL` C。D claim term=4 后 Task fail-closed，
  TYPE_TEXT Capability 仅消费一次，后续 Agent 执行正常。
- `make test-integration` 从安全点 Kill 升级为 HumanTakeover `EXECUTING` 时真实
  `SIGKILL`：term=1 Operation=`ABORTED`，term=2 Replacement=`COMMITTED`，后续
  Agent/Crash Recovery/Node Restart/Terminate 全链路继续通过。
- 修复 S3 SDK feature 合并造成的 rustls CryptoProvider 启动 panic：Node Agent
  显式安装进程级 `ring` Provider，Node 单测与真实 mTLS 集成均通过。
- 新增 [Phase 5 Coordinator 进行中 Operation 接管](progress/32-Phase5-Coordinator进行中Operation接管.md)，
  单独记录已关闭的 HumanTakeover 场景及 Runtime/Agent 真实竞态剩余项。
- Storage Helper 新增 S3-compatible Cold Archive：本地 Checkpoint 完整提交后打包
  `checkpoint.tar.zst`，远端依次写 Archive、Manifest、最后写 `COMMITTED`。
- 远端 Commit Marker 绑定 Checkpoint/Write Epoch、Content Hash、Archive SHA-256
  与字节数；Cache/Ephemeral 不进入归档，生产环境拒绝 HTTP Endpoint。
- 新增 Connect/Operation 有界超时；S3 失败会让 IPC fail-closed，但本地已提交
  Checkpoint 保持完整且可用同一 ID 重试，不产生远端伪提交。
- 新增 `make test-object-storage`：固定版本 MinIO 正常归档并逐对象读取成功；
  暂停容器后 500ms 内失败，本地 Checkpoint 仍可校验与重新打包。
- 新增 [Phase 5 Object Storage 归档与超时演练](progress/31-Phase5-Object-Storage归档与超时演练.md)，
  保留 Multipart Resume、跨 Region Restore、KMS/IAM 和目标云验证缺口。
- 新增 `make test-coordinator-capacity` 和 Build-bound JSON 证书：固定 50,000 Actor、
  250,000 Route、64 Shard 与 10,000 个满载 Mailbox，记录 JVM/OS/CPU、P95/P99、
  吞吐、Shard 分布、Gate 与证书 SHA-256。
- 本机 Stage A 结果：Route P99 1,416ns、Emergency Offer+Poll P99 4,416ns，
  64/64 Shard 使用、10,000/10,000 Emergency 抢占、最大 Shard 负载低于平均 2 倍，
  Hysteresis 全序列通过；该任务已进入 `make ci`。
- 新增 [Phase 6 Coordinator Capacity Certificate Stage A](progress/30-Phase6-Coordinator-Capacity-Stage-A.md)，
  明确该证书不包含数据库/gRPC/容器 GC 长稳，不能冒充生产 50k 并发承诺。
- Coordinator Ownership 从未接线骨架升级为运行时强制边界：所有 Session Command
  必须持有 PostgreSQL Lease；过期后使用原子 CAS 接管并将 `coordinator_term` 严格加一。
- Node Event 同时校验当前 Owner 与 Term；旧实例不能提交，旧 term 事件收到终止性
  `STALE_COORDINATOR_TERM` 并从 Node Journal 清除，避免永久重投。
- Node Runtime Monitor 在每次 State/Crash 上报前读取最新已接受 term；接管后的
  Begin/End Takeover 命令会同步 Node fencing 世代，后续崩溃恢复不再携带旧 term。
- `make test-integration` 新增真实 Coordinator Kill GameDay：`SIGKILL` 实例 A，
  实例 B 在 3 秒 Lease 到期后以 term=2 接管；随后继续完成状态重同步、Runtime
  Crash Recovery、Node Restart Reconciliation、HumanTakeover 与 Session Terminate。
- 新增 [Phase 5 Coordinator Kill 与 Term Fencing 演练](progress/29-Phase5-Coordinator-Kill与Term-Fencing演练.md)，
  明确本次关闭的是安全点接管；进行中 Operation 的恢复/中止及双实例长稳仍是 Gate 缺口。
- PostgreSQL 连接异常不再泄漏为通用 500；统一返回脱敏
  `503 DATABASE_UNAVAILABLE`，并保留 Request ID。
- Hikari 获取/验证与 PostgreSQL 建连/Socket 读取增加可配置超时上界，网络黑洞不会无限
  占用 API 请求线程。
- 新增 `make test-postgres-outage`：真实暂停 PostgreSQL 容器，验证读写均在 3 秒内
  返回 503、控制面存活；恢复后同一幂等键继续成功，已提交数据不丢失且无部分写入。
- 新增 [Phase 5 PostgreSQL 短时不可用演练](progress/28-Phase5-PostgreSQL短时不可用演练.md)，
  明确本次关闭项与托管数据库 Failover、Coordinator Kill、Object Storage 等剩余边界。
- Web Console 新增 OIDC Authorization Code 客户端、Callback/会话恢复/自动续期/退出；
  生产缺少 Authority 或 Client ID 时 fail-closed，不再读取构建时静态 Access Token。
- Tenant/Actor/Role 从运行时身份贯穿所有 API；Bearer 模式不发送可伪造的本地租户和
  Actor Header。本地 Header 身份仅保留给显式开发/E2E 模式。
- 完成 Viewer/Operator/Admin/Security Admin/Platform Admin 导航、路由和操作 Gate；
  Viewer 可读但不可创建、启动、终止、Resync、Agent 或接管。
- 安全中心按后端权限拆分租户安全与平台密钥工作区，避免单角色页面发起必然 403 的请求。
- Groups、Nodes、Extensions 生产路由 fail-closed；新增 Skip Link、导航可访问名称、
  64px 移动端图标栏和窄屏顶部布局。
- `make test-e2e` 增加生产模式 Viewer 构建，输出 `WEB_CONSOLE_VIEWER_RBAC_OK`，
  自动验证写入口隐藏、越权路由、Fixture 404、身份 Header 和 390×844 布局。
- 新增 [Web Console OIDC、RBAC 与生产边界](progress/27-WebConsole-OIDC-RBAC与生产边界.md)，
  单独记录已关闭项、验收证据和真实 IdP/实时事件等剩余缺口。
- 新增真实 Chrome Agent 兼容矩阵：完整启动 mTLS Control Plane、Browser Node、
  Network/Storage Helper、PostgreSQL、Redis 与精确域名出口白名单；真实访问并读取
  `example.com`、W3C，确定性验证 Navigate/Read/Type/Scroll。
- Agent 执行后新增通用 Domain Allowlist 验证；跨域点击被代理拦截后，Chrome
  `chrome-error://` 状态稳定失败为 `POST_ACTION_DOMAIN_NOT_ALLOWED`，不再造成事件
  处理异常和重投风暴。
- 修复真实 CDP 输入协议：Control/Shift 等非字符按键不再发送非法 `"text": null`；
  `TYPE_TEXT` 已由真实 Chrome 执行，API 只返回输入 Hash、长度和分类，正文不回显。
- Storage Helper 将 macOS Chrome 的 `RunningChromeVersion` 识别为已知 Ephemeral
  运行时标记，真实 Chrome 可正常 Checkpoint；任意未知 Profile 符号链接仍 fail-closed。
- 新增 [Phase 4 真实网址与 Agent 控制验收](progress/26-Phase4-真实网址与Agent控制验收.md)，
  固化 `make test-real-url-agent`、公网与确定性夹具分层策略及剩余边界。
- BrowserSession Operator 补齐 Kubernetes Lease Leader Election；双副本只由当前
  Holder 调和，租约过期后使用 `resourceVersion` CAS 接管，RBAC 仅新增 Lease
  `get/create/update/patch`。
- 修复 Operator 容器入口 `operator.py` 遮蔽 Python 标准库导致的启动失败，以及
  namespaced BrowserSession PATCH URL 顺序错误；新增 7 项路径和选主单测。
- 新增临时 Kind 集群 E2E：真实安装 CRD/RBAC/Deployment，验证 CRD admission、
  最小权限、Ready/observedGeneration/finalizer、Leader Pod Kill 接管、第二个会话继续
  调和和删除终止；Mock 统计严格为创建 2、终止 1。
- 修复 Linux 纳秒时钟与 PostgreSQL 微秒时间戳精度差导致的 Agent Task 首次响应/
  幂等重放差异；创建时间在构造响应前规范到微秒，并新增精度单测。集成 smoke
  的 JSON 幂等断言改为结构比较，不再依赖对象字段序列化顺序。
- Control Plane、Browser Node 三容器、Web Console 与 Operator 生产清单统一增加
  `appArmorProfile: RuntimeDefault`；Kind 因嵌套节点不支持宿主 AppArmor，测试先做
  Server-side Dry-run 验证后仅对临时运行清单移除该字段，Seccomp/非 root/只读根文件
  系统/Capability Drop 仍由真实 Pod 执行。
- GitHub Release Run `30195955615` 完整成功：四个 GHCR 镜像按不可变 Digest 构建，
  生成 SPDX SBOM，执行 Keyless Cosign 签名和 SPDX Attestation。
- Release Manifest Schema v2 同时绑定 Source Commit、四个镜像 Digest、四份 SBOM
  SHA-256 和 Production Kustomization Hash；签名 Bundle、渲染 YAML 与证据作为
  `production-release-2665fa...` Artifact 上传并重新下载验证通过。
- 修复主 CI 失效的 Trivy Action 引用，所有 CI Action 固定到 Commit SHA；升级
  Tonic/Prost/Rustls 依赖链，移除 `rustls-webpki 0.102.8` 高危 CRL Panic 漏洞。
- 新增 V017 Secure Debug Session/Access Event：一次 Break-glass Grant 只能消费一次，
  Debug 会话不超过 Grant 剩余时间且最长 15 分钟。
- 每次读取重新验证 Tenant、Operator、精确资源/Scope 和 Grant 状态；非 Operator、
  跨租户、过期或撤销访问全部 fail-closed，拒绝尝试也形成访问证据和统一审计。
- Secure Debug 只暴露 URL Origin、运行上下文版本、State Hash/质量和目标计数；完整
  URL/Query、标题、DOM、目标名称/坐标、截图、Cookie 与 Profile 内容不进入响应。
- START/SNAPSHOT/ACCESS_DENIED/END/EXPIRE/REVOKE 使用独立 Previous Hash 访问链，
  并同步追加租户 `ADMIN_ACCESS` 防篡改审计。
- 安全中心新增真实 Secure Debug 工作区，完成授权消费、最小快照、Evidence Head 与
  主动结束；数据库集成和真实浏览器 E2E 均通过。
- 修复 Remote Desktop Gateway Heartbeat 首次 Tick 与 RFB Echo 测试竞争：首次 Ping
  延后一个完整 Interval，测试显式处理控制帧并连续 30 次通过。
- 将 Storage Helper 从 Node Agent 进程内拆为独立二进制；固定 IPC 只允许
  Acquire/Checkpoint/Release，不传 Profile 内容，Node 会独立重算并校验 Workspace 路径。
- Checkpoint 对同一 Profile Write Epoch 幂等；响应丢失重试会验证 Manifest、Marker
  与全部文件 Hash 后返回原 Checkpoint，不重复递增 Epoch。
- 集成测试在 Profile Checkpoint 前 Kill Storage Helper，确认 Runtime 已停止、Node Agent
  存活、命令 fail-closed；Helper 重启后同一终止操作成功，Checkpoint Epoch 仍严格为 2。
- Kubernetes/Compose 将 Profile 拆为独立卷；Storage Helper 使用独立 UID `11003`，
  不挂载 Node Journal/Runtime、Network Secret 或 mTLS Secret。
- 将 Network Helper 从 Node Agent 进程内依赖拆为独立二进制；Node Agent 不再读取
  Static Proxy Endpoint、Expected Exit 或探测配置，只通过固定 Unix Socket IPC 调用。
- Helper IPC 具备 Schema Version、64 KiB Frame 上限、请求 ID 关联、超时、固定
  `PING/BIND/VERIFY/RELEASE` Allowlist 和 Unix Peer UID 校验。
- Kubernetes Browser Node Pod 新增独立 Network Helper 容器；Node Agent/Helper 使用
  `11001/11002` 不同 UID、共享内存 Socket、只读根文件系统、RuntimeDefault seccomp
  与全部 Capability Drop。
- 集成测试主动 Kill Helper，确认 Node Agent 存活、Chromium 未启动、命令
  fail-closed；Helper 重启后控制面原命令自动重试成功。真实 Web Console E2E 同步通过。
- 新增 V015 Key Rotation 治理：五类 Key Scope、五类触发原因、Platform Admin 双人审批、
  Dual-read/Single-write 重叠窗口、疑似泄露快速路径、验证 Gate 和双证据 Hash。
- 安全中心新增真实 Key Rotation 工作区；Web E2E 已从 UI 发起轮换并验证职责分离提示。
- 集成测试把 Browser Node mTLS 证书轮换登记为治理证据，并验证错误验证不能完成、
  跨控制租户 404 以及五条 `KEY_ROTATION` 哈希链事件；八类必需审计现已全部接入。
- 新增 V016 Runtime 密码学 Provenance：Artifact Digest、Signing Key ID、受信 Ed25519
  Public Key 映射与 Canonical Payload 验签；单测验证有效签名、篡改制品和未知 Key。
- 新增 V014 Runtime Release 治理：独立 `PLATFORM_ADMIN`、双人晋级/禁用、职责分离、
  证据哈希、跨控制租户 404、发布状态与 `RUNTIME_RELEASE` 防篡改审计。
- 集成测试实际执行 Runtime 禁用，验证本人不能自批、第二管理员批准、Registry 进入
  `DISABLED` 并追加 `RUNTIME_RELEASE` 证据。
- 新增 V013 Break-glass 治理：工单、原因、精确资源/Scope、5—60 分钟授权、
  申请/审批职责分离、Reject、Revoke、自动过期和事后 Review。
- Security Admin API 与安全中心 UI 已接入真实 Break-glass 请求；本人申请不能自批，
  第二管理员激活后生成 64 位 Evidence Hash，跨租户不可见。
- 自批拒绝使用独立审计事务，确保业务事务回滚时拒绝证据仍进入防篡改链。
- Prompt Security、Agent 高风险确认/Handoff、Profile Restore 分别接入
  `SECURITY_EVENT`、`HUMAN_AUTHORIZATION`、`PROFILE_RESTORE`。
- 完整回归确认 Break-glass 双人审批、跨租户 404、审批时过期持久撤销、事后 Review
  与 35 条事件哈希链；真实浏览器从安全中心创建请求，页面无 Console Error 或未预期
  HTTP Error。
- Runtime、Logs 与 Security 页面已删除 Fixture，分别接入 Build Registry 和租户隔离的
  防篡改审计 API；新增 Loading/Error/Empty、搜索、链 Head Hash、响应式桌面/移动视图。
- 新增 `GET /api/v1/runtime-builds`、OpenAPI 契约和平台 API 单元测试；集成测试验证
  本地 Stable Build 的 Registry、签名元数据与 SBOM 元数据可查询。
- 修复事件流“暂停视图”仅切换文案但未冻结事件快照的问题。
- 完成 Phase 4—7 大纲与代码的逐项缺口复核，明确 Phase 5/6 Gate 未关闭、Phase 7
  尚未实现，并单独记录真实网址与通用 Agent 的当前边界。
- REST 身份从请求头信任切换为生产 OIDC JWT Principal；本地 Header 仅在非生产启用，
  并新增 Tenant Viewer/Operator/Admin/Security Admin 方法级 RBAC 与管理员 MFA Claim Gate。
- Control Plane 与 Browser Node 双向 gRPC 支持 CA 双向认证；生产环境拒绝明文，
  集成测试使用临时 CA 并在 Node 重启时轮换证书。
- 新增租户序列化 SHA-256 审计链、查询/过滤 API、Retention/Legal Hold 字段和详情脱敏；
  真实集成链验证 24+ 条事件的前向哈希和 Head Hash。
- 通用 Durable Workflow Stage A 已绑定 Session 启停 Operation，具备 Phase Deadline、
  乐观 CAS、幂等键、Stale Callback、Commit Marker、基础 Proxy 补偿与 Dead Letter。
- Runtime 启动前执行 Build Registry、Stable Validation、Signature 和 SBOM Gate；
  CI 新增 CycloneDX SBOM 与 High/Critical Trivy 阻断。
- 新增仓库级 Threat Model、Incident Response 和 Key Rotation Runbook。
- 新增 Coordinator Shard Router、Hot Tenant Virtual Partition、Route Epoch、优先级
  Bounded Mailbox、Passivation 条件及在线容量 Hysteresis。
- 新增 BrowserSession CRD、带 Finalizer/observedGeneration 的 Operator、独立
  Control Plane/Browser Node Pool、Kata RuntimeClass、CNI Egress、CSI Warm Tier、
  PDB 与 RollingUpdate 清单；`kubectl kustomize` 已通过。

- 审阅 `docs/prompt/` 两份 UI/工程提示词并固化设计上下文。
- 保留并整理现有 Neo-Industrial Observatory UI，修复工程依赖、CSP、可访问性和路由。
- 总览与环境管理从生产 Mock 切换为真实 Session API。
- 完成真实创建、服务端状态筛选/分页、详情、启动与终止确认流程。
- 增加统一 Loading、Empty、Error、Request ID、Offline 和 Operation 状态反馈。
- 未实现模块在开发环境明确标识 Fixture，生产构建默认隐藏 Fixture 数据。
- 路由按页面拆包，删除旧版重复 Session 页面。
- 增加 4 个 API 单元测试与真实浏览器端到端测试。
- 扩展 Session 正式读契约与受控 Repository Projection，列表和详情现可读取
  `displayName`、`profileId`、`region`、`resourceTemplate`，不向客户端暴露任意 metadata。
- 环境列表和详情页改为展示真实名称、Profile、区域、解析模板、Runtime 与 Node；
  已有 Session 没有合法 `displayName` 时安全回退到 Session ID。
- 增加 Java 应用服务契约测试，并在真实集成测试中验证新增字段的数据库到 API 闭环。
- 新增 `NodeEventService.Publish` Protobuf/gRPC 契约及 Java、Rust、TypeScript 生成物。
- Browser Node 回传 `RuntimeStarted/RuntimeStopped`，发送失败时通过重复命令重投进程内结果。
- Control Plane 在事务内完成 Event Inbox 去重、Tenant/Term/Epoch/Sequence 校验、
  Session Context/State 提交与 Operation Commit。
- 集成测试已验证 Start/Terminate 两个 Operation 均进入 `COMMITTED`，两个事件进入 Inbox。
- 真实浏览器测试已改为等待“运行中/已终止”终态，不再只验证活跃 Operation。
- Runtime 启动现在必须通过 `/json/version` CDP Readiness，健康检查同时验证进程与 CDP。
- 新增真实 CDP State Collector，通过 WebSocket 采集 URL、Title、交互目标、角色、
  可见性、坐标、State Version 与内容哈希，并限制 CDP 仅可使用 Node 回环地址。
- 使用真实 Chromium 验证页面导航、按钮/输入框状态采集、Runtime 启停、CDP 健康与
  CPU/内存采样。
- 新增 SQLite Node Journal，持久化命令结果、Coordinator Term、Event Sequence 与
  待确认 Node Event。
- Node 重启后会扫描 Journal 并自动重投事件；测试验证 Journal 关闭/重开后事件仍可
  被 Control Plane gRPC 接收并标记已送达。
- Runtime 主动健康监控在浏览器进程退出时持久化并发布 `BrowserCrashed`，失败事件进入
  同一 Journal 重投链路。
- Input Sandbox 新增真实 CDP 键盘、鼠标执行器，按单 Session 序列化输入，
  拒绝陈旧 sequence、幂等处理重复 sequence，并在 Stop 时执行 All-keys-up /
  All-buttons-up。
- Node Runtime Monitor 已接入输入释放看门狗：输入通道超过 5 秒无活动但账本仍有
  按键、按钮或拖拽状态时，主动向 Chromium 发送释放事件；中键/右键 CDP 位掩码和
  Enter、Tab、Escape、删除键、方向键映射均有回归覆盖。
- 新增 `ExecuteInputCommand`、`BrowserStateEvent` 和交互目标 Protobuf 契约及三语言生成物。
- Browser Node 周期采集页面状态，内容哈希变化时持久化事件到 SQLite Journal 并上报；
  Control Plane 完成版本校验、JSONB 最新状态存储和租户隔离的
  `GET /api/v1/sessions/{sessionId}/state`。
- Session 详情页已接入真实 Browser State，展示 URL、标题、质量、版本、哈希、坐标与
  可见/可用标志；运行期间每 2 秒刷新，204 时明确显示尚未采集而不伪造数据。
- 集成烟雾测试现验证 Browser State 从假 Chromium CDP WebSocket 经 Node Event、
  Inbox、PostgreSQL 到 REST API 的完整链路。
- Coordinator 收到 `BrowserCrashed` 后会抢占活跃写操作，创建优先级 95 的
  `RECOVERY` 排他 Operation，重启同一 Runtime Build，并在 `RuntimeStarted`
  返回后递增 Context Epoch/Browser Generation、提交 Operation 和恢复 `RUNNING`。
- Recovery 采用一小时最多三次的持久 Operation 计数熔断；超过预算或缺少 Runtime
  Build 时进入 `FAILED`，避免无限重启。
- Node Journal 新增 Runtime Lease，持久化 Tenant、Term、Context、Generation、
  PID 与进程启动身份。Node 重启会校验 PID 身份、清理遗留进程、恢复 Generation
  下界并发布 Crash Recovery Event。
- Start/Stop 命令结果、待投事件与 Runtime Lease 转换在同一 SQLite Immediate
  Transaction 中提交，关闭“Runtime 已启动但 Lease/Event 尚未落盘”的崩溃窗口。
- 修复 State Content Hash 将版本化 `target_ref` 纳入哈希导致静态页面重复上报的问题；
  相同页面的 State Version 可递增，但 Content Hash 保持稳定。
- 集成测试实际强杀 Chromium 后验证自动恢复到 Epoch 2，再重启 Browser Node 验证
  Runtime Lease 对账和恢复到 Epoch 3，最后正常终止。
- 新增正式 `BeginHumanTakeover` / `EndHumanTakeover` Command 与
  `HumanTakeoverReady` / `HumanTakeoverEnded` Event，三语言 Contract 与 OpenAPI
  同步生成。
- HumanTakeover 以优先级 90 的排他 Operation 抢占低优先级可抢占操作；Node 在接管开始
  和结束时强制 All-keys-up / All-buttons-up，并把即时 Browser State 作为 Ready/Ended
  Event 一并提交。
- Control Plane 只在 Ready Event 后进入 `EXECUTING`，只在 Ended Event 携带 State
  Resync 后提交 Operation；错误 Actor、过期 Epoch、缺失 State 的 Event 均被拒绝。
- Web Console Session 详情接入真实人工接管 Mutation，远程桌面工作区展示 Operation、
  Actor、State 与输入安全状态；只有持有 Operation 的 Actor 可以打开和释放接管。
- 集成测试与真实浏览器 E2E 已验证 HumanTakeover
  `PREPARING → EXECUTING → COMPLETING → COMMITTED` 全链路。
- 新增 Rust `remote-desktop-gateway`：只代理二进制 RFB，VNC TCP 仅注册回环地址；
  HMAC Ticket 绑定 Tenant、Actor、Coordinator Term、Context/Operation Epoch，限制
  120 秒寿命、一次性 Nonce、Origin 白名单和单 Session 单客户端。
- Browser Node 已托管 Xvfb/x11vnc 图形 Runtime，WebSocket 数据面具备 Ping/Pong
  半开连接检测和两秒重连宽限；生产环境拒绝默认 Ticket Secret 和空 Origin 白名单。
- Gateway 断线不依赖前端回调：Node 强制执行 CDP 与 x11vnc All-keys-up /
  All-buttons-up，重采集 Browser State，并把带 `GATEWAY_DISCONNECT` 原因的
  `HumanTakeoverEnded` 持久事件提交给 Control Plane。
- Web Console 已使用正式 `@novnc/novnc` RFB Client 和一次性 Ticket API 展示实时
  Canvas，并明确呈现连接协商、实时、断线与失败状态。
- 真实浏览器 E2E 已验证 320×200 像素帧、鼠标、键盘、显式释放、页面离开后的服务端
  Operation 回收，以及只发送 Shift KeyDown、不发送 KeyUp 时的 x11 清键屏障。
- 依赖审计发现新披露的 React Router RSC Action CSRF 与工具链
  `brace-expansion` DoS 后，已迁移至 `react-router 8.3.0` / React 19.2.8 并锁定
  `brace-expansion 5.0.8`；高危依赖审计重新返回无已知漏洞。
- Profile Core/Ephemeral 已分盘，活跃 Profile 实施单 Writer 与 Write Epoch；
  Runtime 停止后通过 Manifest、SHA-256 和 Commit Marker 原子提交检查点。
- Restore 会校验 Manifest 身份、文件路径/数量/大小/哈希与 Marker；损坏检查点测试确认
  不会误报成功，同一 Session 的 Node 重启会复用未检查点工作区。
- Control Plane 新增 Profile Create/List/Get API、V003 元数据迁移和 Session/Profile
  绑定校验；Web Console Profile 页面已删除 Fixture，接入真实创建、查询、搜索和响应式状态 UI。
- 集成测试验证 Session A 经 Crash/Node 重启后提交 Epoch 1，Session B 恢复同一
  Profile 后持久计数到 4 并提交 Epoch 2；跨租户 Profile 访问返回 403。
- 新增 V004 Proxy Allocation 持久化、Static Provider 服务和租户隔离的
  `GET /api/v1/proxies`；Session 启动前绑定 Proxy，并同步递增 Context Epoch 与
  Network Revision。
- Browser Node 新增 Static HTTP Proxy bind/verify/release、本地 Circuit Breaker
  和严格 Endpoint 校验；出口验证失败时不启动 Chromium，也不回退直连。
- Chromium Runtime 强制注入 Proxy Server 与 Loopback Bypass 参数；
  `RuntimeStartedEvent` 回传 Binding、Exit IP、Country 和 ASN，Control Plane
  核对后提交 `BOUND`，终止后提交 `RELEASED`。
- 生产环境同时在 Control Plane 和 Browser Node 拒绝直连开关；Docker Compose
  仅以显式 local override 保留无 Proxy 的开发路径。
- Proxy 页面已删除 Fixture，展示真实 Provider 状态、Allocation 账本、观察出口、
  验证时间和 Direct Fallback 策略，并具备响应式 Loading/Empty/Error 状态。
- 集成测试与 Web E2E 已验证出口 `203.0.113.10`、运行中 `BOUND`、直连
  `DENIED` 和终止 `RELEASED`；基础设施级出口防逃逸仍列为生产阻塞项。
- State Collector 改用稳定 DOM 路径 Target Ref，普通内容变化不再使全部 Target
  随 State Version 失效；导航和显式 Resync 才递增 Target Revision。
- 新增有界 `BrowserStateDiff`、`DiffTruncated` 与 `RequestStateResync` Protobuf；
  Diff 超过 60,000 字节或目标变化预算时显式截断并冻结增量链。
- Control Plane 只在 Base State Version 精确匹配时应用 Diff；Gap/Truncated 会将
  Current State 标记 `INVALID` 并通过 Outbox 自动请求 Full Resync。
- 新增租户隔离、运行态 Gate 和 PostgreSQL 幂等的
  `POST /sessions/{id}:resync-state`；Current State 在新 Full Snapshot 提交前保持
  `RESYNCING`。
- Session 详情新增 Full/Region Resync 控件；早期 Region 的
  `REGION_RESYNC_FULL_FALLBACK` 已由进度 119 替换为原生区域采集和原子 Diff。
- 集成测试验证 Full v1 → Diff v2 → `RESYNCING` → Full Resync v3/`COMPLETE`；
  Web E2E 已通过真实页面发起 Full Resync。
- 新增 V005 `agent_tasks` 持久化及租户隔离的 Agent Task Create/Get/List API；
  幂等创建同时保存受限 Plan 与脱敏后的 Prompt Security Evidence。
- 单一规则 Planner 当前只生成 `navigate`、`get_current_state`、`get_url` 和
  `get_page_summary` 计划；Session 运行态、State Quality、动作上限、计划过期、
  Replan Budget 与精确域名白名单均进入 Plan Validator。
- Intent Guard 拒绝 Cookie/Password/OTP/Shell/Raw CDP/Vault/Kubernetes/Node Helper
  等越权目标；高风险财务/账号目标进入确认需求，但因确认闭环尚未实现而安全阻断。
- 外部网页、邮件、文档与第三方内容默认 `UNTRUSTED` 且不可执行；调用方伪造
  System/Platform Policy 来源会被拒绝，Indirect Prompt Injection 只记录 Hash、
  Taint 与规则命中，不持久化原始正文。
- Agent Goal 入库前执行 Password/Cookie/OTP、Email 和 Phone 基础脱敏；
  API View 不返回签名 Capability Bearer，只暴露不可用作调用的 Token ID。
- HMAC Capability Token 已绑定 Tenant、Session、Intent、Plan Operation、Tool、
  Action、Domain、Data Scope、Risk、Expiry 与单次调用预算；生产环境拒绝默认 Secret。
- Automation 页面已删除 Fixture，连接真实任务 API，展示 Plan/Blocked、风险、
  Capability Handle、验证规则和 Prompt Security Event，并明确标注 Executor 尚未接入。
- 集成测试已验证 V005 迁移、计划幂等、域名越权阻断、Injection 隔离、Bearer 不泄露
  与租户列表；Web E2E 已通过真实 UI 创建计划并观察安全事件。
- 新增 V006 Tool Capability 单次使用账本和 Agent Task 执行结果字段；任务支持
  `PLANNED → RUNNING → COMPLETED/FAILED`，并绑定 `AGENT_INTERACTIVE` 排他 Operation。
- 单 Executor 已真实执行 `get_current_state`、`get_url`、`get_page_summary`；
  每一步在 Tool Service 重新验签，并原子消费 Token，重复 Token 会被拒绝。
- 只读 Tool 强制校验 Tenant、Session、Context Epoch、State Quality 和当前域名；
  URL 输出移除 UserInfo/Query/Fragment，页面摘要限制目标数并执行 Email/Phone Mask。
- Tool Result 保存结构化 Output、SHA-256、Verification 和完成时间；全部 Step 验证后
  才提交 Operation 和 Task，失败则中止 Operation 并保留已完成证据与安全错误码。
- Automation 页面可执行只读及含 Navigate 的安全计划，显示
  `RUNNING → COMPLETED`、每步 Verified 和实际脱敏 Tool Output。
- 集成测试验证三枚 Capability 恰好消费一次、Agent Operation `COMMITTED`、
  Idempotent Replay 与三个 Verified Result；真实 Web E2E 已通过 UI 执行并展示证据。
- 新增 V007 Agent 导航执行状态：持久化 Pending State Version 与 Replan Count；
  Navigate 从同步请求切换为 `RUNNING` 异步等待 Node 回调。
- 新增正式 `AgentNavigateCommand` 与 `AgentNavigationFailedEvent` 三语言契约；
  Capability Bearer 只在 Control Plane 验签并消费，Node 仅接收 Task/Step、目标 URL
  与 Base State Version。
- Browser Node 通过 CDP `Page.navigate` 执行导航，随后强制 Full State；
  成功结果复用 `BrowserStateUpdated`，由 Coordinator Term、Context Epoch 与
  Operation Epoch 绑定当前 Agent 排他 Operation，失败只回传稳定错误码。
- Control Plane 验证 State Version 前进、State Quality 可执行、最终域名精确匹配；
  URL Result 移除 UserInfo、Query 与 Fragment。验证成功后再执行三个只读 Tool，
  最终一次性提交 Operation 与 Task。
- 导航观测不稳定时只在 `replanBudget` 内请求一次绑定原 Operation 的 Full Resync；
  预算耗尽或重定向持续越界会 Abort Operation，并保存稳定失败码。
- Automation 已启用 Navigate 按钮，展示 Replan 已用量、导航及后续只读 Result；
  顶部状态更新为 `Verified read tools · Node Navigate live`。
- 集成测试验证 Navigate Outbox → Node CDP → Full State → 四个 Verified Result、
  四枚 Token 单次消费和 Agent Operation Commit；Web E2E 已从 UI 完成同一流程。
- 新增正式 `AgentActionCommand` / `AgentActionFailedEvent`，Browser Node 真实执行
  `click_target`、`type_text`、`scroll` 和 `wait_for`；Target Ref 与 Revision
  精确绑定，敏感输入目标 Fail Closed。
- Type Text 载荷使用 AES-256-GCM 保存，AAD 绑定 Tenant/Task/Step；Plan、Task View、
  Outbox 和日志均不保存明文，只有投递 Node 前短暂解密。
- 新增 V008 持久 Step、Deadline、Executor Lease、Checkpoint 与 Replan Reason；
  Scheduler 可在进程恢复后继续超时处理，且不会盲目重放 Click/Type 写动作。
- Target Revision 改为只在 URL、目标集合变化或 Full Resync 时前进；稳定页面上的连续
  Click/Type 可执行，真实 DOM 变化仍使旧 Target Fail Closed。
- 新增 V009 平台确认与 Agent Human Handoff 状态；高风险计划进入
  `AWAITING_CONFIRMATION`，平台 Actor 的 Approve/Reject 保存 Evidence Hash，
  网页无法伪造确认事件。
- `request_human_takeover` 已接入真实 HumanTakeover Operation；Agent 停在
  `WAITING_FOR_HUMAN`，只有平台接受后才交出执行权。
- Automation 页面升级为 Agent 执行控制台：三栏任务/证据/Builder 布局、权威 State
  绑定、动作编辑、Durable Deadline/Lease、Confirmation 与 Handoff Gate 均接入真实 API。
- 真实 E2E 已验证 Navigate、Click、Type、Scroll、Stable Wait、高风险确认、人工接管与
  释放，且无浏览器 Console Error 或未预期 HTTP Error。

## 本轮验收证据

已通过：

```bash
make ci
pnpm --dir apps/web-console build
pnpm --dir apps/web-console lint
pnpm --dir apps/web-console test
pnpm --registry=https://registry.npmjs.org --dir apps/web-console audit --audit-level high
make contracts-check
make test-integration
make test-postgres-outage
make test-e2e  # 自动托管 PostgreSQL、Redis、Browser Node、Control Plane 与 Vite
REAL_CHROMIUM_PATH=... cargo test -p runtime-supervisor starts_probes_and_stops_real_chromium -- --ignored
REAL_CHROMIUM_PATH=... cargo test -p state-collector collects_state_from_real_chromium -- --ignored
```

依赖审计结果：`No known vulnerabilities found`。

真实浏览器流程覆盖：

1. 从 Control Plane 加载 Session 列表；
2. 创建第一个 Session 并进入详情；
3. 发起 Start Operation 并自动进入“运行中”；
4. 直接打开协作远程桌面，不创建 HumanTakeover，并观察 `HUMAN READY`；
5. 等待真实 RFB 像素帧并验证鼠标、键盘输入抵达 VNC Server；
6. 离开远程桌面后等待输入释放和 State Resync，Agent Operation 保持不变；
7. 再次连接后仅发送 Shift KeyDown 并制造网络分区，验证服务端清键且不终止 Agent；
8. 终止运行中的 Session，提交 Profile Checkpoint Epoch 1；
9. 在 Browser State 面板发起 Full Resync，并验证后端返回 `QUEUED`；
10. 打开真实 Proxy 页面，验证 Provider `CONFIGURED`、Allocation `BOUND`、
    出口 IP 与 Direct Fallback `DENIED`；
11. 打开真实 Profile 页面并通过创建对话框创建 Profile；
12. 创建第二个从未启动的 Session 并完成空终止，不伪造检查点；
13. 打开真实 Agent 任务页面，从运行中 Session 创建含 Navigate 的受限计划，验证
    `PROMPT_INJECTION_DETECTED` 隔离事件；
14. 通过 UI 执行 Node Navigate 和三个只读 Tool，验证 `RUNNING → COMPLETED`、
    四个 `VERIFIED`、最终域名与脱敏结果；
15. 从权威 Browser State 选择按钮和公开文本框，执行 Click、Type、Scroll、
    Stable Wait，验证四个动作全部提交且输入明文不出现在 Task View；
16. 创建高风险任务，验证 `AWAITING_CONFIRMATION → APPROVED → COMPLETED`；
17. 执行 `request_human_takeover`，验证 `WAITING_FOR_HUMAN`、平台接受、
    HumanTakeover `EXECUTING` 和安全释放；
18. 页面无浏览器控制台错误或未预期 HTTP 错误。
19. 生产模式 Viewer 只能读取：写入口和高权限导航不可见，直接越权进入
    `unauthorized`，Fixture 路由进入 404，并通过 390×844 窄屏验收。

## 分步文档

- [Phase 0：工程、契约与基础设施](progress/01-Phase0-工程契约与基础设施.md)
- [Phase 1—2：Browser Runtime 与 Session Coordinator](progress/02-Phase1-2-Runtime与Coordinator.md)
- [Web Console：UI、真实 API 与端到端联调](progress/03-WebConsole与前后端联调.md)
- [剩余缺口、Gate 与实施顺序](progress/04-剩余缺口与实施顺序.md)
- [Remote Desktop：noVNC、输入与断线闭环](progress/05-RemoteDesktop-noVNC与断线闭环.md)
- [Profile MVP 与恢复 PoC](progress/06-Profile-MVP与恢复PoC.md)
- [Proxy MVP 与出口闭环](progress/07-Proxy-MVP与出口闭环.md)
- [State Gateway Diff 与 Resync MVP](progress/08-State-Gateway-Diff与Resync-MVP.md)
- [Agent 安全计划 MVP](progress/09-Agent安全计划-MVP.md)
- [Agent 只读 Tool 执行 MVP](progress/10-Agent只读Tool执行-MVP.md)
- [Agent Node Navigate 与有限 Replan MVP](progress/11-Agent-Node-Navigate与有限Replan-MVP.md)
- [Agent 结构化动作与持久执行闭环](progress/12-Agent结构化动作与持久执行闭环.md)
- [Agent 人工治理与控制台联调](progress/13-Agent人工治理与控制台联调.md)
- [Phase 5：可靠性、安全与审计](progress/14-Phase5-可靠性安全与审计.md)
- [Phase 6：密度与 Kubernetes](progress/15-Phase6-密度与Kubernetes.md)
- [Phase 4—7：大纲缺口审计](progress/16-Phase4-7-大纲缺口审计.md)
- [Phase 5：Break-glass 与审计覆盖](progress/17-Phase5-Break-glass与审计覆盖.md)
- [Phase 5：Runtime Release 治理](progress/18-Phase5-Runtime-Release治理.md)
- [Phase 5：Key Rotation 治理](progress/19-Phase5-Key-Rotation治理.md)
- [Phase 5：Runtime Ed25519 制品验签](progress/20-Phase5-Runtime-Ed25519验签.md)
- [Phase 5：Network Helper 进程隔离](progress/21-Phase5-Network-Helper进程隔离.md)
- [Phase 5：Storage Helper 进程隔离](progress/22-Phase5-Storage-Helper进程隔离.md)
- [Phase 5：Secure Debug 治理数据面](progress/23-Phase5-Secure-Debug治理数据面.md)
- [Phase 5：签名镜像与 Digest 发布链路](progress/24-Phase5-签名镜像与Digest发布链路.md)
- [Phase 6：Kubernetes Operator 真实集群验收](progress/25-Phase6-Kubernetes-Operator真实集群验收.md)
- [Phase 4：真实网址与 Agent 控制验收](progress/26-Phase4-真实网址与Agent控制验收.md)
- [Web Console：OIDC、RBAC 与生产边界](progress/27-WebConsole-OIDC-RBAC与生产边界.md)
- [Phase 5：PostgreSQL 短时不可用演练](progress/28-Phase5-PostgreSQL短时不可用演练.md)
- [Phase 5：Coordinator Kill 与 Term Fencing 演练](progress/29-Phase5-Coordinator-Kill与Term-Fencing演练.md)
- [Phase 6：Coordinator Capacity Certificate Stage A](progress/30-Phase6-Coordinator-Capacity-Stage-A.md)
- [Phase 5：Object Storage 归档与超时演练](progress/31-Phase5-Object-Storage归档与超时演练.md)
- [Phase 5：Coordinator 进行中 Operation 接管](progress/32-Phase5-Coordinator进行中Operation接管.md)
- [当前未实现清单](progress/33-当前未实现清单.md)
- [Phase 5：Remote Desktop 输入网络分区](progress/34-Phase5-RemoteDesktop网络分区.md)
- [Phase 5：Coordinator Reconcile 可观测性](progress/35-Phase5-Coordinator-Reconcile可观测性.md)
- [Phase 6：Browser Density、资源硬限制与真实 UI](progress/37-Phase6-Browser-Density与真实UI.md)
- [Phase 1：真实 Chromium 500 次生命周期证书](progress/36-Phase1-真实Chromium-500次生命周期证书.md)
- [Phase 7：企业运营核心验收](progress/38-Phase7-企业运营核心验收.md)
- [Phase 4—7：本地最终验收与生产 Gate](progress/39-Phase4-7-本地最终验收与生产Gate.md)
- [Web Console：环境管理与六步创建向导](progress/40-WebConsole-环境管理与六步创建向导.md)
- [Web Console：AUTO 资源策略与详情面板](progress/41-WebConsole-AUTO资源策略与详情面板.md)
- [AUTO 资源遥测与在线执行闭环](progress/42-AUTO资源遥测与在线执行闭环.md)
- [Safe Point、自动休眠与跨节点迁移闭环](progress/43-SafePoint自动休眠与跨节点迁移闭环.md)
- [资源事件 SSE 与断点恢复](progress/44-资源事件SSE与断点恢复.md)
- [Tauri 2 桌面端安全容器与发布基线](progress/45-Tauri2桌面端安全容器与发布基线.md)
- [AUTO 细粒度资源信号与决策迟滞](progress/46-AUTO细粒度资源信号与决策迟滞.md)
- [State Collector 与远程桌面在线执行器](progress/47-StateCollector与远程桌面在线执行器.md)
- [CDP 浏览器活动 Safe Point](progress/48-CDP浏览器活动SafePoint.md)
- [Browser/Profile I/O 真实遥测闭环](progress/49-Browser-ProfileIO真实遥测闭环.md)
- [应用业务安全点 Lease 闭环](progress/50-应用业务安全点Lease闭环.md)
- [应用感知 Business Recovery 与 Ready Gate](progress/51-应用感知Business-Recovery-Ready-Gate.md)
- [Extension 真实加载、遥测与资源权重](progress/52-Extension真实加载遥测与资源权重.md)
- [当前剩余实现与前端技术质量审计](progress/53-当前剩余实现与前端技术质量审计.md)
- [Media 编码遥测与 Slot 执行闭环](progress/54-Media编码遥测与Slot执行闭环.md)
- [Workspace Groups 正式数据模型与 UI](progress/55-Workspace-Groups正式数据模型与UI.md)
- [Business Recovery 有界自动动作闭环](progress/56-Business-Recovery有界自动动作闭环.md)
- [Workspace Tags 正式数据模型与 UI](progress/57-Workspace-Tags正式数据模型与UI.md)
- [Workspace Settings 与创建默认值正式闭环](progress/58-Workspace-Settings与创建默认值正式闭环.md)
- [Agent Policy 一等契约与执行强制](progress/59-Agent-Policy一等契约与执行强制.md)
- [Session Extension 正式绑定与投影](progress/60-Session-Extension正式绑定与投影.md)
- [受信 Extension 自动恢复动作闭环](progress/61-受信Extension自动恢复动作闭环.md)
- [Application Recovery Contract 作者工作区](progress/62-Application-Recovery-Contract作者工作区.md)
- [Hot Tenant 权威路由与 Route Epoch 安全迁移](progress/63-Hot-Tenant权威路由与Route-Epoch安全迁移.md)
- [Node Command 物理分片派发与双重栅栏](progress/64-Node-Command物理分片派发与双重栅栏.md)
- [AUTO 成本趋势与上限前降载闭环](progress/65-AUTO成本趋势与上限前降载闭环.md)
- [AUTO 后台标签冻结与新建标签阻断闭环](progress/66-AUTO后台标签冻结与新建标签阻断闭环.md)
- [AUTO 非特权扩展后台暂停闭环](progress/67-AUTO非特权扩展后台暂停闭环.md)
- [AUTO 成功 Trace 动态采样闭环](progress/68-AUTO成功Trace动态采样闭环.md)
- [AUTO Observer 帧率在线执行闭环](progress/69-AUTO-Observer帧率在线执行闭环.md)
- [AUTO 独立像素录制与停止执行闭环](progress/70-AUTO独立像素录制与停止执行闭环.md)
- [公开资源等级迁移为 Resource Template](progress/71-公开资源等级迁移为Resource-Template.md)
- [AUTO 截图证据与采样执行闭环](progress/72-AUTO截图证据与采样执行闭环.md)
- [Observer 手动截图与受控证据访问闭环](progress/87-Observer手动截图与受控证据访问闭环.md)
- [截图敏感区域遮罩与分类闭环](progress/88-截图敏感区域遮罩与分类闭环.md)
- [工作区通知中心正式闭环](progress/89-工作区通知中心正式闭环.md)
- [用户主题偏好与浅色控制台正式闭环](progress/90-用户主题偏好与浅色控制台正式闭环.md)
- [认证用户菜单与权限入口闭环](progress/91-认证用户菜单与权限入口闭环.md)
- [Session 与 Group/Tag 列表批量投影闭环](progress/92-Session与Group-Tag列表批量投影闭环.md)
- [Application Recovery Contract 版本审批与审计闭环](progress/73-Application-Recovery-Contract版本审批与审计闭环.md)
- [Application Recovery Contract 不可变历史与 Session Rebind](progress/74-Application-Recovery-Contract不可变历史与Session-Rebind.md)
- [Application Recovery Contract 版本 Diff 与受控回滚](progress/75-Application-Recovery-Contract版本Diff与受控回滚.md)
- [Business Recovery Provider Evidence 可信证明闭环](progress/76-Business-Recovery-Provider-Evidence可信证明闭环.md)
- [Environment Saved Views 正式闭环](progress/77-Environment-Saved-Views正式闭环.md)
- [Environment Import 正式闭环](progress/78-Environment-Import正式闭环.md)
- [Profile / Checkpoint Import 正式闭环](progress/79-Profile-Checkpoint-Import正式闭环.md)
- [双 Browser Node、对象存储与迁移集成闭环](progress/80-双BrowserNode对象存储迁移集成闭环.md)
- [迁移目标清理与有界重试闭环](progress/81-迁移目标清理与有界重试闭环.md)
- [AUTO 危险事件即时保护闭环](progress/97-AUTO危险事件即时保护闭环.md)
- [Workspace Overview 权威投影与实时事件闭环](progress/98-Workspace-Overview权威投影与实时事件闭环.md)
- [Kubernetes Operator List/Watch 与 AUTO CRD 闭环](progress/104-Kubernetes-Operator-List-Watch与AUTO-CRD闭环.md)
- [Kubernetes Operator Prometheus 指标与告警闭环](progress/105-Kubernetes-Operator-Prometheus指标与告警闭环.md)
- [Application Adapter 真实 Provider 与业务 Lease SDK 闭环](progress/106-Application-Adapter真实Provider与业务Lease-SDK闭环.md)
- [Business Recovery 浏览器就绪证据闭环](progress/107-Business-Recovery浏览器就绪证据闭环.md)
- [工作区通知可续传 SSE 闭环](progress/108-工作区通知可续传SSE闭环.md)
- [Error Budget Burn Rate 自动发布冻结闭环](progress/109-Error-Budget-Burn-Rate自动发布冻结闭环.md)
- [Runtime Validation Worker 队列与自动矩阵闭环](progress/110-Runtime-Validation-Worker队列与自动矩阵闭环.md)
- [Recovery GameDay Runner 自动演练与恢复栅栏闭环](progress/111-Recovery-GameDay-Runner自动演练与恢复栅栏闭环.md)
- [Recovery GameDay 治理报表、趋势与整改工单闭环](progress/112-Recovery-GameDay治理报表趋势与整改工单闭环.md)
- [独立 Agent Worker 队列与最小权限故障域闭环](progress/113-独立Agent-Worker队列与最小权限故障域闭环.md)
- [Reviewer Agent 与真实模型治理闭环](progress/114-Reviewer-Agent与真实模型治理闭环.md)
- [VNC 与 Agent 协作控制及真人输入优先闭环](progress/115-VNC与Agent协作控制及真人输入优先闭环.md)
- [Terraform Provider 正式 API、协议与签名发布闭环](progress/116-Terraform-Provider正式API协议与签名发布闭环.md)

## 变更日志

| 日期       | 变更                                                                                                                                                                       |
| ---------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 2026-07-23 | 初始文档与代码骨架                                                                                                                                                         |
| 2026-07-23 | 合规整改：补齐构建、契约、CI、数据库、Coordinator/Node PoC、Docker 与集成烟雾测试                                                                                          |
| 2026-07-23 | Web Console 第一阶段：修复现有 UI，接入真实 Session API，完成创建/启动/终止浏览器联调，并将进度文档拆分到 `docs/progress/`                                                 |
| 2026-07-23 | Session 读契约闭环：受控返回展示名、Profile、区域和资源等级，更新列表/详情 UI，并补齐单元、契约与真实集成验证                                                              |
| 2026-07-23 | Node Event 在线闭环：补齐 Runtime Start/Stop 事件 gRPC、Inbox 与版本校验，使 Session 和 Operation 自动进入终态                                                             |
| 2026-07-24 | Runtime/Journal 可靠性：补齐 CDP Readiness、真实页面状态采集、资源统计、Crash Event、SQLite Node Journal 与重启后持久事件重投                                              |
| 2026-07-24 | Browser State/Input 闭环：补齐真实 CDP 输入、状态事件、Control Plane 状态存储/API、Web Console 状态观测与端到端集成验证                                                    |
| 2026-07-26 | Runtime 自动恢复：补齐 Recovery Operation、三次熔断、Runtime Lease、Node 重启对账、孤儿进程清理及双故障集成验证                                                            |
| 2026-07-26 | HumanTakeover 控制闭环：补齐排他 Operation、Actor 所有权、Node 输入屏障、开始/结束 State Resync、Web 控制工作区和真实 E2E                                                  |
| 2026-07-26 | Remote Desktop 数据面：补齐 Xvfb/x11vnc、一次性 Ticket、RFB Gateway、真实 Canvas/输入、心跳、重连宽限与断线 Key Up Loss 服务端回收                                         |
| 2026-07-26 | 前端依赖安全：迁移 React Router 8 / React 19.2，修复新披露的高危依赖并完成真实 E2E 回归                                                                                    |
| 2026-07-26 | Profile 技术 MVP：完成 Core/Ephemeral、单 Writer、Manifest/Marker/Integrity Restore、Control Plane API、真实 UI 与跨 Session 恢复 PoC                                      |
| 2026-07-26 | Proxy 技术 MVP：完成 Static Provider、Allocation/Binding、出口验证、禁止直连回退、本地熔断、Release、真实 UI 与端到端验证                                                  |
| 2026-07-26 | State Gateway MVP：完成稳定 Target Ref、有界 Diff、DiffTruncated、自动/手动 Full Resync、状态质量与真实 UI；Region 暂为显式 Full fallback                                  |
| 2026-07-26 | Agent 安全计划 MVP：完成受限 Planner、Intent/Plan Gate、Prompt Injection 隔离、Capability Token、真实 API/UI 与端到端验证；Executor 与 Tool 执行仍待开发                   |
| 2026-07-26 | Agent 只读执行 MVP：完成排他 Executor、Tool Token 单次账本、三个只读 Tool、结果验证、真实 UI 与端到端证据；Navigate 与 Target Input 待开发                                 |
| 2026-07-26 | Agent Navigate MVP：完成 Outbox/Node CDP 导航、版本化 State 回调、最终域名与结果验证、一次预算内 Full Resync Replan、真实 UI 与端到端证据；Target Input 与完整确认流待开发 |
| 2026-07-26 | Agent 结构化动作与持久执行：完成 Click/Type/Scroll/Wait、敏感载荷加密、Target Revision Gate、持久 Step/Deadline/Lease、恢复与有限 Replan                                   |
| 2026-07-26 | Agent 人工治理与 UI 闭环：完成平台高风险确认、Agent→Human Handoff、三栏执行控制台和真实全链路 E2E；Phase 4 MVP 退出 Gate 关闭                                              |
| 2026-07-26 | Phase 5 核心闭环：完成 OIDC/RBAC/MFA、gRPC mTLS/轮换、Durable Workflow Stage A、防篡改审计、Runtime 供应链 Gate 与安全运行手册                                             |
| 2026-07-26 | Phase 6 基础实现：完成 Shard/Virtual Partition/容量准入核心以及 CRD、Operator、RuntimeClass、CNI/CSI、分池与滚动清单；真实集群压测 Gate 保持未关闭                         |
| 2026-07-26 | Phase 4—7 缺口审计与运营 UI：Runtime/Logs/Security 接入真实 API；明确 Phase 5/6 未关闭和 Phase 7 未实现项                                                                  |
| 2026-07-26 | Phase 5 Break-glass：完成双人审批、限时授权、自动撤销、事后 Review、真实 UI 与新增审计类型                                                                                 |
| 2026-07-26 | Phase 5 Runtime Release：完成 Platform Admin 双人晋级/禁用、发布状态、证据与审计闭环                                                                                       |
| 2026-07-26 | Phase 5 Key Rotation：完成双人轮换、重叠窗口、验证 Gate、真实 UI 与第八类必需审计                                                                                          |
| 2026-07-26 | Phase 5 Runtime 验签：将格式检查升级为受信 Key ID 与 Ed25519 密码学验证                                                                                                    |
| 2026-07-26 | Phase 5 Network Helper 隔离：独立进程、固定有界 IPC、Peer UID、最小权限容器、崩溃 fail-closed 与独立恢复验收                                                               |
| 2026-07-26 | Phase 5 Storage Helper 隔离：独立进程、路径校验、幂等 Checkpoint、独立 Profile 卷与 Helper Kill/恢复验收                                                                   |
| 2026-07-26 | Phase 5 签名发布：四镜像 GHCR Keyless Cosign 签名/SPDX Attestation、SBOM Hash 与 Source Commit 绑定、Digest Kustomize 发布包及 GitHub Artifact 验收通过                    |
| 2026-07-26 | Phase 6 Operator 集群验收：Lease Leader Election、namespaced 调和修复、RuntimeDefault AppArmor 清单和 Kind CRD/RBAC/故障接管/Finalizer E2E 通过                            |
| 2026-07-26 | Phase 4 真实网址验收：真实 Chrome 访问公开网址并执行 Navigate/Read/Type/Scroll，跨域和非授权计划 fail-closed                                                               |
| 2026-07-26 | Web Console 生产身份边界：OIDC 会话、动态 Bearer、角色路由/操作 Gate、Fixture 路由隔离、Viewer 与移动端生产 E2E 通过                                                       |
| 2026-07-26 | Phase 5 PostgreSQL GameDay：断库读写 3 秒内稳定返回脱敏 503，控制面存活；恢复后同一幂等键继续且已提交数据无丢失                                                            |
| 2026-07-26 | Phase 5 Coordinator GameDay：PostgreSQL Lease/CAS Ownership、term fencing、旧事件终止清理，以及 Control Plane Kill 后 term=2 接管与后续 Runtime/Node 恢复通过              |
| 2026-07-26 | Phase 6 Coordinator Capacity Stage A：50k Actor/250k Route/10k 满 Mailbox 的 Build-bound P95/P99、Shard 分布、Hysteresis 与证书 Hash 验收进入 CI                           |
| 2026-07-26 | Phase 5 Object Storage Stage A：S3 压缩归档、Manifest/Commit Marker 顺序、有界超时、本地重试安全和真实 MinIO Pause GameDay 通过                                            |
| 2026-07-26 | Phase 5 Coordinator 进行中接管：HumanTakeover EXECUTING 时 SIGKILL，旧 term Operation 中止、新 term Barrier 重建并提交；Agent 恢复 fail-closed 与 rustls 启动回归修复     |
| 2026-07-26 | Phase 5 Agent Step 换主：Node 暂停形成 Navigate pending，Coordinator B SIGKILL 后 C 自动 claim term=3，旧 Operation 中止、Task 精确失败且后续 Agent 链路继续通过              |
| 2026-07-26 | Phase 5 Runtime STARTING 换主：独立 Session 在 Node Pause 时提交 Start，Coordinator Kill 后新 term Stop Cleanup 提交并收敛 TERMINATED，消除未知结果重放风险                |
| 2026-07-26 | Phase 5 Runtime TERMINATING 换主：RUNNING Session 在 Node Pause 后提交 Stop，Coordinator Kill 后新 term 幂等 Stop 提交，旧 Operation 中止且资源安全收敛                     |
| 2026-07-26 | Phase 5 Runtime RECOVERING 换主：精确 Kill 独立 Runtime、延迟替代 Browser 启动保持 Recovery ACTIVE，Coordinator Kill 后新 term Cleanup 提交并拒绝旧回调                     |
| 2026-07-26 | Phase 5 HumanTakeover Barrier 换主：Node Pause 固定 PREPARING/COMPLETING，Coordinator Kill 后 term=2 重建 Begin/End、旧 Operation 中止且输入屏障完整释放                     |
| 2026-07-26 | Phase 5 Agent 副作用换主：TYPE_TEXT 已执行但 Event 未提交时 Kill Coordinator，term=4 中止旧 Operation、Capability 精确消费一次且后续任务正常                           |
| 2026-07-26 | 按 `docs/outline/` 重新审计全部阶段：新增当前未实现清单，区分代码未实现、Stage A 骨架与生产环境/组织 Gate，并修正最新集成证据                                               |
| 2026-07-26 | Phase 5 Remote Desktop 网络分区：TCP 故障代理冻结输入链路，Gateway 心跳驱逐、服务端 all-keys-up/State Resync 和 Grace 内安全重连通过真实 Web E2E                              |
| 2026-07-26 | Phase 5 Reconcile 可观测性：跨 Coordinator 世代验证延迟/中止/Cleanup/失败指标，交付 Prometheus 端点、三条告警规则和故障响应 Runbook                                               |
| 2026-07-26 | Phase 1 真实 Chromium 容量：Chrome 150 连续启动/健康采样/停止 500 次，Start P99 708ms、FD 增长 0、残留进程 0，生成 Build-bound Hash 证书                                         |
| 2026-07-26 | Phase 6 Browser Density：Chrome 150 完成 500 次、并发 4 的容量证书；Media Slot、Extension 自适应采样、PSI 有界驱逐和多 Node 精确路由闭环                                      |
| 2026-07-26 | Phase 6 N/N-1：Kind 真实执行旧提交 Operator、Leader Kill、升级 N、回滚 N-1，既有/新增 Session 均 Ready，创建 4 次、终止 1 次                                                   |
| 2026-07-26 | Phase 7 核心运营：Validation/Replay/Compatibility、Cost/SLO/Retention/Residency/Compliance/GameDay、License/Audit Export、Terraform Module、四语言 SDK 与企业 UI 闭环         |
| 2026-07-27 | AUTO 资源执行：Browser Node 5 秒 CPU/内存/PSI 遥测、30 秒窗口与快扩慢缩、Cgroup 在线调整、Node ACK/Operation 提交和超时回收闭环；修复 GitHub Kind 浅克隆与宿主 PSI 随机失败 |
| 2026-07-28 | Tauri 2 桌面基线：共享 Web UI、最小 Capability/CSP、OS Vault、系统浏览器 OIDC/Deep Link、托盘/单实例、签名 Updater 门禁、macOS 本机构建与 macOS/Windows CI |
| 2026-07-28 | AUTO 细粒度闭环：补齐 Renderer/Tab/主线程/Agent/State Diff/Remote Desktop 信号、State Collector/Remote Desktop 在线执行器及真实 ACK/回滚 |
| 2026-07-28 | CDP 浏览器活动 Safe Point：真实上传/下载/导航表单信号、能力协商滚动兼容、V028 安全迁移、PostgreSQL/Browser Node/真实 Chrome 验收 |
| 2026-07-28 | Browser/Profile I/O：Cgroup v2 Browser 子组 `io.stat` 真实速率、能力协商、P95/EWMA 决策、API/UI 和本机集成验收 |
| 2026-07-28 | 应用业务安全 Lease：可信 Adapter 显式声明（非网页语义检测）、Owner/TTL/幂等 PostgreSQL Lease、Safe Point 与迁移行锁屏障、V029 事件流和真实集成验收 |
| 2026-07-28 | Coordinator Exactly-once 集成稳定性：以本次 Operation ID 精确定位 AgentAction Outbox 命令，消除跨历史命令误判导致的 CI 偶发失败 |
| 2026-07-28 | 应用感知 Business Recovery：V030 版本化契约/Session 绑定/持久 Verdict、迁移 Ready Gate、真实 API/Web、跨租户与幂等集成验收；修复 CDP 无名 Target 500 |
| 2026-07-28 | Extension 资源闭环：可信目录真实加载、独立 Cgroup 聚合 CPU/RSS、V031 权重、Operation/Node ACK、Web 展示和无 Cgroup 不伪造集成验收 |
| 2026-07-28 | 当前剩余实现审计：确认 Media Producer/Slot 为下一代码主线，补记 Groups/Settings/Hot Tenant/自动恢复的真实边界，并记录 Web 13/20 技术质量基线 |
| 2026-07-28 | Media 编码闭环：x11vnc 独立 Cgroup CPU Producer、V032 当前/预留 Slot、AUTO Operation/ACK/回滚、API/UI、N/N-1 与完整集成验收 |
| 2026-07-28 | Workspace Groups：V033 PostgreSQL 权威模型、Session 归属与 AUTO 默认策略继承、正式 API/UI、RBAC/幂等/审计和完整集成验收 |
| 2026-07-28 | Business Recovery 有界动作：V034 Attempt/命令/State Version、受限 CDP 动作、Node State ACK、二次 Ready Gate、API/UI、N/N-1 与真实集成验收 |
| 2026-07-28 | Workspace Tags：V035 租户权威模型、复合外键隔离、旧 Metadata 回填、Session 多对多归属、正式 API/UI、RBAC/幂等/审计和完整集成验收 |
| 2026-07-28 | Workspace Settings：V036 租户权威默认值、Runtime/HumanTakeover 正式创建绑定、RBAC/幂等/审计、真实 Settings/UI 与完整集成验收 |
| 2026-07-28 | Agent Policy：V037 Session/Task 不可变绑定、旧 Metadata 回填、Planner/Executor/Handoff 强制、正式 API/UI 投影与完整集成验收 |
| 2026-07-28 | Session Extension：V038 不可变创建绑定、旧 Demand 回填、Placement 一致性、正式 List/Detail/UI 投影与完整集成验收 |
| 2026-07-28 | 受信 Extension 自动恢复：V039 Contract/Placement 目标 Gate、Extension CDP `chrome.runtime.reload()`、State ACK、二次 Ready、API/UI 与完整集成验收 |
| 2026-07-28 | Recovery Contract 作者工作区：真实 Admin API、expectedVersion CAS、声明式证据/动作编辑、13 文件/42 测试和 1440/390 浏览器验收 |
| 2026-07-29 | Node Command 物理分片派发：V041 Route/Claim、Deployment Worker Lease、Rendezvous Hash、SKIP LOCKED、Node Route+Term 原子栅栏、任意 Pod Event 入口与完整故障切换验收 |
| 2026-07-29 | AUTO 成本与上限降载：V042 五分钟真实成本趋势、maximumCost 执行、费率缺失 fail-closed、一次性 Node 非核心预算降载、API/UI 与完整集成验收 |
| 2026-07-29 | AUTO 标签页保护：V043 权威状态、后台 Page CDP 冻结短 Lease、新建 Target 持续阻断、Node ACK/回滚、API/UI、N/N−1 与完整集成验收 |
| 2026-07-29 | AUTO 扩展后台保护：V044 非特权 Profile 选择、Extension Target CDP pause/resume、重启持续抑制、Node ACK/API/UI 与完整集成验收 |
| 2026-07-29 | AUTO 成功 Trace 背压：V045 Session 级稳定采样、上限时 100%→10%、失败与强制证据保留、Node ACK/API/UI、N/N−1 与完整集成验收 |
| 2026-07-29 | AUTO Observer 帧率：V046 权威 FPS、桌面 30→5/无桌面 0、真实 Gateway 非阻塞输入节流、回滚/ACK/API/UI、N/N−1 与完整集成验收 |
| 2026-07-29 | AUTO 独立像素录制：V047 请求/当前状态、CDP Screencast、有界分段、Storage Helper commit-last、上限真实停止、回滚/ACK/API/UI 与 MinIO 验收 |
| 2026-07-29 | 公开资源模板迁移：V048 N−1 同步 Trigger、Session/Placement/费率/成本解释/OpenAPI/Web/四 SDK 移除公开 L0–L5，创建统一 AUTO |
| 2026-07-29 | AUTO 截图证据：V049 成功截图 100%→10% Node 执行器、真实 CDP JPEG、Storage Helper commit-last、FAILED 事件、租户 API 与 Session Detail；GitHub CI `30453888344` 的完整集成、对象存储 GameDay、Operator E2E 和 Desktop `30453888166` 的 macOS/Windows 构建通过 |
| 2026-07-30 | Observer 受控证据：V062 手动 Capture Request/Outbox、真实 CDP JPEG、Storage Helper commit-last、Purpose-bound 五分钟 Grant、同操作者一次性 Redeem、60 秒签名 GET、Web/Tauri 共享 UI、RBAC/幂等/审计与 PostgreSQL/mTLS/MinIO Integration |
| 2026-07-31 | Evidence 隐私保护：统一 Agent/Observer 敏感区域不透明遮罩、页面脚本暂停与冻结后 CDP DOM 复核、扩展 State 敏感分类、V063 Redaction State/Count、N−1 Legacy 标记和 Web/Tauri 状态展示 |
| 2026-07-31 | 工作区通知中心：V064 高信号 Audit 事务投影、Tenant/Actor 单调已读游标、90 天有界保留、正式 API/RBAC、Web/Tauri 共享抽屉与完整 Integration |
| 2026-07-31 | 用户主题偏好：V065 Tenant/Actor PostgreSQL 持久化、SYSTEM/DARK/LIGHT、幂等版本、正式 API/Audit、Web/Tauri 共享主题层、浅色 OKLCH Token 与 1440/390 验收 |
| 2026-07-31 | 认证用户菜单：真实 Tenant/Actor/Role 与认证模式展示、权限化设置/安全入口、OIDC 退出、本地开发边界、Web/Tauri 复用及 1440/390/键盘验收 |
| 2026-07-31 | Session 与 Group/Tag 列表批量投影：批量 Context/Ownership/Operation/Tag/Proxy 读取、Tenant 范围成员投影及逐项查询回退测试 |
| 2026-07-29 | Recovery Contract 审批：V050 精确版本双人审批、证据哈希与审计链、Session 版本固定、旧绑定拒绝新策略、N−1 fail-closed、正式 Web 工作区和完整 PostgreSQL/Chromium Integration 通过 |
| 2026-07-29 | Recovery Contract 历史与升级：V051 不可变 Revision 正文、精确版本继续运行、Session Rebind Operation、幂等/CAS/审计、正式 Web 入口和完整 PostgreSQL/Chromium Integration 通过 |
| 2026-07-30 | Recovery Contract Diff 与受控回滚：不可变版本列表、服务端字段 Diff、已批准历史生成新 DRAFT、CAS/幂等/审计、两步 Web 恢复和完整 PostgreSQL/Browser/Viewer E2E 通过 |
| 2026-07-30 | Business Recovery Provider Evidence：V052 精确 Revision 要求、受信 Adapter 写入、短 TTL/状态绑定、迁移 fail-closed、只读 Web 账本、幂等/审计和完整 PostgreSQL/Chromium Integration 通过 |
| 2026-07-30 | 双 Browser Node 迁移：两个独立 Node/Storage Helper、共享 MinIO、AUTO 压力/Safe Point、Checkpoint/Restore/State Resync/Business Validation 和跨 Node Browser Generation Fencing 集成通过 |
| 2026-07-30 | 混版迁移能力准入：Node 声明 Generation Floor v1，PostgreSQL 候选与服务层双重过滤 N−1 目标，不可达旧 Node 集成夹具、兼容 Gate 和双节点迁移通过 |
| 2026-07-30 | 迁移目标失败闭环：V056 目标尝试/失败节点账本、Cleanup Operation/RuntimeStopped 屏障、最多三次重试、三 Node Storage Helper 故障注入、API/UI 与完整 Integration 通过 |
| 2026-08-01 | AUTO 危险事件：真实 Cgroup OOM 计数、Profile 文件系统容量、持续 CDP Unresponsive 与 OOM Crash 分类；立即 Agent Pause、OOM Operation/ACK 扩容、磁盘降载后受控终止、隔离失效持久终止派发闭环 |
| 2026-08-01 | Workspace Overview：V070 权威 PostgreSQL 聚合、权限化 Node 容量、Payload-minimized 可续传 SSE、前端实时重取、Agent 暂停状态约束修复与 1440/390 真实验收 |
| 2026-08-01 | Proxy 主动健康：Browser Node 隔离出口探测、V071 七天样本、3/2 Hysteresis、成功/延迟 EWMA、质量/新鲜度 API/UI、合并式 Workspace SSE 与 1440/390 真实验收 |
| 2026-08-08 | Proxy 冷 Binding：V072 PostgreSQL 分布式租约/Revision 栅栏、能力化 mTLS Node RPC、隔离 Network Helper 临时绑定、无 Session 样本、差异化重试及完整 Integration 验收 |
| 2026-08-08 | OpenAPI TypeScript SDK：159 个唯一 Operation、32 服务、230 Model 自动生成，独立锁与 SHA-256 Manifest 漂移门禁、Node ESM/实例隔离/运行时/发布 Tarball 验收 |
| 2026-08-08 | Proxy 多 Provider 路由：V073 健康新鲜度/质量/信誉/成本/地域/容量硬 Gate 与稳定评分、悲观锁容量预留、fail-closed、完整决策证据和真实 UI/Integration 验收 |
| 2026-08-08 | 四语言 SDK 发布门禁：Python/Go/Java 生成 159 Operation/215 Schema，统一版本与确定性 8 制品、SHA256SUMS、身份边界测试和 GitHub OIDC Provenance Release 流水线 |
| 2026-08-08 | Kubernetes Operator List/Watch：一致分页、resourceVersion/bookmark、410 重列举、定期 resync、有界退避、稳定状态无反馈 PATCH、AUTO CRD 与真实 Kind N/N-1 验收 |
| 2026-08-08 | Kubernetes Operator 可观测性：低基数 Prometheus 指标、固定内存 reconcile Histogram、Metrics Service、七条告警、事故 Runbook 和真实 Kind Leader 指标抓取 |
| 2026-08-08 | Application Adapter：最小权限真实 Provider HTTP 读取、0600 短期 Token、JSON Pointer/本地 SHA-256、业务 Lease SDK、签名镜像供应链与完整迁移 Integration |
| 2026-08-08 | Business Recovery 浏览器就绪证据：真实 Document Ready、持续 CDP Network Quiet、Alert/Status/Dialog 阻断、V074 不可变契约、公开 API/UI/四语言 SDK 和 N/N−1 Gate |
| 2026-08-08 | 工作区通知实时流：Tenant Audit Sequence 可续传 SSE、Replay/Reset/连接上限、Web/Tauri 断线状态、159 Operation 四语言 SDK；修复 Network Quiet 状态版本竞态并通过完整 Integration/Object Storage GameDay |
| 2026-08-09 | Error Budget 自动发布冻结：V075 PostgreSQL 权威 Gate、上下阈值与稳定恢复、30 秒多 Control Plane 串行评估、Runtime 申请/审批双阻断、Emergency Disable 旁路、Audit/API/UI/160 Operation 四语言 SDK、N/N-1 与完整 Integration |
| 2026-08-09 | Runtime Validation 自动矩阵：V076 PostgreSQL 队列/不可变事件、Worker 能力匹配、Token/Epoch/Lease 栅栏、重试/耗尽、固定 Chromium Runner、Sandbox K8s、166 Operation 四语言 SDK、七镜像签名发布、完整 Integration 与真实 URL/Chrome Evidence COMMIT |
| 2026-08-09 | Recovery GameDay Runner：V077 PostgreSQL 队列/不可变事件、固定场景与 Blast Radius、Token/Epoch/Lease 栅栏、故障后强制 Recovery-only Claim、Sandbox K8s、174 Operation 四语言 SDK、七组件签名镜像/八 Workload 引用发布和完整 Integration；Coordinator 接管清理测试统一为 60 秒生产恢复门禁 |
| 2026-08-09 | Recovery GameDay 治理：V078 事件 Keyset 分页、SHA-256/HMAC 签名报告、失败/中止自动整改工单、ACK/RESOLVED Audit、90 天场景趋势、真实 UI、180 Operation/238 Schema 四语言 SDK 与完整 PostgreSQL Integration |
| 2026-08-09 | 独立 Agent Worker：V079 PostgreSQL 权威队列/不可变事件、固定最小 IPC、Token Hash/Epoch/Lease 栅栏、Kata Sandbox/CP-only 网络、第八签名组件镜像、185 Operation/243 Schema 四语言 SDK 与真实 Python Worker Integration |
| 2026-08-09 | Reviewer Agent：V080 PostgreSQL 权威队列/不可变事件、Plan Hash 与 Claim Token/Epoch/Lease 栅栏、固定 OpenAI Responses Provider、模型版本/数据/Token/成本治理、独立 Kata Worker 与受控 Egress、190 Operation/253 Schema 四语言 SDK、真实 Python Reviewer/模型 HTTP Integration |
| 2026-08-09 | Coordinator Owner 切换窗口：入口路由、PostgreSQL Inbox Claim、Command Processor 与 SessionCoordinator 统一物理 Shard/逻辑 Owner 规则，独立 Reviewer Session + 主 Session Crash/Node Restart 完整 Integration 通过 |
| 2026-08-10 | VNC/Agent 协作控制：Session-bound 协作票据、真实 RFB 输入识别、真人两秒优先、Agent 命令无损延后/自动续行、断线不结束 Agent、Web/Tauri 协作 UI 与定向三栈验证 |
| 2026-08-11 | VNC/Agent 并发闭环：V081 `executionWait` 权威投影、Session SSE 失效、四语言 SDK、真人连续输入/Agent 同 Operation 自动续行/RFB 持久在线双页面 E2E |
| 2026-08-11 | Terraform Provider：Protocol 6、正式 HTTPS/Bearer API、Group/Tag CRUD 与精确归属、Workspace Settings 数据源、CI/race/vet、GPG checksum 与 OIDC Provenance 发布门禁 |
| 2026-08-11 | VNC 多协作者：8 连接有界 Lease、x11vnc/noVNC Shared、签名只读票据与 Node 输入拒绝、最后断线清键、显式接管主动撤销协作者、Web/Tauri 模式切换 |
| 2026-08-11 | State Resync 治理：Node 持久事件高水位与单一 Barrier、V082 Session/Tenant 加权请求预算、自动 Full 循环断路、429/Retry-After、审计与 N/N−1/完整 Integration |
| 2026-08-11 | Full State Snapshot 流：16 KiB/32 块/512 KiB 边界、逐块与整流 SHA-256、Node Journal 原子有序重投、V083 PostgreSQL 组装/Commit/过期清理与完整 Integration |
| 2026-08-12 | State Resync 多维预算：V084—V086 安全迁移、Session/Tenant/Node/Region 四维准入、Full/Region 实际字节与 Browser cgroup CPU 结算、N/N−1 保守回退与完整 Integration |
